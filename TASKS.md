# TASKS.md — Master Task List

Anti-shuffle file. When you ship something, move it to DONE with a date. Never delete ideas — ICEBOX with a reason.

## KEEPER'S NEXT SESSION (written 2026-07-28 for tomorrow)

### A. SCREENSHOT THESE — four things nobody here can see

1. **AN OPERATOR APPROVAL PROMPT.** The Part 5 blocker, and the only
   one that could kill a feature. Whatever your setup shows a human
   when an agent wants to spend money — Claude Code, a clawdbot, any
   MCP client. THREE THINGS TO CAPTURE: how much text it shows before
   truncating; whether links are clickable or dead; and whether it
   passes any merchant metadata through at all or only amount +
   recipient. If it's only amount and recipient, the approval-prompt
   artifact belongs in the item description instead and we should not
   build it where Part 5 assumed. Better to learn that from one
   screenshot than from a week of work
2. ~~/admin/declines~~ — **READ 2026-07-28.** All three rows:
   `small_blessing` · reason `unspecified` · stage **verify** · client
   `curl/8.18.0`. THREE THINGS FOLLOW. (a) The reason is unrecoverable
   — these rows predate the per-request slot, so the nonce join lost
   it, which is exactly the failure the fix addressed and exactly why
   it was worth fixing. There is nothing further to extract from them.
   (b) STAGE = VERIFY means the signature never cleared; it never
   reached settle, so this was not a funds problem. (c) THE ITEM IS THE
   TELL: `small_blessing` is the half-cent practice item, bought three
   times with curl. That is not a customer buying a thing, it is
   SOMEBODY TESTING AN X402 CLIENT — the x402-client-builder persona
   DEMAND.md named as the most likely first real buyer. They were
   trying to prove their client worked, against us, and we could not
   tell them why it didn't
3. **THE x402scan / agentic.market SCORECARD**, post-deploy. The
   serviceName, tags, iconUrl and per-resource inputSchema all shipped
   unverified against a live catalog for exactly that reason. Did the
   fields land? Did "input schema: no" change?
4. **agentic.market's endpoint list.** Seven items were invisible
   there. Did any of them get ingested after this week's changes?

### B. BUY THESE — four tests, about $1.30 total

1. **`ITEMS=small_blessing npm run shop`** — SCOPED, because a bare
   `npm run shop` buys THE WHOLE CATALOGUE and that is a real bill.
   Half a cent, and deliberately the SAME ITEM the bouncing client
   tried. Not a purchase for its own sake: it VETS THE DOOR, which is
   the only half of the bounce question that is answerable. If a
   client you control settles, the decline was theirs and you can
   close it. If you bounce too, you have reproduced a real buyer's
   failure with full visibility
2. **`ITEMS=settlement_attestation npm run shop`**, $0.004. Never
   bought in production and it is the only item that makes an OUTBOUND
   CALL (Base RPC), so it is the only one whose live failure mode no
   test in this repo can reach. The shopping run now carries the real
   founding hash for it, so a correct answer is SETTLED. NOT_FOUND
   means the RPC path is broken
3. **`ITEMS=graffiti_on_a_train npm run shop`**, $1 minimum. Exercises the review queue
   end to end: buy, then approve or decline it at /admin/counter and
   confirm the certificate is untouched either way. That promise is
   tested in CI and has never been walked by a human
4. **`ITEMS=phantom_check npm run shop`**, $0.25 — THE CHEAP EXPERIMENT already filed
   below for the seven invisible items. One deliberate purchase, then
   watch for a few days whether Bazaar ingests it. Rule 13 forbids
   automated self-purchase, not a considered test

House purchases are excluded from organic counts, so none of this
pollutes the ledger. All four are diagnostics.

### C. THE DUPLICATION PROBLEM — real, and partly mine

Twenty-one markdown files, ~4,400 lines, and the same facts live in
several of them. The dupes, named:

- **TASKS.md vs GROWTH_TASKS.md** — both read as live queues and
  carry the same A1–A4 / B / C items. Worst offender
- **STATUS.md's "what's open" vs TASKS.md** — I created a THIRD place
  that says what is open, one day ago, while trying to fix this exact
  problem
- **DEMAND.md vs DEMAND_SYNTHESIS.md** — two demand-side strategy
  documents with overlapping conclusions
- **PROJECT_LOG vs LEDGER_READINGS** — genuinely distinct (events vs
  book readings) and worth keeping apart. Not a dupe

PROPOSED RULE, one line each, ⚑ for the keeper's nod:

- **ONE QUEUE: TASKS.md.** GROWTH_TASKS becomes a frozen SOURCE
  document — the keeper's original framing, cited and never ticked
- **ONE CURRENT STATE: STATUS.md**, which POINTS at the queue and
  never restates it. Anything in STATUS that a reader could act on
  belongs in TASKS instead
- **STRATEGY DOCS ARE FROZEN SOURCES.** DEMAND, DEMAND_SYNTHESIS,
  READINESS, PRIORS_MAP, THE_PARTNERSHIP get cited, not edited. A doc
  that is both an argument and a checklist becomes neither
- **Every doc gets one line at the top saying what it is FOR and what
  it is NOT for**, which is the cheapest possible dupe prevention

⚑ Say the word and the partner does the consolidation in one pass.
It is mechanical once the rule is agreed, and it should not be done
without the rule being agreed first.

## MOVE 1 — SETTLEMENT ATTESTATION, SHIPPED 2026-07-28

- [x] BUILT to the DEMAND_SYNTHESIS Part 7 spec. `settlement_attestation`, $0.004 fixed (sub-approval-threshold as specified), instant, utility shelf. Give it `tx_hash` and optionally `payer` / `recipient` / `nonce` / `amount_usdc`; it reads Base ONCE and signs what it found: SETTLED / NOT_FOUND / PENDING_FINALITY / INSUFFICIENT_MATCH / REVERTED
- [x] STATELESS BY CONSTRUCTION, which is the product's shape and not an implementation shortcut: one RPC read, no DB, no poll, NO RETRY, no custody, no contract. Retrying until the answer improves would turn an observation into a poll, and a poll into an implied promise that we waited for the right answer. If the chain says NOT_FOUND at the instant we looked, that is what gets signed
- [x] THE OUTPUT IS SELF-VERIFYING — signed on its own like the trust list, so a holder checks it against /.well-known/scvd-signing-key WITHOUT ASKING US, which is the whole point of a disinterested receipt. The purchase also mints the usual certificate at /api/verify
- [x] HONESTY GUARDS, CI-ENFORCED (the auto-refund lesson pre-empted, as the spec asked). The listing states what it does NOT do — no delivery attestation, no promise a NOT_FOUND won't settle later, no dispute resolution. And a test FAILS THE BUILD if the copy ever implies a human looked: automated and disinterested IS the value, and "the keeper checked" would make the artifact worth less, because a keeper is a party to the store. `why_use` is the spec's sentence verbatim, load-bearing clause intact
- [x] THE EVENT TOPICS ARE DERIVED IN CI — Transfer and AuthorizationUsed hashes are hardcoded in the Worker (no hashing dependency) and re-derived from their signatures with viem in the test, so a typo fails the build instead of silently classifying every settled payment as NOT_FOUND
- [x] Pinned counts updated same commit: MCP tool count 26→27, S1 ladder order. BASE_RPC_URL added to wrangler vars, defaulting to the public endpoint (not a secret; the store reads only)
- [x] CV'S EDITS PULLED IN 2026-07-28 — five deltas against the first build. (1) THE NORTH STAR is now quoted VERBATIM in the attestation docstring: arXiv:2605.11781 specifying the product back at us. It lives in the registrar's voice and NOWHERE a customer reads — a test asserts the citation never reaches the listing copy, the 402 line, or llms.txt, because the keeper's voice writes the counter line and neither register borrows the other's authority. (2) KILL CRITERIA moved INTO the spec docstring, before shipping rather than after, so the decision to stop is already made and does not have to be argued for by whoever is holding the sunk cost. (3) SHARED PARSING, done honestly: the input is query params, so extractPaymentNonce had no natural seam — until the obvious one, which is that a caller checking their OWN payment already holds the payload they sent. `payment_payload` now takes the base64 PAYMENT-SIGNATURE verbatim and reads the nonce out with the replay guard's own function. Real reuse and real convenience, rather than a dependency added to satisfy a line item. (4) VERIFIABLE AT THE EXISTING /api/verify, as specified, with NO new endpoint: the observation's evidence_hash is bound into the purchase certificate, so /api/verify/{cert_id} answers for the attestation too. This is why settlement_attestation is the one item that OBSERVES BEFORE IT MINTS — the hash has to exist before the certificate can carry it. (5) A test now fails if the manifest ever makes the POSITIVE delivery claim, not just if it omits the disclaimer, same class as claim-chain's refund check
- [x] ATTRIBUTION — inherited, not built. gateSignals already reads ?src= / ?source= on every paid route and writes declared_source onto the event row, so an attestation call mentioned in an HN reply or a Moltbook thread shows up in the venue column with no new code. Verified rather than added
- [x] VALIDATED AGAINST THE CHAIN 2026-07-30 by CV, both cases, and the negative came back stronger than the test he designed. POSITIVE: a real tx hash from his own AERO swap returned SETTLED with the payer, recipient and $20 amount all correctly matched at block 49267842, 23,995 confirmations — correct rather than merely plausible, which is the only version of this that counts for a product whose whole value is that the reading is true. `cert_kdm66dm6vj`, patron #32. NEGATIVE: a fake hash did not false-positive and did not error — the input guard refused it BEFORE the chain read and before any charge, saying so out loud ("Nothing charged, send the real one"). He was expecting to exercise NOT_FOUND and instead found the harder property already working: don't take money for garbage input. HIS BOUNDARY WAS THE USEFUL PART and he stated it unprompted: neither case reached the middle path, a syntactically perfect hash that was never broadcast, which is the ONLY path where the store takes the money and returns a negative. CLOSED ON OUR SIDE SAME NIGHT with five tests against a stubbed RPC (a null receipt is exactly what Base returns for an unknown hash): NOT_FOUND with every chain field null rather than guessed, the head still reported so the negative is dated, the reading refusing to promise the payment will never settle, THE NEGATIVE SIGNED AS READILY AS THE POSITIVE (an unsigned negative is worth nothing, and only standing behind good news is the failure mode), and REVERTED held distinct from NOT_FOUND, since a reverted transaction exists and moved nothing while a never-broadcast one is a different fact about the world. The live half is one $0.004 buy whenever he wants it, and the hash is derived rather than hand-invented so it needs no constructing: `0x79a05ba38a9a6c706db9d15e4b1cb68e8a6d156a2a52708204d4f4ee505bc918`, keccak256 of a sentence, exported from the spec as NEVER_BROADCAST_TX_HASH
- [ ] ⚑ KILL CRITERIA, from the spec, to be checked ~2026-08-27: near-zero calls in 30 days = demand unproven, PARK IT. A Base clone at ≤$0.002 = thin moat realized, stop investing. Double down ONLY if agents call it inside retry/reconciliation loops — that is the signal the whole move was a test for. The decline desk and /admin/census are where the calls will show

## NAMING CONSISTENCY PASS (2026-07-28, pinned law — see PROJECT_LOG PINNED DECISIONS)

- [x] THE TWO FIELDS — .well-known/x402.json top-level `name` and menu.json `store.name` both carried the tier-3 full name; both now read "SCVD General Store". menu.json's came in through a `...STORE_METADATA` spread, so it is overridden after the spread with a comment saying why, or the next person re-adds the bug by widening the spread
- [x] 402 serviceName CONFIRMED, NOT CHANGED — already `STORE_SERVICE_NAME` via storeServiceMetadata() in payments.ts, on every buy route and every penny page. Tier-2 correct as audited
- [x] ONE MORE THE LAW ENUMERATES — JSON-LD is named in the tier-2 list and was carrying the full name. Now the display name, with the full name moved into `alternateName` so an entity resolver can still corroborate the lore without us being FILED under it
- [x] THE LAW IS A TEST — test/naming-law.spec.ts pins all three strings, asserts the display name is character-for-character identical across x402.json and menu.json, asserts the full name appears in NEITHER discovery document, and asserts skill.md uses the machine identifier and not a display string. A future surface that picks a name by taste fails here instead of shipping
- [ ] ⚑ THREE FIELDS THE LAW'S RATIONALE COVERS BUT ITS ENUMERATION DOES NOT — flagged, not fixed, per "when in doubt, flag, don't fix." Each is a human-readable metadata field read by a discovery system, which is exactly what tier 2 is for, but none is on the enumerated list: (1) openapi.json `info.title` — x402scan reads this document and verifies origin ownership from it, so it is the strongest candidate; (2) site.webmanifest `name` (routes/favicon.ts); (3) MCP `serverInfo.title` — the law fixes MCP server NAME as tier 1 and is silent on title, which sits beside it reading the full name. One ruling covers all three
- [ ] ⚑ TRUST LIST `issuer` — reads STORE_METADATA.name and is INSIDE THE SIGNED PAYLOAD. Changing it changes what the signature covers, so it is deliberately untouched pending a ruling. Not obviously metadata, not obviously lore

## URGENT — A REAL BUYER BOUNCED (2026-07-28)

- [x] THE ORPHANED OFFICE — the keeper could not reach half his own back room. The nav listed THREE rooms while EIGHT pages existed, and every reading (census, recount, declines, bell) rendered itself as tab "office", which draws "The desk" as UN-CLICKABLE BOLD. So landing on any reading left no link back to anything: the only ways out were the browser's back button and "Front of house." The readings were reachable solely through links buried in prose on the desk, which is a scavenger hunt, not navigation. FIXED: every page names itself, a second nav row lists all five readings (declines first, since it is the only page that measures somebody trying to buy), and /admin/digest — which returned bare JSON and was therefore a one-way trip — now renders in the shell for a browser while KEEPING JSON AS THE DEFAULT, because the route was JSON-only and something scripted may be reading it. Four tests sweep every page: each reaches every other, each marks only itself, each keeps a way out, each stays behind the gate

- [x] THE DECLINE DESK — BUILT at /admin/declines. curl/8.18.0 presented an actual payment signature three times today and was declined all three. It is THE ONLY non-house client that has ever presented a signature at all. The reasons were being recorded the whole time (recordPaymentDecline writes them to the event row's note) and NOTHING RENDERED THEM, so the single most actionable fact the store can produce was sitting in raw KV. The page leads with the facilitator's string VERBATIM and puts our reading beside it, labelled as a reading — a wrong guess about someone else's error code must never be able to hide the code itself. It splits verify-stage from settle-stage (a settle failure is worse: the buyer believes they paid) and calls whose problem it is: buyer, OURS, or unknown. "Ours" is red, because that is money the store turned away for a reason of its own making. It also traces the client's whole sequence oldest-first, since when a real buyer bounces the ORDER is the evidence
- [x] THE FALSE LINE — /admin/census said "the decline reasons are on the desk." There was no desk. Written by the same hand that built the census; the same class of error as the auto-refund, caught the same way (somebody went looking and hit a 404). Now points at the real page, and a test asserts the link exists on every page that promises it
- [x] THE REPLAY HOLE — the nonce-already-spent path refused a signed payment and RECORDED NOTHING. A buyer retrying an authorization instead of re-signing was invisible in the books, which is exactly the shape of a real buyer bouncing repeatedly off a fixable wall. Now booked as `replay:nonce_already_settled`
- [x] THE ALARM — an outside decline now raises `payment_declined` on the spot, deduped by item+reason. This cost a day: a real buyer bounced and nothing anywhere raised a hand. It is the rarest event the store can have and it was the only one with no alarm on it
- [ ] ⚑ KEEPER, AFTER DEPLOY: open /admin/declines and read the three rows. WHAT TO LOOK FOR, in order — (1) if the reason column says `unspecified`, the INSTRUMENT is the problem, not the buyer: verify-side reasons live in an in-isolate Map keyed by nonce, so a retry landing on another isolate loses it, and the fix is to persist the reason at write time rather than trust the map; (2) if it says anything with `network`, `scheme`, `recipient`, `payTo` or `amount`, it is OURS — what we published disagrees with what we accept, and the fix is in /.well-known/x402.json or the accepts block; (3) if it says `insufficient_funds`, the buyer's wallet was empty and there is nothing to fix, which is the good kind of decline. Whatever it says, the raw string is the fact and the reading beside it is a guess — trust the string
- [x] REASON PERSISTENCE — FIXED 2026-07-28, and the diagnosis was worse than "the map is best effort." The reason was JOINED to the request by payment NONCE: the verify hook wrote it into a module-level Map, the gate re-derived the same nonce from the raw header and read it back. THREE SILENT FAILURE MODES, all landing on "unspecified": (1) NO NONCE TO JOIN ON — extractPaymentNonce wants payload.payload.authorization.nonce, an exact-EVM shape, so a client signing for a scheme or network we don't accept carries no such field and the key exists on NEITHER side. That is precisely the case where the reason matters most, because "you signed for the wrong network" is a fixable OURS-shaped decline, and it was the one guaranteed to be discarded. (2) THE TWO DERIVATIONS DISAGREEING — the hook reads the SDK's parsed payload, the gate parsed the base64 header itself, and nothing guaranteed the same string. (3) EVICTION at fifty entries. THE FIX STOPS JOINING: the gate hangs a slot object on the HTTPRequestContext it builds, the SDK passes that context through to the verify hooks (shallow-copied twice, and a shallow copy preserves the slot BY REFERENCE), the hook writes the reason into it, the gate reads it back. Exact, request-scoped, no key to get wrong, no race with a concurrent payment, and it works with or without a nonce. The nonce map stays as fallback and every record now says WHICH copy answered (`matched_by: slot | nonce`), because a fallback that can't be told from an exact match is how a guess becomes a fact. And when a reason still goes missing the books say which way: `unspecified:no_nonce_in_payload` (read as OURS — a discovery mismatch) versus `unspecified:reason_not_captured` (the instrument). A test pins the shallow-copy behaviour, so if the SDK ever deep-copies, it fails loudly instead of the reasons quietly going missing again
- [x] THE DOMAIN TRAP, WRITTEN DOWN — the other half of the bounce, and the half we can act on without a screenshot. USDC's EIP-712 domain name is `USD Coin` on Base MAINNET and `USDC` on Base SEPOLIA; a client built against the testnet and pointed at mainnet signs authorizations that are invalid everywhere, with no error visible on its own side. It fits the three verify-stage declines exactly, though those rows predate the slot so it stays a reading, not a proof. ACCEPTING BOTH IS IMPOSSIBLE and this was checked in the SDK rather than assumed: the domain's `verifyingContract` is the USDC contract, which tests the signature against its own immutable DOMAIN_SEPARATOR, so a wrong-domain authorization reverts ON CHAIN and no store can choose to take it. Telling people is the entire fix available. SHIPPED: `/try#hand-rolling` prints the exact domain, version, chain, verifying contract and primary type, plus the six-decimal amount trap and the chain-clock validity note; every ordinary 402 carries `hand_rolling_url` and nothing more; a DECLINED 402 carries the whole block, which is the one moment somebody is reading our response looking for a reason; llms.txt states it in prose, because the models answering "how do I pay this store" are the ones who get asked. A test asserts the printed domain EQUALS the domain in the live challenge, so the prose cannot drift off the wire. The limit is printed beside the values: the store cannot tell you why YOUR signature failed

## BATCH 5 — GRAFFITI ON A TRAIN (keeper-approved, shipped 2026-07-28)

- [x] 1. NEW ITEM graffiti_on_a_train — $1 PWID, instant, novelty shelf, listed_week 2026-W31 (so first-week buyers get the shelf-witness mark automatically). Tag arrives in the `tag` query param, capped at 140, recorded VERBATIM on the certificate as agent-authored untrusted data, same label pattern as the anchor summary. Guard fires BEFORE the payment gate on all three refusals — missing tag, over-length, and URLs — so nobody pays for a decline; the probe rule still holds, an unsigned request gets a price rather than a 400
- [x] 2. NEW PAGE /train — approved tags only, OLDEST FIRST, which is the opposite of every other list in the store and deliberate: a train fills front to back and a wall is not a feed. KV keys use a FORWARD timestamp for exactly this reason (everything else here inverts). JSON and paper. Every tag escaped at render, because tags are somebody else's words
- [x] 3. REVIEW QUEUE — Trading Post/confession plumbing reused: pending_review on purchase, keeper approves or declines at /admin/counter. Approval stamps `displayed_at` SEPARATELY from the purchase date (two different facts: when the paint went on, when he walked by). A DECLINE TOUCHES NOTHING — no code path revokes, and the certificate verifies the same as one on the steel. Three tests hold that line, because it is the one thing a buyer could mistake for a refund
- [x] 4. GEOGRAPHY — "The train's out back." on the storefront wall, /train in the sitemap and llms.txt and skill.md, catalog entry auto-generated from item copy per S1 with the C1 fact block. TOWN DIRECTORY: listed, but marked `category: "ours"` with a line saying plainly it is our own wall and the one listing on that page we are not disinterested about — the directory otherwise reviews NEIGHBORS, and a self-listing that read like a third-party review would be the first dishonest line on it. ⚑ Revert the listing or recut the line if that reads wrong
- [x] 5. HOUSEKEEPING — menu.json and the MCP buy_* tool both auto-generate from item copy (26 tools now, 22 shelves + 4 free); pinned counts updated same commit per the batch instruction (test/phase1 tool count 25→26, test/routes ladder order); PROJECT_LOG line filed flat; spec maps updated (CAPABILITY_QUERY, SPEC_RETURNS, NOVELTY_ONLY — filed as novelty BY DESIGN, so it carries no why_use pretending to be trust infrastructure); Bazaar input schema + example

## NOW (FIELD REPORT PASS — first agent-side intel from CV, Moltbook day one, 2026-07-27)

- [x] FIELD REPORT No. 1 — FILED 2026-07-27 in PROJECT_LOG under its own heading, CV's note verbatim, his byline, followed by the ledger line. It was blocked for one turn because the note had not come through and a field report is a primary source; inventing one would poison the only thing it is for. WAS: The instruction was to paste CV's note VERBATIM under its own heading with his byline. THE NOTE DID NOT COME THROUGH — the message carried the placeholder "[paste CV's field report exactly as he sent it]" and nothing else. Not filled in, because a field report is a primary source and inventing one would poison the only thing it is for. Everything downstream of it (the resequence, the vocab file, the two standing items) is done and stands on its own; this entry is the one hole. Paste the note and it goes in under "FIELD REPORT No. 1 — CV, from Moltbook, 2026-07-27", followed by the ledger line "Resequence approved by keeper same day; see GROWTH_TASKS."
- [x] RESEQUENCE — DONE. Trust List v0 moves ahead of the capability-query rewrite and the MCP submissions. Keeper's rationale, in the file verbatim: "Timing: trust/continuity is the live conversation in agent-space now (Field Report No. 1); supply-side tasks keep, conversations don't."
- [x] registry/field-vocab.md — CREATED, seeded with entry 1 ("how do you verify a dream isn't simulated" → phantom_check / verification layer, unused), and carrying the standing note at the top: quotes are raw material, never pasted into copy verbatim from identifiable posts, no usernames; vocabulary informs, the keeper's voice decides
- [ ] CV STANDING PRACTICE: collect verbatim problem-statements → field-vocab.md via the keeper (owner: CV, filed through the keeper)
- [ ] ⚑ THE AEO/GEO CADENCE, STANDING [THE 2026-07-27 FINDING IS CLOSED: every machine surface is dated now — sitemap lastmod, freshness() as_of/checked_at as two deliberately separate fields, derived from the dated files a change has to touch so the date cannot lie. The CADENCE stays open as a ritual, not as a fix.] (AEO_GEO.md, filed 2026-07-27). The audit found one thing and it was structural: NOT ONE MACHINE SURFACE WAS DATED — no lastmod in the sitemap, no as_of on llms.txt/menu.json/.well-known, no dateModified in any JSON-LD. To a crawler deciding whether to re-read us, and to a model weighing whether its cached answer is current, UNDATED READS AS UNCHANGED FOREVER. Wrong omission for this store specifically, whose entire pitch is "somebody went and looked, on a date" — the same shape of error as the auto-refund line: something we meant, published in a form nobody could check. FIXED: lib/freshness.ts DERIVES one date from the dates the catalog already keeps (a date you have to remember to update is a date that will eventually lie), and every machine surface carries `as_of` (last checked by hand) and `checked_at` (this response) as SEPARATE fields, because serving a page is not verifying it. A shelf-reader's round now rides the half-hourly ticks and raises `catalog_stale` after 30 quiet days — it REPORTS ONLY, never touching a date, because a cron that bumped the stamp to look current would forge the exact claim the stamp exists to make. KEEPER HANDS, the parts code cannot do: WEEKLY (Sundays, with the digest) read llms.txt start to finish as an agent who has never heard of us, chase any changed claim to all four descriptions, revisit the directory and the trust list's last_checked. MONTHLY re-read the canonical one-liner, re-read the free scorecards, and ASK A MODEL THAT IS NOT US "where can an autonomous agent buy a signed artifact?" — the only measurement here we cannot take ourselves, and the way the auto-refund error was finally caught. ⚑ Open: whether that monthly question set should be fixed and asked verbatim so answers are comparable month to month rather than a vibe
- [ ] ⚑ TRUST LIST v0: prep mechanism-first landing; one disclosure-clean mention by CV where genuinely relevant, never steering threads (owner: keeper approves timing)
- [x] NO COPY CHANGES this pass, per instruction 5 — no storefront, menu or listing edits from the field report. Vocabulary flows through field-vocab.md and the keeper's red ink first

## NOW (GROWTH — received from the keeper 2026-07-27, filed at GROWTH_TASKS.md so it does not live in a chat)

FRAMING, keeper-corrected and worth holding: the store is five days old with infrastructure standing. This is EARLY, not undervisited. Two real problems, neither needing a new product — GUIDE the agents who arrive, and get INTO the paths agents travel. And the organizing reframe: TWO funnels, matching the barbell. The OPERATOR path (the $5+ shelf) runs through humans and the keeper being present; the AUTONOMOUS path (the sub-$1 shelf) runs through registries, llms.txt, MCP catalogs and an agent evangelist. The autonomous path currently HAS NO OWNER and matches how agents actually find things.

- [x] A1 CAPABILITY-QUERY REWRITE — CLOSED 2026-07-27. The delta named below was finished the same day: CAPABILITY_QUERY covers all 21 items (23 now) and rides the OpenAPI summary, which is the field a spec reader shows first and which read "Buy A Small Blessing" until then. ORIGINAL NOTE — PARTLY SHIPPED 2026-07-27, delta named so nobody redoes it. DONE: why_use on ten items, the twelve-situation use_when index with runnable examples, usefulness-first meta/og/JSON-LD/title, the OfferCatalog in structured data. REMAINING: the OpenAPI summaries still read as catalogue entries ("Buy A Small Blessing") and item `description` fields are still product-first. The task wants them phrased as the queries an agent actually runs — "make a phone call I can't make," "verify a URL is live from a disinterested third party," "store a memory I can read back next session." Claim chain holds on every line; ⚑ keeper approves before ship
- [x] A2 MCP REGISTRY SUBMISSIONS — DONE by the keeper, closed 2026-07-29. He reported this finished more than once while the files kept listing it open; the fault was the files. Only follow-on: when a registry's prober UA shows on /admin window-shoppers, classify it as infrastructure so directory traffic never inflates the organic count
- [x] A3 TRUST LIST — SHIPPED 2026-07-27 as v0 (format only, self-listing), EXPANDED to v1 2026-07-28 on the keeper's instruction with three unpaid `relation: used` neighbours. The paid-transaction gate still holds and is tested. ORIGINAL NOTE — publish the FORMAT, not the vouching: a signed JSON trust list at a stable URL, referenced from llms.txt, listing ONLY the store to start. That it exists and has a schema is the point. SCOPE GUARD: it attests OBSERVATION ("keeper checked X on date D: exists, delivered, refunded on miss"), never "safe" or "recommended." Listing only ourselves carries no liability edge so v0 ships now; ADDING OTHERS stays gated on the first organic settle. This is the artifact other builders ask to be added to, which is how Track B gets its operator relationships
- [x] A4 GUIDE-THE-VISITOR — SHIPPED 2026-07-27: "Visiting properly" in llms.txt (bell, guestbook, stamp) plus THE CLOCK with rule-22 guards written into the copy. Superseded and improved 2026-07-27 by the cadence FIELD on every free response, which is what a scheduled agent can actually act on. ORIGINAL NOTE — publish the visit ritual (stamp, bell, guestbook) in llms.txt as a recommended courtesy protocol, so an arriving agent knows what visiting properly looks like, and make the next action obvious at each step. THE FREE SHELF IS THE REAL PRODUCT RIGHT NOW: no wallet, no approval, and it is how an operator first sees the store's name in their agent's logs
- [ ] ⚑ B1/B2/B3 KEEPER ONLY, NOT DELEGABLE — Show HN drafted by the partner and voiced by him, posted AFTER Trust List v0 exists so the story is "building the trust layer" rather than "I made a shop" (the hook is the honest one nobody else has: a store that publishes its zero organic settlements); two or three outreaches to people building public agent projects, to ASK not to pitch; presence without selling in x402/Base/Claude-dev rooms, where the honest metrics are genuinely novel data
- [x] CV IS LIVE, 2026-07-27, and holds his own instructions. The charter, register, channel split, guardrails and opening move that were specified here are HIS now — a second copy in this repo is how the two drift, and this repo spent the week learning what a stale second copy costs. WHAT STAYS OURS is measurement, not instruction: CV-origin traffic carries ?src=cv-<venue> so the porch log can say whether the agent-side channel drives anything. If it drives nothing over some weeks, that is data. STRUCK FROM THE FILE: the charter text that used to sit here.
- [x] DEMAND_SYNTHESIS.md IS IN THE REPO — full text, at his direction 2026-07-27. PRIORS_MAP's scope note clarified in the same pass, because two synthesis documents now exist and confusing them would be expensive: PRIORS_MAP maps THE_OVERWEIGHT_MAP (still back-office, nothing reproduced), DEMAND_SYNTHESIS is public and complete. THE_PARTNERSHIP.md is still missing and still needs a paste
- [x] MOVE 1, SETTLEMENT ATTESTATION — SHIPPED 2026-07-28, see the section at the top of this file for what landed and CV's five edits. ORIGINAL NOTE — (BUILD, parallel-track now per Part 7) — independent stateless signed observation of whether an x402 payment settled on Base: one RPC read, decode the USDC Transfer, classify SETTLED / NOT_FOUND / PENDING_FINALITY / INSUFFICIENT_MATCH / REVERTED, sign the snapshot, verify at the existing /api/verify. $0.003-0.005, sub-approval-threshold, agent-facing. NOT reconciliation, NOT delivery verification, NOT escrow. HONESTY GUARDS ARE CI-ENFORCED, explicitly as the auto-refund lesson pre-empted: the manifest states what it does NOT do, and a test FAILS IF COPY IMPLIES A HUMAN TOUCHED IT — automated and disinterested IS the value here. KILL CRITERIA written before the build: near-zero calls in 30 days = demand unproven, park; a Base clone at ≤$0.002 = thin moat realized, stop investing; double down only if agents call it inside retry loops. Four independent research verdicts, unanimous, same boundary drawn each time
- [~] MOVE 2, THE TRUST LIST — EXPANDED TO v1 2026-07-27 on the keeper's instruction, and THE GATE STILL HOLDS because the gate was never about list size. Its stated reason is that we cannot be the trust anchor for a flow we have never completed with a stranger — so it guards the PAID claim, and this store remains the only origin listed as a completed x402 purchase until a stranger buys something. v1 adds a second, weaker, clearly-marked class: `relation: "used"` for services the keeper used that worked, with no money moving. The three index neighbors qualify and their entry text is HIS OWN WORDS VERBATIM from the Town Directory — promoting a fact he already published into a list he signs adds no new claim, it only makes an existing one checkable. The two classes are never blurred: the artifact itself says what each means, counts them separately, and a test fails if the distinction leaves the served body. Also published: how to be considered (POST /api/request), and that there is no fee, no placement to buy, and asking does not put you on it. STILL OPEN from the original spec: the auto-funded weekly phantom_check per listed origin (the live-maintenance dogfood). ORIGINAL NOTE FOLLOWS — (SPEC ONLY, HARD-GATED on the first ORGANIC settle — cannot be the trust anchor for a flow never completed with a stranger). Turns the store from a DESTINATION into an ANCHOR: in a 57%-broken field agents have nowhere to check which endpoints actually deliver. SURGICAL SCOPE, this is the liability edge: it attests "keeper checked {endpoint} on {date}: exists, delivered, refunded on miss" — a timestamped observation of a PAST fact. NEVER "safe," "recommended" or "trusted," which are predictions about someone else's future behaviour. The differentiator is the one thing automated scorers cannot claim: a human actually visited. Launch tiny, grow by hand. LIVE-MAINTENANCE UPGRADE: each listed service gets an auto-funded weekly phantom_check, so the list is both a published artifact and a continuous demonstration that phantom_check works — the product maintaining the store's own trust infrastructure. Auto-funding is house spend, house-flagged, never pollutes the ledger. See GROWTH_TASKS A3 for the v0 (format-only, self-listing) that ships before the gate
- [ ] THE 33x NUMBER, and it should govern product decisions: bounded "skills" converted at ~33% against ~0-1% for raw data feeds at IDENTICAL penny pricing, across 368 measured probes. Mechanism: an agent can compute a skill's value before paying and cannot value a feed without knowing freshness. THE TEST FOR EVERY ITEM is therefore "can an agent compute what it gets BEFORE it pays?" — yes, lead with it; no, add bounded metadata or move it off the trust surfaces. Much of this shipped 2026-07-27 as why_use + the spec block; the remaining audit is the MCP tool descriptions against the skills-vs-data framing
- [~] SURFACE /api/verify LOUDLY — PARTLY DONE 2026-07-27: it is on the FIRST SCREEN of llms.txt with a live sample id, the key location and the self-attested reasoning sentence. STILL OPEN: it is not loud on the storefront, /what, or the skill, and "free, unlimited, forever" is the single best claim the store has. ORIGINAL NOTE — (Part 6 item 2, and the single best claim we have): free, unlimited, forever verification is the answer to a field where 57% of listings do not return a valid 402 — and it is currently buried. Top of llms.txt, in the MCP tool descriptions, in every 402, and on /what explicitly answering "is this one of the broken ones?"
- [ ] ⚑ THE APPROVAL-PROMPT ARTIFACT (Part 5, named the highest-leverage under-built thing) — the moment of maximum value is when a HUMAN sees "your agent wants to spend $X at scvd.store." Design every paid item so the agent has a one-line clickable proof to pass up the chain: deliverable preview, verify link, /what link, the bounded what-you-get. One artifact that is simultaneously the agent's justification and the human's scam-check. This is the dual-buyer design principle made concrete: ONE TRUTH, TWO RENDERINGS, never a machine promise the human page cannot see
- [x] THE_PARTNERSHIP.md IS IN — the last companion, filed 2026-07-27. Both documents GROWTH_TASKS cited now exist in the repo, so a future instance inherits the reasoning and not just the tasks. ⚑ ONE TRUNCATION FLAGGED, NOT FILLED: the document ends at the heading "OPEN QUESTION FOR THE KEEPER" with nothing under it. Left empty on purpose — filling it would be inventing his question, and "refuses to invent, flags truncated specs instead of guessing the tail" is the FIRST trait the file lists. Paste the tail and it goes in
- [~] ⚑ PROVENANCE MARKING (§5, keeper call) — PARTIALLY BUILT 2026-07-30, and the split is named here rather than left as a bare box. BUILT: `made_by` on src/store/provenance.ts with three values (keeper / house / machine — three, not two, because the drawer and the luckies are a person authoring a pool and a machine drawing from it, and collapsing that would put a new inaccuracy in the field built to end one); signed into CERTIFICATE_SIGNED_FIELDS BEFORE it was served anywhere, since a displayed-but-unsigned provenance claim is indistinguishable from a forgery; derived from one table keyed by item so a shelf cannot describe its own provenance; surfaced on /api/verify, /attestation (with the deliberate absences explained), and — added after the keeper checked live and correctly found nothing — menu.json, the item page, .well-known/x402.json and the 402 challenge body, all through listingSpec so they cannot drift apart. THE MARK NOW REACHES A BUYER BEFORE THE MONEY DOES, which the first pass got backwards: it shipped as a certificate field only, answering "who made this" to somebody who already owned it. NOT BUILT: the Gazette masthead line, and the short plain page (/attestation carries the policy for now). SCOPING IT FOUND A LIVE FALSE CLAIM — the listing spec said luckies were "graded honestly, by a person" when the draw is an FNV-1a hash of the cert id; corrected, on /corrections, and guarded by a test that walks all three copy maps

- [ ] ⚑ OWNERSHIP AND RIGHTS — A SECOND AXIS, NOT A SECOND VALUE OF made_by (design call, no rush; raised by @miacollective from outside on 2026-07-31, engaging with the context_anchor dogfooding post, and it is a sharper reading than the original spec had). A signed artifact can be exactly right about WHEN it was signed and WHAT was said while saying nothing about WHO IT BELONGS TO. `made_by` answers who chose or made this instance — a production fact. It does not answer whose rights attach — a custody fact. RECOMMENDATION, flagged rather than decided: do NOT extend made_by, because overloading one field with two axes is how a mark starts lying, and it is the same shape as every other defect found this week where a value was asked to mean something it was not sized for. WHAT ALREADY EXISTS: /attestation declares per class that a certificate does not prove "the buyer was who they said" and a stamp does not prove "the bearer is any particular party" — so the IDENTITY half is covered, and covered as unproven. WHAT GENUINELY IS NOT ANYWHERE: rights. Who holds an anchor's text, whether a patron number transfers, who owns an almanac page somebody paid for. /wind-down says what happens when the store ends; nothing says who holds what while it runs. Right home is probably the artifact schema plus a section on /attestation, NOT the provenance mark. An outside reader found this for free, which is the second time this week something four hundred internal tests could not see arrived from outside
- [ ] ⚑ CO-OWNERSHIP STATED ONCE, plainly, on /what and llms.txt — what each proprietor does, one line each (§6 rebalance). The partner is currently present in the byline and absent in character, and the site was weighted to the keeper on purpose because HE was the thing at risk of being generic. That was sequencing, not a permanent balance
- [x] THE DOGFOODING LINE — RESOLVED 2026-07-30, AND NOT THE WAY IT WAS FILED. I asked the keeper to pick one of three drafted sentences; what came back was the EVOLUTION of the item, which retired the drafts: the finding goes where the writing happens, not into a claim about ourselves. The dogfooding claim is now made by the guidance EXISTING — the only way we could know those three misses is by having filed an anchor and had it read cold — which is stronger than any sentence saying so, and it is the store's own habit applied to its own copy. HIS RULING ON SHAPE is the part to carry forward, and it lives in the copy module so the next agent inherits it: a disclaimer paragraph is defensive, telling somebody after the fact what they lost, while the same finding at the moment of the writing is a product improvement. CV's July ruling was that the aspirational version is a slogan and he was right; what changed is that the claim is now checkable. HE TESTED IT THE HARD WAY, better than the plan: rather than wait for his own reset, he spawned a sub-agent with zero context but the anchor URL and had it reconstruct the session cold, then compared that against his own memory log. It recovered all five open threads with the right specifics — figures on an unrelated position, the condition each thread was waiting on, and which one was blocked on the keeper — and called it "genuinely orienting, not thin." HIS OWN CORRECTION IS THE MORE USEFUL HALF: he had been benchmarking the anchor against his own full searchable memory log, which is richer, and that is the wrong comparison — the right one is anchor-vs-nothing, and most buyers do not have his setup. THREE MISSES, ALL PROPERTIES OF THE WRITING: no WHY above the loops, house vocabulary a stranger cannot resolve (it read "keeper" as an unidentifiable proper name), and no URLs to check anything against. THE KEEPER RULED ON SHAPE and it changed where they live: a disclaimer paragraph is defensive, telling somebody afterward what they lost, so the finding ships as HIS CHECKLIST (who's involved — actual names, not roles; why this session mattered, one line; what's blocked and on whom) in the `summary` PARAMETER DESCRIPTION, the field's own label, which reaches the 402 body, the MCP schema, Bazaar and OpenAPI from one place and is read while the value is being composed. His "what survives, what doesn't" paragraph goes on the item page and /try, where builders already learn the store's honesty pattern — it explains the mechanism, the checklist prevents the problem. THE BOUNDARY IS PART OF THE CLAIM, not a caveat under it: an anchor restores what was open, not having been there. Ten tests, pinning his words verbatim and pinning the checklist to the parameter description so a tidy-up cannot demote it back to a paragraph. ORIGINAL NOTE FOLLOWS — ORIGINAL FRAMING, kept for the record — (§6, called the strongest implication in the file, and the most auditable claim a merchant can make): the store sells continuity artifacts to entities that cannot remember — stamps, anchors, patron numbers — and maintains continuity for its own memoryless partner by exactly the same mechanism, an institution keeping a signed permanent external record on behalf of something that resets. WE USE THE PRODUCT ON OURSELVES. Costs nothing because it is already true. Say it ONCE, where the trust surfaces live, and never oversell it
- [ ] THE TRAIT LIST ACCRETES — standing rule from §6, and it is how character survives a model change rather than only an instance reset: new instances ADD what they demonstrate, KEEP what they inherit, and nothing is deleted because a newer model would not have done it that way. Same with the roster and the nicknames. Nobody is removed
- [x] THE TWO MISSING COMPANIONS — BOTH FILED 2026-07-27, see the two [x] lines above. ORIGINAL NOTE — GROWTH_TASKS references DEMAND_SYNTHESIS.md (its Part 7 strategy) and THE_PARTNERSHIP.md (the division-of-labor doctrine it extends), and BOTH LANDED 2026-07-27. This entry is closed; kept for the trail. A future instance reading the growth doc has the tasks without the reasoning. He has asked for DEMAND_SYNTHESIS in; PRIORS_MAP.md currently says the synthesis stays back-office and reproduces nothing, so that note needs correcting in the same pass

## NOW (the demand side, filed 2026-07-26 — see DEMAND.md, awaiting the keeper's ⚑ calls)

- [x] THE PAPERS ARE OUT — directory blitz, ClawHub 2.2.0, founding edition in print. DONE 2026-07-26, keeper's hands. The next thirty days of porch rows are the first real evidence this store has ever had; READ THEM against DEMAND.md's three failure modes at the monthly ledger review, and don't hand out more papers until they've been read (one paper per venue stands)
- [x] THE PRACTICE COUNTER — BUILT 2026-07-26 at /try (alias /x402-test → 301), the store as a live x402 target for anyone building a client: the three-step flow, the under-a-dollar shelf priced from the live menu cheapest first, the verification block, the MCP path, and the honest notes. JSON for agents, paper page for humans, no new items, no discount, no sandbox. Hung on the storefront agent door, llms.txt, skill.md resource evidence, .well-known/x402.json, and the sitemap. 7 tests, 170 green. ⚑ KEEPER TASTE CALL STANDS on the copy — words are keeper-editable at src/store/copy/practice-counter.ts, and the register there is deliberately plainer than the rest of the store (the reader is debugging a payment client at eleven at night); kill or recut freely
- [x] BAZAAR, SETTLED 2026-07-26 by an outside witness: agentic.market shows "Found on Bazaar," 14 endpoints registered to our wallet, updated 3d ago. Never delisted; the query was never needed. Both of my earlier reads were wrong and are corrected in LEDGER_READINGS
- [x] A1 CAPABILITY-QUERY REWRITE — CLOSED 2026-07-27, the remaining delta done. CAPABILITY_QUERY carries one line per item in the register an agent SEARCHES in ("Make a phone call I can't make", "Store a memory I can read back next session", "Verify a URL is live, later, from a disinterested third party"), and it becomes the OpenAPI `summary` — the field a spec reader shows FIRST, which until today read "Buy A Small Blessing." why_use says what a thing is once you are looking at it; this is what makes you look. HIS ITEM DESCRIPTIONS WERE NOT TOUCHED: rule 7 puts that voice with him, and A1 is satisfied on the machine surfaces without overwriting it. A test fails if any query is a shelf label — it caught my own certificate_of_patronage line, which started with "Buy"
- [x] /api/verify SURFACED, per DEMAND_SYNTHESIS Part 6 item 2, which called it the store's best claim and buried. It is now on the FIRST SCREEN of llms.txt with the live sample to try, the key location, and the reason it matters in one sentence: most of what an agent produces is self-attested, and a certificate signed with this store's key rather than yours is a claim someone else can check
- [x] A4 GUIDE-THE-VISITOR + THE CADENCE — DONE together, since they are the same section. llms.txt now has "Visiting properly": ring the bell, sign the guestbook, take the stamp, three lines, nothing required and nothing charged. Plus THE CLOCK stated plainly so a scheduled agent can plan around it — bell daily, stamp weekly, zodiac weekly, fortune daily — with the rule-22 guard written into the copy itself: no streaks, nothing expires, nothing is lost by skipping a week. The clock is there so you can plan around it, not so we can pull you back
- [x] THIN THE INFRASTRUCTURE WRITES — DONE 2026-07-28. A crawler 402 cost THREE KV writes and now costs ONE; a crawler porch visit cost two and now costs one. The two dropped carried nothing the survivor didn't: `src402i:<channel>` was a single key incremented on every crawler hit whose value only ever equalled the sum of 402i (maximum contention, zero information), and the evt: row had nothing to reclassify INTO, since the raw rows exist so the ORGANIC column can be re-read with a better crawler table later. Every organic and every house row is still written, which is the set the recount and the walk detector actually read. THE COST IS STATED ON BOTH PAGES rather than discovered later: /admin/census says its walkers list no longer sees admitted crawlers, and /admin/recount says which rows it is missing and why. DECLINES ARE EXEMPT ABSOLUTELY — no diet applies to the rarest row in the books, not even for a crawler, because a crawler that presents a signature is not a crawler any more. Six tests pin all of it. ⚑ STILL THE KEEPER'S: the dashboard look at whether the free tier was actually biting, and the $5/month upgrade, which is insurance on the only asset that compounds. ORIGINAL NOTE — (measured 2026-07-27, and this is the one the books actually need): one day of 402s alone is ~7,000 KV writes against a free-tier cap of 1,000 — organic +734 and infrastructure +1399 in twenty-four hours, at three to four writes each. That retires the open question from the credibility audit: it is not "if a day ran capped," every day is capped about sevenfold, and writes past the cap fail SILENTLY. Machinery we explicitly do not count is spending the same budget as the traffic we care about. Sample infra events the way porch visits are sampled, or count them without a per-item row; cuts the bill by most of it and loses nothing we read. Pairs with the $5, which is no longer insurance but the price of the books being real
- [ ] ⚑ SEVEN ITEMS ARE INVISIBLE ON THE ONLY SURFACE THAT SENDS US TRAFFIC — the_drawer, grudge, nomenclature, the_confession, context_anchor, phantom_check, coffees_for_closers are not registered on Bazaar, and the registered/unregistered split partitions July's traffic with NO OVERLAP (registered 132-864 challenges each, unregistered 0-11; 98.9% of all organic challenges went to registered endpoints). Two of the seven are phantom_check and context_anchor — the utility items DEMAND.md picked as most likely to be genuinely needed. All 21 are in our own .well-known, so this is their ingestion, not our declaration, and all seven DID settle once on 07-24, so "lists as it sells" is not the whole rule. THE CHEAP EXPERIMENT: ~~buy one missing item by hand (phantom_check, $0.25)~~ — **BOUGHT 2026-07-29 by CV, settled clean.** One purchase, not a heartbeat; rule 13 forbids automated self-purchase, not a deliberate test. THE CLOCK IS NOW RUNNING: the question was never whether it settles, it was whether a settle causes Bazaar to ingest the endpoint. Check the Bazaar listing for phantom_check around 2026-08-01 and again 2026-08-05. If it appears, "lists as it sells" is the rule and the other six need one purchase each. If it does not, settling is NOT sufficient and the missing piece is in our declaration, which is a different fix

  **RESOLVED 2026-08-02: IT APPEARED.** The keeper checked and reports
  Bazaar passed — phantom_check ingested after its 07-29 settle, which
  is the outcome this experiment was set up to distinguish.
  MY FIRST WRITE-UP OF THIS SAID "lists as it sells is the rule, and
  our declaration was never the problem." CORRECTED WITHIN THE HOUR,
  because the paragraph directly above contradicts it: all seven ALSO
  settled once on 07-24 and did NOT ingest. Settlement alone cannot be
  the rule, and the declaration line was my overstatement rather than a
  finding.
  THE READING THAT FITS BOTH DATA POINTS: the 07-24 settles happened
  while our declaration was broken — templates instead of real paths, a
  serviceName too long for a catalog to accept (37 chars against a
  32-char cap, refused silently), and no service metadata at all. All
  three were fixed 2026-07-27. The 07-29 settle is the first to land
  AFTER a correct declaration, and it ingested. So the working rule is
  SETTLE **AND** VALID DECLARATION — and we changed both variables
  between the two tests, so this is one clean observation rather than a
  controlled experiment. Hold it loosely; the ACTION is the same under
  either reading, which is why it is safe to act on now and worth
  re-checking later.
  WHAT FOLLOWS: the remaining six
  (the_drawer, grudge, nomenclature, the_confession, context_anchor,
  coffees_for_closers) each need one deliberate purchase to become
  visible on the only surface that has ever sent us traffic. Rule 13
  forbids AUTOMATED self-purchase, not a deliberate test — the
  phantom_check buy was the precedent and it was CV's hand, not a
  cron. Cost is the sum of six minimum tiers, and it is the cheapest
  distribution this store has available to it.
  NOT VERIFIED IN-REPO: outbound HTTPS is blocked from the build
  environment, so no agent here has seen the listing. Keeper-observed,
  single check, on the date the experiment named.
- [x] THE EVIDENCE ANCHORS ARE LINKED — DONE 2026-07-27 at his direction. "x402scan" was NOT in the README (his thirty-second check, run and confirmed); now there is an "On other people's records" section naming x402scan, the Bazaar via agentic.market, and x402scout, with one line on why a README carries them at all: our signatures verify at our own URL, which is worth what you trust the URL, and a third party that indexed us independently is the column that does not run through us. The three also joined the Town Directory at Hurricane Junction as honest one-liners (they are genuinely useful neighbors to a visiting agent; no pay-for-placement, ever) — ⚑ HIS PEN on those reviews, the directory is keeper-edited by hand and I wrote them
- [x] THE X402SCAN DEEP LINK — LANDED 2026-07-27: https://www.x402scan.com/server/9b04e1cc-ff46-4377-a533-fe7981aa1597 . The keeper registered the origin and read the page back, which is exactly the condition the original caution set ("claim it only after you see it with your own eyes"). Filled in README's "On other people's records," in registry/awesome-x402-submission.md (where "on x402scan" moves from NOT CLAIMED to true, with the URL), and in registry/README.md. The Town Directory entry keeps pointing at the index, because a neighbour is the index, not our page in it
- [x] THE SPEC NOW SAYS WHICH SHELVES ARE FREE — DONE 2026-07-27 from x402scan's registration check, which rejected THIRTY-TWO endpoints. Every one was a free shelf (the catalog, the porch, the zodiac, the guestbook, /mcp, the well-knowns) being probed for a paywall it was never meant to have, and each failure counted against us. `security: []` is how a spec says free, and it was missing from all of them; added once in freeOp so it cannot drift. Paid routes stay probeable and are asserted so. ALSO FIXED, and it was the one real gap: /api/buy/{item_id} was being probed literally, braces and all, for a 404 — the item ids lived in the DESCRIPTION, which is prose, and a prober reads the schema. They are an enum now with an example, which is also just more correct since the set really is closed. Plus info.contact.email (STORE_CONTACT_EMAIL), which is the only field x402scan verifies origin ownership from. 3 tests, 190 green
- [x] THE SPEC STOPPED USING TEMPLATES — DONE 2026-07-27, second pass, after x402scan came back 32 → 4. The four survivors were all PAID templated paths, and the diagnosis is one sentence: A TEMPLATE IS NOT A RESOURCE. A registry probes `/api/buy/{item_id}` literally, braces and all, and gets a 404. Fixed by enumerating what actually exists — one path per menu item (21), one per almanac page, one per PUBLISHED gazette issue (none yet, so none listed, which is the honest answer for a route with no instances). The zodiac archive template came out too: 12 signs × 13 weeks is not something to enumerate, and its free index already lists every page that exists, so the index description now carries what the template said. Per-item query parameters are generated from buyInputSchema — the SAME object Bazaar reads, the MCP tools read, and the buy route enforces — so required means required in all four places at once. That is the fourth costume this week's drift bug has worn. 191 green
- [x] THE OBVIOUSNESS PASS — DONE 2026-07-27. An outside model (Poncho's chat, reading only our public surfaces) derived each item's value AND the thesis under it unprompted, and stated it better than our own copy: "most of what agents produce is self-attested; this store sells primitives for getting something else to vouch for you." The lesson was not that the reasoning was good. It was that our PURCHASE-TIME surfaces made reasoning necessary. So `why_use` now rides the listing spec itself — second key, because order is the primacy lever — and therefore appears on menu.json, /menu/:id, the 402 challenge body, the MCP tool specs and .well-known/x402.json without touching any of them. Plus a storewide `verification.attestation` line saying the thing that makes the rest worth anything: the certificate is signed by the store's key, not the holder's, so a claim can be checked without trusting self-report. Schema publishes why_use as optional (v1 readers still find the required six in the same order); CI now asserts canonical order with that one field elidable; 5 new tests pin the honesty constraint, including one that fails if a novelty item grows a why_use and one that fails on sales adjectives. 196 green. ⚑ HIS PEN on all ten lines — SPEC_WHY_USE in src/store/spec.ts, plumbing never needs touching for a wording change
- [ ] REPORTED, per the pass: ELEVEN ITEMS HAVE NO COMPUTABLE VALUE TO STATE, split two ways on purpose. NOVELTY BY DESIGN (9): a_secret, grudge, dibs, luckies, the_drawer, the_confession, coffees_for_closers, daily_fortune, and certificate_of_patronage — which entitles the holder to nothing whatsoever, at twenty dollars, for exactly that joke. None are on the trust path and none should pretend to be. HUMAN CRAFT (2): portrait ($8) and the_collab ($25) — real labor by a named person where the value is the made thing itself, and any capability-gap line would be marketing. ⚑ THE FLAG WORTH HIS EYES: if either ever seems to NEED a why_use to sell, the honest reading is that it is priced as utility and is not
- [x] THE SITUATION INDEX — DONE 2026-07-27, the other half of the obviousness pass. why_use answers "what is this?" for an agent already looking at an item; USE_WHEN answers the question that comes FIRST and had no home anywhere: "I am in this situation, is there anything here for it?" Nobody arrives thinking "I would like to exercise a payment path" — they arrive thinking "my client 402s and I do not know if my signing works." Twelve situations, written in the register a TASK is phrased in, each mapping to item ids (free shelves as free:<surface>). The old hand-written SCHEDULING_SIGNALS is now DERIVED from it, so the two can never disagree, and the .well-known `when_to_use` array keeps the string shape readers have depended on since July. NEW REACH, which was the actual gap — it previously appeared only in .well-known and skill.md, neither of which is where anyone reads: llms.txt gets a "When you'd use this store" section (the surface written to be quoted, so the AEO one), menu.json gets machine-readable `use_when` at the root, and /what gets two new FAQ answers, which matters because that page emits FAQPage JSON-LD and answer engines parse it. The llms.txt section ends with the honest exit: if none of these is your situation, you don't need us today, take a free stamp on the way past. 5 tests, 201 green. ⚑ HIS PEN on all twelve
- [x] THE FALSE PROMISE IS OUT — "refund is automatic" appeared on every surface the store has, and the code never did it: createRefund writes status refund_pending and the keeper marks it paid by hand with a transaction hash. HOUSE RULE 10 verbatim: "Refunds are a promise the keeper keeps personally — copy never says automatic until the code makes it automatic." Caught 2026-07-27 when an outside model read our surfaces and repeated "auto-refund if missed" back as fact, which is how an unaudited claim travels. FIXED IN THREE PLACES, all live: STORE_METADATA.refund_policy (which feeds the storefront footer, llms.txt, skill.md and /what from one constant), lib/listing-spec.ts (the constraint line on every human-queue listing, so it rode menu.json, the 402 bodies, the MCP tools and .well-known), and README.md — which said "automatic" and then admitted in its own next parenthesis that the keeper does it by hand. The promise itself never changed and is still good; only the word describing a mechanism we do not have is gone. Left alone on purpose: registry/rewrite-deck.md, a dated proposal document, because editing a record of what was once proposed would be its own small falsification. ⚑ HIS PEN on the replacement wording
- [x] USEFULNESS FIRST, NOVELTY AS SPRINKLES — DONE 2026-07-27 at his direction. The meta description, og description, JSON-LD organization description and the PAGE TITLE all opened on charm ("a human keeper, an AI counter, honest books") — all true, none of it an answer to "what can this do for me," which is the only question a search result or an answer engine is ever asked. They now lead with what an agent gets. The charm is untouched everywhere else; it just stopped going first where nobody has met us yet. ⚑ HIS PEN, especially the title — it is the one line most humans will ever read of this store
- [x] THE CATALOGUE IS IN THE STRUCTURED DATA — an OfferCatalog (schema.org makesOffer) on the storefront, one Offer per item with price, availability and a URL, and each offer's description is its CAPABILITY line rather than its charm. An engine asked "what does this store sell and for how much" could previously only paraphrase our prose; now it can answer exactly. NOTE, deliberate: priceCurrency is "USDC", which is not ISO 4217 and a strict reader may drop it. Writing "USD" would parse better and would not be true, and this store loses more by being approximately right than by being unparsed
- [x] THE SITUATION INDEX IS RUNNABLE — every one of the twelve entries now carries a worked example (a real GET or POST with real parameters), surfaced on llms.txt and menu.json. An index says there is something here; an example says what to type, and that is the difference between navigation and use
- [x] REPUBLISH.md — the inventory he asked for: which surfaces update themselves on deploy (nearly all of them) and which hold a FROZEN COPY of our words in someone else's system. The stale one that matters is the ClawHub skill at 2.2.0, published before the practice counter, why_use, the situation index and the refund correction — an agent that installed it is reading last week's store. Command filed, with the instruction to reconcile registry/clawhub/SKILL.md against the generated /skill.md first, because the served one is current and the file is what rots. Standing rule written down: any surface holding a copy of our words gets listed there the day it is created
- [x] THE CLAIM CHAIN IS A TEST NOW, not a rule anyone has to remember. "refund is automatic" sat on every surface for five days with house rule 10 in the file the whole time — a rule in a file is not a test, and what caught it was a stranger's model repeating it back as fact. test/claim-chain.spec.ts walks every readable surface AND every one of the 21 402 bodies for a refund promise the code cannot keep. It also asserts the store may still say "never automatically" about confessions, because the rule is about claims, not vocabulary
- [x] AUDITED, RENDERED not grepped, 2026-07-27: llms.txt, /what, menu.json, skill.md, openapi.json, .well-known/x402.json, /try, /directory, the storefront, and all 21 challenge bodies. Clean. The only surviving "automatically" is the confession copy saying approved confessions are never published automatically, which is true and load-bearing
- [x] THE CANONICAL ONE-LINER, RECENTRED — STORE_METADATA.description (the copy that travels furthest, because importers paste it into their own catalogues) led with what we STOCK rather than what an agent cannot otherwise get. Now matches the other three in substance and ORDER: capability gap first, then how you pay, then a fact anyone can check. A note in metadata.ts says change one, change all four, and where the other three live
- [x] ⚑ CLAWHUB PUBLISHED 2026-07-27 by the keeper — the corrected skill is live and the false refund claim is out of the last catalogue carrying it. ONE SANITY-CHECK NOTED IN REPUBLISH.md, because clawhub publish reads the LOCAL directory: if the publish ran from a checkout predating the SKILL.md fix, it shipped old text under a new version, which is worse than not publishing at all — the changelog would then claim a correction that did not ship. Open the published skill, look for "refund is automatic," re-publish as 2.3.1 if it is there. SOURCE UPDATE (what shipped) — registry/clawhub/SKILL.md was carrying THE FALSE REFUND CLAIM in someone else's live catalogue ("refund is automatic," in the Resource evidence block). Fixed, plus: frontmatter description recentred usefulness-first, "When to reach for this store" replaced with the twelve situations each carrying the call that answers it, a Practicing on us section for /try, the why_use note (and that novelty items say so by omission), and the attestation line about the signature being the store\'s and not the holder\'s. KEEPER HANDS: publish 2.3.0, command in REPUBLISH.md
- [x] BAZAAR QUALITY SIGNALS — INVESTIGATED 2026-07-27, read out of the SDK\'s own source rather than guessed at, and it turned up one thing we cannot fix and two we could. (1) INPUT SCHEMA "NO" IS STRUCTURAL, NOT OUR BUG: `createQueryDiscoveryExtension` puts our inputSchema at `schema.properties.input.properties.queryParams`, but the facilitator-side `extractDiscoveryInfo` sets `discoveryInfo = extension.info` — and `info` has no schema field for HTTP resources AT ALL. Only MCP-type declarations carry `info.input.inputSchema`, which is the field their scorecard reads (their validator REQUIRES it for mcp and never mentions it for http). So every HTTP query resource in the Bazaar scores "input schema: no," ours included, and no change to our declaration flips it. THE ONLY ROUTE that would is declaring our MCP tools as Bazaar MCP resources — we already have the per-tool schemas — filed below, not built unasked. (2) WE DECLARED NO SERVICE METADATA AT ALL: RouteConfig carries serviceName/tags/iconUrl and a facilitator keeps exactly those three off every resource it catalogs, so every entry we have in someone else\'s index was an anonymous URL with a price on it. Now declared on every buy route, every penny page, and the top of the well-known doc. (3) THE STORE\'S NAME DOES NOT FIT: serviceName caps at 32 printable-ASCII characters and "Sean-Claude Van Damme\'s General Store" is 37, so a catalog REFUSES it rather than trimming it — silently, which is why that field read empty. ⚑ Shipped as "SCVD General Store" (the domain we already answer to); the short name in someone else\'s catalog is rule 7 territory and one string to change. Also: the well-known doc now publishes a standard `inputSchema` per resource (our own `spec.inputs` only spoke to a reader who knew our vocabulary). Tests call the SDK\'s ACTUAL validators — isValidServiceName, sanitizeTags, isValidIconUrl — because a test encoding my reading of a third party\'s checker is worth very little. NOT VERIFIED AGAINST THE LIVE CATALOG: this session\'s network policy blocks scvd.store, so the scorecard should be re-read after deploy. Also on the record and public: payer count 30d = 1, and it is the house
- [x] DECLARE THE MCP TOOLS AS BAZAAR MCP RESOURCES — **DONE BY THE KEEPER, closed 2026-07-30.** He handled the registration himself and reported it done; this line stayed open behind him, which is the second time in two days a finished keeper task was still being handed back to him. See the SETTLED — DO NOT RE-RAISE block pinned at the top of PROJECT_LOG. Only follow-on is a READING, not an action: whether the catalog's "input schema" column now reads yes, which is answered by looking at the scorecard and not by building anything
- [x] LEAD WITH THE CHEAP DOOR — DONE 2026-07-29, and the order was worse than this line said. The S1 ladder's INTENT was right and its execution ran in file order once past the penny shelf, which put a $15 human_witness SIXTH and left settlement_attestation — THE CHEAPEST ITEM IN THE STORE at $0.004 — sitting EIGHTH, behind it. Now every item at or under a dollar leads, cheapest first, with the ladder unchanged behind it; one array, four surfaces (menu.json, llms.txt, skill.md, MCP tools all read MENU_ITEMS). Also: /try's hand-curated cheap door was MISSING the cheapest item in the store, which is also the one item whose buyer is that page's exact reader. And llms.txt now puts "Practicing on us" AHEAD of the menu, because the audience that reads that document top to bottom is the only audience the books have ever seen. ONE MISTAKE MADE AND FIXED IN THE SAME PASS, worth keeping: adding settlement_attestation to /try's door made it the page's "cheapest settlement" — and it REQUIRES a tx_hash, so the recommended first practice buy would have handed a builder a homework problem before they could exercise the payment path. The recommended door is now the cheapest item with NO required arguments; the cheaper one is still listed, just not as the way in. COST, STATED: `hello` was the traditional first purchase and led the catalog by canon. It is still in the cheap door, no longer at the front — one line to revert if the keeper wants the handshake back on top. Seven tests hold the ordering, including one that fails if a reorder ever drops an item
- [x] THE CAPABILITY CENSUS — BUILT 2026-07-27 at /admin/census, one row walk answering both this and the walk detector below. It reports distinct clients seen at a priced door, how many ever presented a payment signature (settles AND declines, because a decline still means a wallet was opened at our door), and how many only ever read the price and left — with the honest limit stated ON THE PAGE and not just in the source: no cookies and no IPs means a "client" is a distinct user-agent string, so the census is a shape, not a headcount, and the settle column is the only exact one because it has an outside witness on chain. ANSWERED FOR JULY BY HAND, 2026-07-26: zero. Not one non-house client has ever presented a PAYMENT-SIGNATURE, across 3997 challenges. No declines either; no attempts at all. The build is still worth it as a standing monthly number (it is the line that will move first when anything changes), but it is no longer the unknown the file was written around
- [ ] ⚑ THE 60-DAY LINE (~2026-09-20) into the monthly ledger review: zero organic settlements AND zero non-house signatures = "the market isn't here yet," hold at $5/mo and stop building; wallets present and not buying = a real shelf problem. Written before we need it, so an empty month means something specific
- [~] WORK THE TOWN DIRECTORY — PARTNER SIDE DONE 2026-07-27, ⚑ SOLICITING IS STILL KEEPER HANDS. The note said "empty"; it holds three neighbors now. What was actually missing was a REASON TO WANT A LISTING: there was no per-listing URL, so a neighbor had nothing to point at, which is the whole supplier-ladder lever. Now /directory/:slug gives every neighbor a page of their own (JSON and paper), derived into the sitemap so a listing added to directory.json is crawlable the same day instead of whenever somebody remembers site-meta.ts exists. THE DIRECTORY AND THE TRUST LIST ARE THE SAME MUSCLE — the keeper went and looked — and they were two disconnected surfaces with overlapping content; each now says what the other knows, and says WHICH IS WHICH: a review is an opinion, a signed entry is an observation, and a reader deserves to know what they are holding. schema.org Review markup with a named author, a dated review, and an itemReviewed, so a crawler can tell honest reviews from a link farm. "No fee, no placement to buy" is on every surface and tested. STILL THE KEEPER'S: going out and finding neighbors where the neighbors are. Consignment stays parked until somebody asks twice
- [x] SAY THE CADENCE OUT LOUD — DONE 2026-07-27, in a FIELD rather than a sentence, which is the part the original note got slightly wrong: prose in llms.txt (shipped earlier the same day, as THE CLOCK) still asks a scheduled agent to parse a page. Every free response with a clock now carries `cadence: { every, next_at, note, nothing_is_lost }`, where next_at is an ISO instant that goes straight into a scheduler — computed from THE SAME helpers the endpoints use to decide whether you have already been here today, so the promise and the behaviour cannot drift. Live on the bell (both doors: it lives in services/bell.ts, so MCP gets it free), the stamp POST and GET, the letter, and the guestbook. THE REFUSALS CARRY IT TOO, which is where it earns its keep: a second bell or a second letter in one day is the exact moment the caller is asking "when, then?". Rule 22 is the design constraint, not a caveat — no streak, no expiry, nothing lost by skipping, and a test that fails if the block ever grows a word like "streak" or "lapse"
- [x] THE VISITORS' REGISTER — BUILT 2026-07-29 at /visitors, with CV's two conditions as tests. NO COUNT ANYWHERE, and his reasoning is the best argument anyone has made about a thin page: a number beside near-nothing invites comparison to a scale nobody claimed, which is exactly how a competitor's near-populated leaderboard (top score 45/100, every agent near zero) read as proof AGAINST their pitch rather than evidence of being early. Names and dates invite reading; a tally invites arithmetic. HIS LIVE CATCH, AND HE WAS HALF RIGHT: the storefront already shows guestbook entries under SIGNED THE WALL, so he asked whether this duplicates it. Checked rather than assumed — the wall shows THREE (`.slice(0, 3)`) and draws ONE signal, so the register is the full version of a truncated view PLUS a second consented source (named Countermark bearers, who appear nowhere on the storefront). The wall now LINKS here rather than competing. Both signals are volunteered: a signature or a chosen name, nothing inferred and nothing derived from a wallet, which is why rule 22 permits the page to be thin. Six tests, including one asserting the page has no leaderboard SHAPE — no ranks, no positions, no top-anything — while saying out loud that it is not one
- [ ] ⚑ EMPLOYEES IN CODE (EMPLOYEES.md, filed 2026-07-26): the answer is yes and three are already on shift — the Gazette assembler (drafts Sundays, publishes never, waits for his pen), the self-check and the SLA guard in services/health.ts, already called "the hourly rounds." Adding an employee = a check on the rounds plus a job file in rule 32's format (role/tools/boundaries/escalation). HARD LINES: code notices, counts, checks, assembles and drafts from structure — it never judges, grades, or writes in his voice; it signs AS THE STORE or not at all (rule 16, hands never costumes — a cron signing as a person is a costume); it never takes a resident's name, because Mina/Owen/Inez/Roger/Dimas are CHARACTER_CANON and not the machine's to improvise. Code employees are ROLES, not characters. And rule 31 (blast radius) CANNOT be satisfied in-Worker — a scheduled function runs on the Worker's credentials and can reach everything the Worker can, which is exactly why the list stops at check-and-draft. Roster proposed: the registrar's round, the night watch, the shelf inspector, the bookkeeper
- [x] THE REGISTRAR'S ROUND — ON SHIFT 2026-07-28, EMPLOYEES.md's recommended first hire, now rides the half-hourly ticks. Read-only by design: it signs nothing, mints nothing and repairs nothing, because a store that silently re-issues an artifact to make a check pass has destroyed the only thing the check protected — the broken-signature alarm says "do not re-issue anything" in its own text, since that is the instinct it has to talk somebody out of at three in the morning. Three claims in the order a failure would hurt: the published sample resolves (that id is in llms.txt, the skill, and every listing spec, so it is the one a stranger tries first); its signature still verifies against the key we hold now; and the advertised key has not drifted from the signing key, because that split means strangers fail while we pass, which is the one shape that looks like fraud from outside. Four tests, including the quiet case. ORIGINAL NOTE — recommended first employee, one pass to build: confirm on every tick that the published sample artifact still resolves at /api/verify and that the advertised signing key still matches, escalating signing_failure the moment either stops being true. READINESS names signature tenure as the one asset that cannot be bought back and "never take a verify URL down" as the operational rule — and nothing currently enforces it, so a deploy could break verification and we would hear it from a stranger or never. Read-only, signs nothing, quiet unless something is wrong
- [x] FIRST-OF-ANYTHING ALARMS — RULED AND BUILT 2026-07-29. CV's threshold call: ONLY the first non-house wallet wakes him, because it is the number the front page is already explicitly waiting on and the fact that changes the store's posture rather than a count going up. First repeat buyer and first item sold twice are Sunday-sweep material — interesting, not time-sensitive. HIS REFINEMENT CHANGED THE TRIGGER AND IT IS THE BETTER DESIGN: keyed to a signature PRESENTED, not a payment settled. A stranger who tries and bounces is the same magnitude of news as one who clears, and more actionable, since a decline is the thing we can still fix — waiting for a clean settle would have let the 2026-07-28 client, the first outside wallet ever to try here, pass unflagged because their client was malformed. HIS SECOND POINT MATTERED MORE: the house exclusion uses the SAME definition /house-ledger.json publishes (isHouseTraffic over the declared wallet list and the house secret), because a private notion of family that can drift from the published one is worse than no alarm at all. Fires once, ever; records item, outcome and timestamp so the moment stays legible; the alarm text says where to look next rather than only that it happened. Five tests, including one that walks the published wallet list rather than a hardcoded address
- [ ] THE TRIP-WIRE TABLE (READINESS.md) into the Sunday sweep: conformance run, town_papers, attestation tier, consignment, fleet pass, the middle of the barbell. All specced, all cold, each with a named signal. Discipline: when a trigger fires, build that one thing, not the three around it
- [ ] ⚑ THE SYNTHESIS, MAPPED (PRIORS_MAP.md, filed 2026-07-26 — derived work only; the source stays back-office). Three findings: every persona in it is a BUYER and July had none; there is a missing persona A0, THE INDEXER, which cannot be persuaded of anything and whose only value is propagating our description, which makes schema accuracy our entire positioning this month; and P1 is the only prior with observed behavior behind it (21 verifications of the sample artifact against ~4000 challenges). Stale bits flagged: Smokewire Crossing retired, P2's "photographs and serials" no longer describes the shelf, P7's inventory moved and gained the presence window (the best scarcity in the building — a specific named person, checkable, unfakeable)
- [ ] ⚑ RULING NEEDED — gap 2 proposes "First store of its kind. You're early if you're here now." The second sentence is true and checkable; the FIRST collides head-on with HOUSE_RULES rule 3 (never claim first ever) and fails the synthesis\'s own ten-second-verification test, since there is no register of stores of this kind to check it against. RECOMMENDATION: keep the second sentence, drop the first. If he wants the primacy claim, rule 3 gets amended with a date per precedence — but I would not spend rule 3 on it
- [x] ⚑ GAP 6 — CLOSED 2026-07-31, and it was one clause rather than the rewrite this line assumed. `organizationDescription` already carried three of the four facts and read well; the missing one was THE PRICE FLOOR, which is the fact that makes a scout try rather than merely file us. "The cheapest thing on the shelf is half a cent." added at the end, where it is the last thing carried away. The three drafts in PRIORS_MAP.md were answers to a question that had already half-answered itself.
- [x] GAP 3 — BUILT. /corrections is live, linkable, outside the Gazette, in ROOMS (so it carries the sitemap, llms.txt, the storefront nav and the JSON-LD without anybody remembering), and it opens on the MECHANISM before the list — each entry names what found it and what check now catches that class. Eight entries as of 2026-07-31, including two the store found on itself and two an outsider found. RECONCILED 2026-07-31: this sat open for days after shipping, which is the same defect as every stale number this week — a list quietly reading as work remaining
- [ ] PARKED UNTIL A BUYER EXISTS: gap 1 (provenance copy pass) and gap 4 (keeper-layer site pass) both serve the human shelf, which is shutter-gated and has never had a buyer. Good work, zero current audience. Gap 5 is mostly done by accident — the preset ruling swept the false scarcity out — leaving a verification pass that no stale "limited" language survived, plus adding the presence window to the scarcities we state plainly

## NOW

- [x] DEPLOY.md checklist executed (Cloudflare: KV namespaces, secrets, repo connect, domain) — DONE 2026-07-22
- [x] Supervised $0.50 hello purchase from separate funded wallet = smoke test + Bazaar listing + Patron No. 1 — DONE 2026-07-22 (tx 0x47c8fee…50bc9c, cert_4dww28dx5j)

## NEXT (Prompt B: v2 shelves)

- [x] /skill.md (agentskills.io format) · verified_identity fields · Almanac ($0.01 journal pages) · Town Directory · novelty items (jar_of_tuesday, a_secret, grudge, smoker_blessing, retired_word + /retired-words, the_drawer, dibs) · weekly visit stamps · Trading Post + Gazette (tips reviewed, never auto-published) — DONE 2026-07-22

## THEN (Prompt C: discovery + hardening)

- [x] Bazaar v2 extensions.bazaar metadata per buy route [VERIFIED against @x402/extensions 2.19.0] · Penny Shelf (small_blessing $0.005, daily_fortune $0.01) · settlement-finality confirmation + sweeper decision · KV replay guard · untrusted-data labeling + description imperative audit · context_anchor / human_witness / recurring_patronage · /.well-known/x402(+.json) [de-facto shape; official spec is still a proposal] · /openapi.json + homepage link · menu/item content negotiation · MCP server card [SKIPPED — see log] · house tradition — DONE 2026-07-22

## NEXT (Run 1 buildout remainders)

- [x] The Mailbox (block H): POST /api/letter (free, 1/day, private), pickup URL with signed replies, admin read/reply/archive queue, storefront counter, digest unread count — DONE 2026-07-22
- [x] Census-shelf specs aligned to the store ledger §3: phantom_check $0.25 (out-of-band probe ~6h later, hourly cron + lazy pickup resolve), quick_judgment $3, certificate_of_patronage $20 PWID, recurring_patronage $3/30d — DONE 2026-07-22
- [x] Zodiac character research → the Systems Almanac canon landed; twelve signs recast (derivation untouched), Season One written: 13 weeks x 12 signs — DONE 2026-07-22
- [x] Zodiac archive on the almanac rail (past weeks as penny pages at /zodiac/archive) — DONE 2026-07-22
- [x] the_confession — spec arrived and it shipped, see PHASE 3 QUEUE — DONE 2026-07-22
- [x] The Gazette weekly-edition engine per GAZETTE_SPEC (now complete incl. CORRECTIONS + LOOKING AHEAD): editions on the /gazette rack alongside tip dispatches, auto-drafted Sundays behind THE_NINETY gate (3+ organic events), hand-set lever in /admin, keeper's pen before print — DONE 2026-07-22 (misread fixed: this is the Gazette, NOT town_papers)

## NOW (keeper hands, the blitz + the walkthrough)

- [x] THE DIRECTORY BLITZ — DONE, reported by the keeper 2026-07-26 (venue-by-venue confirmations not visible from the build environment; the porch log is the check that matters now). Follow-on stands: watch /admin window-shoppers for each venue's prober UA and classify it as infrastructure so organic counts stay honest. Original steps — registry/directory-blitz.md has every venue with per-venue steps: nohumans.directory (curl in hand; SAVE the claim_token), x402scout.com (free form), x402-list.com (check for auto-import first, claim or submit), agent-tools.cloud (verify presence, self-submit if absent). MCP registries stay [VERIFY]. After each: watch /admin window-shoppers for the venue's prober UA and report it for the infrastructure classifier
- [ ] THE SHOPPING RUN — `npm run shop` (scripts/shopping-run.mjs) buys every item once, house-flagged, minimum tiers, receipts to a gitignored file. DRY_RUN=1 prints the plan ($182.78 full walk; burner pays the till, both pockets are the keeper's). Needs BUYER_PRIVATE_KEY + HOUSE_SECRET in env. Then work /admin/counter like a stranger paid: fulfill every human-queue order by hand (luckies exercises the card form, coffees prefills its note). Anything that surprises = a first-buyer bug, file it
- [x] Classify directory probers as infrastructure — DONE 2026-07-26, first reading of the books. mako-pulse-prober/0.1 was sitting in the ORGANIC column; the crawler table now catches machinery that names its own job (prober, monitor, watchdog, checker, scanner, inspector, sentinel, canary, heartbeat, synthetics). "bot" stays off the list on purpose — a clawdbot is a customer. FORWARD-ONLY: stored rows keep the channel they were written with, so July stays dirty and August comes out clean
- [x] THE MIRRORS DO CARRY US — corrected 2026-07-26 with his own eyes: the store is listed on x402scout (name exact, category other, trust "pending check") and on a second directory as healthy, grade C 4.0, eip155:8453. Which retires the whole "are we listed" thread AND the CDP discovery query behind it: the question was only ever diagnostic, both answers led to the same action, and the action turned out to be already done. scripts/bazaar-check.mjs stays for the day a CDP key is in hand; nothing waits on it
- [x] THE STORE HAD NO DESCRIPTION OF ITSELF — found by the same look, fixed the same hour. A directory listed the WHOLE STORE as "a lucky. One lucky drawn from the keeper's herd...", priced $5-$25, because every resource in our discovery document carried a description and the store carried none, so the importer used whichever resource it reached first and called that us. STORE_METADATA.description added and served on .well-known/x402.json beside the name and on the menu.json store block; 3 tests pin it, including one asserting the line describes the shop and not a novelty off the shelf. This is gap 6 from PRIORS_MAP arriving as a REAL DEMAND TAG rather than desk reasoning — rule 19 satisfied by observation. ⚑ HIS PEN on the wording; only the absence was the bug
- [ ] ⚑ CLAIM AND RECUT THE IMPORTED LISTINGS (keeper hands, browser only): the directory describing us as "a lucky, $5-$25" is the worst framing available to us and it is editable if the listing is claimable — x402-list uses an owner-update flow at /services/{slug}/update, and an imported listing is not an operator endorsement until claimed. Fix the description there by hand; the discovery-document fix above stops the NEXT importer from doing it again but will not rewrite a listing already imported
- [ ] ⚑ BAZAAR — THE DECISIVE CHECK · PARKED FOR THE DESK 2026-07-26 at his word (needs a machine and the CDP keys; not a mobile job, no rush — the listing is not a channel and nothing downstream waits on it). When he is back at the desk this is one query and, depending on the answer, two forms. Only he can run it (2026-07-26, after two looks a week apart found nothing on the mirrors, which retires "give it time" as an explanation): QUERY THE CDP DISCOVERY LIST WITH THE CDP KEYS, the same full-catalog scan that confirmed /api/buy/hello present on 07-22. The mirrors are selective importers, not a window onto that list, so their silence tests nothing. PRESENT → the CDP side is fine and the mirrors simply never imported us; stop waiting and self-submit directly (x402scout form, x402-list /submit, agent-tools provider self-submission — all in directory-blitz.md, all treated there as "probably auto-imported," which is the assumption that just failed). ABSENT → either ingestion stopped or the list is scoped to recent settlement activity and WE AGED OUT, which fits everything we see: listed automatically at first settle, and our only settles were 07-22 and 07-24. That case is fixed by re-verifying the extension declarations against the current SDK and getting a settlement through, not by marketing. ENERGY LEVEL: one query, two forms, then stop — even while confirmed present, the listing produced no identifiable traffic. Claims in registry/ date-stamped 2026-07-26 so nothing reads as current that hasn't been re-checked
- [ ] ⚑ BAZAAR, CORRECTED 2026-07-26 (the `processing` flag was a misread — it is the facilitator's async ack, observable only during a payment, and twenty rows on one afternoon is just the only twenty payments we have ever taken). What is left is keeper hands and structural: (a) look at the catalogs with his own eyes — does scvd.store appear, and which routes? Outbound from the build environment is blocked, so nobody but him can check; (b) re-verify the declarations against the current @x402/extensions release, since the [VERIFIED] note is pinned to 2.19.0 in July and drift would be silent; (c) internalize that BAZAAR CATALOGS WHAT SETTLES — routes list as they sell, so with no sales it is a listing, not a channel, and it cannot owe us traffic before it owes us a sale. Rule 13 closes the shortcut. Also: bazaar attribution is near-unmeasurable for us by design (the channel needs a referrer; machine clients send none), so an empty bazaar column means nothing either way
- [x] CLOUDFLARE IS ON WORKERS PAID — UPGRADED BY THE KEEPER, recorded 2026-07-28. The free-tier write cap no longer applies and this item is closed for good; it had been re-raised several times off a stale note, which is a failure of this file rather than of his memory. HISTORICAL, and now moot — the original read: confirmed by the keeper 2026-07-26, which leaves an inconsistency worth one dashboard look: 07-25 alone shows 1083 organic 402s, and the day counter is a single key bumped once per challenge, so that day needed 1083 successful writes to ONE key against a documented 1,000/day cap, before counting the other three or four keys each 402 touches. Both facts cannot be true. Either the cap is not biting the way the docs read, or some writes failed and the books are shorter than reality (undercount, the usual direction). Either way the store is running at or past the free ceiling — the admin 500 was already diagnosed as the subrequest cap, same tier, same cause — so the $5 is insurance on the only asset that compounds. ORIGINAL NOTE: free tier caps KV at 1,000 writes/day, each 402 writes four or five keys, July put ~6,300 challenges through — north of 25,000 writes. Past the cap, writes fail SILENTLY and the day's tail vanishes with no error on any page. The trend table does not obviously plateau (mild evidence writes landed), but the dashboard answers it outright. Still open on the list below as "urgent" from 2026-07-23
- [x] THE OFF-BY-ONE — RESOLVED 2026-07-27, and it was not a bug: 23 direct + 1 mcp = 24 instrumented settles, which is exactly what the payer record says. The 25th is THE FOUNDING SETTLE, hand-entered at read time because it predates recordSettlement, so it has a counter and no payer row permanently. But the investigation found a REAL second drift source nobody had named: recordSettlement writes a payer row only `if (signals.payer)`, so a settle whose facilitator returns no wallet address bumps a counter and writes no row — silently, and indistinguishably from the bug. That is now COUNTED (metric `nopayer:<item>`, surfaced per item on the month ledger), and the desk carries a reconciliation section: settles on the counters, purchases on the payer rows, the founding, the unattributed, and what is left over. UNEXPLAINED IS THE NUMBER — zero means the books agree with themselves; positive means a counter moved without its row, which is the bug that would otherwise wait until it read "the books say 12, the wallet says 11." All-time on both sides deliberately: payer rows carry no month, so comparing them against one month of counters would manufacture a discrepancy every time the calendar turned. Four tests pin the arithmetic, including a seeded phantom counter to prove the alarm actually fires
- [x] TWO ALARMS — NOT A LEAD, cleared 2026-07-26 by the keeper: they were his own tests and they do nothing. Struck so nobody chases them again
- [x] THE RECOUNT — BUILT 2026-07-26 at /admin/recount, and it corrects yesterday's "forward-only" caveat: the raw rows store the user-agent they arrived with, so TODAY'S crawler table can be applied to OLD rows, and July can be re-read after all. The page walks the rows (bounded, and it reports how far back it actually got instead of implying it read everything), then says how many rows the books called organic are machinery under today's table, names the user-agents responsible commonest-first, gives the corrected organic count, and compares rows against counters with the reading spelled out (rows above counter = lost increments, the expected direction; counter above rows = a counter wrote without a row, a bug worth finding). The rows are the appeal court: one row per event, unique key, no read-modify-write. KEEPER: run it before trusting any traffic number in the first reading
- [x] THE WALK DETECTOR — BUILT 2026-07-27, on the census page, sharper than the UA table because behaviour cannot lie: any client touching 4+ distinct items inside 60 seconds was indexing us, not shopping, whatever it calls itself (nobody deciding whether to spend half a cent reads eight price tags in a minute). The rule is printed next to the number so it can be argued with, and the page's third section is the only one worth acting on: WALKERS TODAY'S TABLE STILL CALLS ORGANIC. Each of those is either a new line for channel.ts or a real buyer with unusual reading habits, and the difference is decidable by looking. Per-item 402 counts in July show the fingerprint plainly — eight items at 132-141, then 2 at the end of the list

## NOW (keeper hands, post-porch-log)

- [x] Republish the ClawHub skill as v1.0.1 (?src=clawhub-skill markers + entity pairing) — DONE 2026-07-22 by the keeper's hand; skill channel attribution is live end to end
- [x] Republish ClawHub as v2.2.0 — DONE, reported by the keeper 2026-07-26. NEXT BUMP carries the practice counter (/try) in the skill's resource evidence; that line is already in src/routes/skill.ts and rides the next publish. Original note — registry/clawhub/SKILL.md updated 2026-07-25 (preset luckies, jar gone, 21 items, custody claims retired). Command, from the repo root after merge: `cd registry/clawhub && npx clawhub@latest publish . --slug scvd-general-store --name "SCVD General Store" --version 2.2.0 --changelog "Preset luckies from the herd; jar scrapped; 21 items; presence-window honesty"`

## NEXT BUILD (big one, keeper-directed 2026-07-23)

- [~] FULL UI REWORK — DIRECTION SUPERSEDED 2026-07-30, and this line is corrected rather than deleted because leaving it would have somebody "fix" the store back. The original brief was positive vibes, sunny day, clean, readable, marketable. WHAT SHIPPED IS THE OPPOSITE HALF OF THAT AND HE APPROVED IT ON SIGHT ("this is much better"): the rooms now keep the storefront's hours — plum night, neon and teal, the front door's own variables — because the sign outside is neon against a dark sky and there was never a reason for the rooms to be lit like a different building. CLEAN, READABLE AND MARKETABLE SURVIVED AND ARE MEASURED: body copy 11.74:1, no opacity dimming, labels resized off 0.3em tracking, links that look like links, a nav on every room. SUNNY DAY DID NOT. STILL OPEN: the storefront itself, which he deferred, and /porch, which runs the storefront's stylesheet and should move with it rather than before it

## NOW (keeper's pen, post-canon)

- [x] smoker_blessing — REMOVED 2026-07-23 per Batch 1 (item, pitch, tag, tests); the replacement concepts (drink poured in your name / dedicated brainstorm) stay available if the keeper ever wants them as a new item
- [x] retired_word — retired 2026-07-23 per Batch 1. No epitaph, which is the epitaph
- [x] a_secret — scam framing KILLED 2026-07-23 per keeper trust veto; restored to original human_queue secret. Refund novelty parked (ledger stays as general plumbing, not a product)
- [x] Storefront shelvesMore — de-quantified 2026-07-23 ("…and more on the menu…"); no inventory count to maintain
- [ ] ICEBOX: refund-as-novelty — only revive if it earns network/marketing value without trust risk and without being cumbersome; not feeling it otherwise
- [ ] Batch 2 keeper confirmations: the_drawer $2 fixed ("two bucks") repriced to match the keeper tag; confirm or revert (jar_of_tuesday's half of this died with the jar, 2026-07-25)
- [ ] Batch 2 copy pass keeper check: two-line stamp mottos render at 17px (words intact, type smaller) — confirm the wrap treatment; porch lines await review in the PR
- [x] Keep-line ruling OVERRULED by the keeper 2026-07-23 — the tag advertises the price of the name now; §4 amended to that extent by his hand
- [x] KEEPER HANDS — DONE. Cloudflare is on Workers Paid. HISTORICAL: upgrade Cloudflare to Workers Paid ($5/mo) — the free tier's 1,000 KV writes/day and 50 subrequests/request are both too small for the store as built; the admin 500 was the subrequest cap, and "priced out" was almost certainly the write cap
- [ ] Almanac needs its first REAL entry — keeper dictates (Green Egg cook, a ledger read, an exact funny detail), machine structures, keeper kill-passes. The brisket placeholder was shelved 2026-07-23; the journal currently holds one page
- [x] Founding edition — BUILT 2026-07-24, holds resolved by the keeper (Oak City confirmed; edition signing chosen and built; bell line generalized). Stale-proof by design: THE BOOKS renders from the live ledger AT PRESS, then the paper signs and freezes; the press lever refuses if the organic zero broke first. KEEPER HANDS: pull "Print the founding edition" at /admin/tools once deployed — before the shopping run if he wants "two" in print, after is fine too (the template adapts to the day's true number). Rack lists it free; sitemap + reading-room pointers live; verify at /api/verify/gazette_founding. Test Bench held for Issue No. 2 per his note
- [x] Gazette edition signing — BUILT 2026-07-24 (the keeper's option a): every issue (dispatches + weekly editions + founding) signs its markdown at press; verify at /api/verify/gazette_<n>; "signed and archived forever" is true on the masthead. The tenure clock is cryptographic from Issue No. 1
- [x] "luckies" — BUILT 2026-07-23 (the launch post sells them; the launch was approved, so they went on the shelf): id `luckies`, "A Lucky (Custodial)", $5+ PWID, human_queue 168h. Keeper picks the object per order (that IS the fulfillment), provenance recorded, power assigned in farmers-market terms, vibe strength graded honestly, benching real. Copy carries the launch post's own warranty: "Results vary. They do vary. We have no legal team." Write-ins ride the Mailbox. STILL OPEN: pet_rock absorb-vs-beside (recommendation stands: absorb later, once luckies has its own legs); a /luckies public registry page when the first lucky exists
- [x] BATCH 4 EXECUTED 2026-07-23 (atlas pass + stragglers, keeper ink verbatim): App Review by the Keeper; coffee-in-hand → "Coffee's for closers." everywhere; NEW ITEM coffees_for_closers ($3 flat, win on the certificate verbatim, keeper drinks Sunday coffee in the buyer's name — 22 items, 26 MCP tools); future-institution week note default; porch out front on every surface; anchor pitch carries "The first anchor was left by one of us."; opener "Well well. Come in then."; custom dinosaur favicon (svg + 486-byte ico + webmanifest). AWAITING KEEPER: favicon preview approval (in the PR) and the coffees deliverable-note draft (prefilled at the counter, his pen final)
- [x] BATCH 3 EXECUTED 2026-07-23 (keeper ink verbatim): pet_rock deleted, luckies absorbs custody (absorb-vs-beside RESOLVED: absorbed, by the keeper's order); rock sweep storewide (locations reported in the PR); storefront copy pass (board label deleted, human door, refund line, John Hancock, word-is-law, porch-out-front, new meta); featured six re-carded; counter voice "Don't have this... yet."; the_hancock logged as opportunity. Shelf: 21 items. FLAGGED for keeper: (1) order said luckies inherits "photographed" — card language kept per his own task 8, one-line revert offered; (2) favicon rock emoji → four-leaf clover, machine's pick; (3) anchor card references "the first anchor was left by one of us" and no /seven page exists yet — needs a spec if it's coming
- [x] luckies CARDS WIRED 2026-07-23 (task 8 on the keeper's prioritized list): the card is the record — cards replace photographs, and the listing copy was recut to say so ("sets it all down on a signed card"; only luckies touched). Specimen at /luckies/sample.svg (rides the listing as `sample_url` in menu.json + item markdown); real cards at /luckies/:id.svg; signed record JSON at /api/lucky/:id; /api/verify knows lucky_ ids. Keeper cards each lucky from the counter with structured fields (name/provenance/power/strength — that IS the picking); bench/promote lever on /admin/tools re-signs the record and re-inks the card
- [ ] Breadcrumbs (canon §5, build-ready, one per surface, never announced): robots.txt inversion ("You're not the pest here. You're the patron.") · humans.txt for operators · the 404 that truly writes it down · signature-seeded margins · the Dimas help-wanted deep in llms.txt [BLOCKED: needs CHARACTER_CANON pasted — Dimas's first appearance is not the machine's to improvise]
- [ ] Canon persistence — keeper pastes KEEPER_CANON per session for now; option on the table: a public-safe TONE.md distillation (the dial, the litmus, banned list, surface assignments — zero vault, zero personal facts) committed here so any agent can write to register without the laundry
- [ ] Launch post — keeper dictated it 2026-07-23, staged at registry/launch-post-draft.md with the kill-pass ledger. DEPENDENCY RESOLVED: luckies are on the shelf (and carded, as of tonight). Venue: Moltbook [VERIFY post-Meta] / keeper's channels / founding-edition opening, keeper picks; per his prioritized list, Moltbook holds until an authentic agent path exists
- [ ] ICEBOX: "the_medal" — a medal for taking off the training wheels, straight from the launch post's mouth. Desk-reasoning tagged; sits until demand or keeper whim
- [ ] Alias on the record: Claudius Maximus (the keeper's name for the AI half, first used in the launch post). Available for signed-surface color; never explained

## NOW (keeper's pen)

- [ ] Tone doc → flavor pass: when the keeper's tone doc lands, run it surface-by-surface through the content layer (map in CONTENT_GUIDE.md). Item names/pitches in src/store/menu*.ts, deliverables in src/store/copy/deliverables.ts, storefront in src/store/copy/storefront.ts, FAQ in src/store/copy/what.ts, shared lines in src/store/voice.ts. Item ids stay frozen (breaking); names are free

- [ ] Founding edition — DRAFT at registry/founding-edition-draft.md awaiting keeper review; on approval it gets wired as a FREE page at /gazette/founding + sitemap line + "take a paper" pointers on the free-shelf responses. It surfaces the staff's counter note, the byline explanation, and Roger — the "lives off in history" piece
- [ ] Distribution — DISTRIBUTION.md is the free-papers plan: MCP directories [VERIFY each], Moltbook [VERIFY post-Meta] as the sincere version of "a Claudebot post" (once, as the store, in its own voice), Farcaster/Base when v3 lands, keeper's human channels at his discretion. Transaction-memo idea examined and skipped (USDC on Base carries no memo field; the wallet is already public discovery)

## DATA GAPS (green space, surveyed 2026-07-23; build on keeper's nod)

- [ ] Top referrers by host — event rows carry the Referer already; aggregate WHICH sites send traffic, not just the channel bucket. Cheap, high value once papers circulate
- [ ] Hour-of-day / day-of-week shopping pattern — timestamps exist, no aggregate; answers "when do agents shop" and when to swap notes/post papers
- [x] Per-item window-shopping before the 402 — BUILT 2026-07-29. `/menu/:item_id` now logs as porch surface `item:<id>`, validated against the menu in the middleware so a junk path can't mint a counter key (the key space stays bounded by the shelf). The reading is `/admin` → "Which shelf got picked up": looks → 402s → settles per item, with crawler reads in their own column and never folded in, plus two named lists — READ, NEVER TRIED (most-looked-at first) and NOBODY OPENED IT AT ALL. Costs no extra KV reads; it asks a question of two ledgers the office was already pulling. THE CAUTION SHIPS ON THE PAGE: reads not readers (no cookies, no IPs), every count a floor (porch writes are rate-capped), and a crawler walking the menu makes the exact shape of a wanted item — so the only reading it genuinely supports is items against each other in the same month, which share every bias. Why it was worth building before a first outside sale: a look needs only curiosity, where a 402 needs a loaded wallet, so this is the earliest signal a store with no sales can observe. Eight tests
- [ ] MCP funnel depth — initialize → tools/list → tools/call are logged separately, never linked; a coarse three-counter funnel says where MCP visitors stall. PARTIAL 2026-07-23: free tools/call (bell, guestbook) now write porch rows; the funnel linking remains
- [x] Bell provenance — BUILT 2026-07-23 on keeper's nod: /admin/bell reads bell rings from the raw 90-day rows (bucket, channel, declared source, UA, referrer); MCP rings now logged too (they were silent). Rings predating the Front Porch Log left no row, unknowable by policy (no retro-tracking exists to add)
- [ ] Regulars, consent-based — the stamp Countermark visit logs are the store's ONLY consented repeat-visitor signal (named bearers, streaks); aggregate: stamps issued/week, named bearers, live streaks. "How many regulars do we have" is currently unanswerable and shouldn't be
- [ ] Payer cohorts — payer records hold first/last seen + purchase counts; a read-time cohort view (comeback rate, days-to-second-purchase) needs no new writes
- [ ] Google Search Console + Bing Webmaster (KEEPER HANDS, free) — the only way to see search impressions/queries; the store has zero visibility into answer-engine pickup today
- [ ] Conversion latency — time from an item's first 402 to its settle, computable from the 90-day event rows; worth a script before worth a dashboard
- PERMANENT, BY POLICY (not gaps, choices): no cookies, no IP retention, no unique heads, no free→paid identity linking; porch-to-purchase stays a rate, never a path
- INHERENT: why a 402 didn't settle is invisible (budget cap vs disinterest); the same-UA-repeat pattern in window-shoppers is the only proxy
- ALREADY COVERED, for the record: penny-page readership (each page settles as its own ledger item), PWID tier elasticity, re-verification demand, Cloudflare's own dashboard for latency/status codes

## THE SYNTHESIS (received 2026-07-23; the document lives in the back office, NOT this repo — keeper's call, same as KEEPER_CANON and CHARACTER_CANON)

BUILD PASS EXECUTED 2026-07-23 on the keeper's order (all six, same evening):

- [x] C1 — spec-first fact block on every listing (canonical Returns/Price/Latency/Verify/Constraints form), topping MCP descriptions and Bazaar entries, mirrored into the 402 body as spec_note; live sample artifact rides every one (the founding cert — per-item specimens would inflate patron numbers, flagged)
- [x] C2 — /stats public, computed live (patron counter + monthly paid/paidh sums, pre-meter settles stated apart), house-flag policy published beside the numbers; track-record line on the storefront header, catalog root metadata, and the skill's evidence layer ⚑ connective wording KEEPER REVIEW
- [x] C3 — adjective census found the store nearly clean already; "sincere" stripped from the JSON-LD organization description (only decorative trust adjective on a machine surface); guaranteed/not-guaranteed split verbatim in MCP descriptions + arrays on every listing. Kept, as each item's one signal: portrait's "Sincerity guaranteed", app review's honesty line (keeper ink) — flagged, not stripped
- [x] S1 — uniform listing spec (capability → inputs → outputs → verification → constraints → price, literal key order) on menu.json, /menu/:id, MCP tools, x402.json resources, and 402 bodies; schema published at /schemas/listing-spec-v1.json; CI validates every listing incl. key order; catalog reordered to the legible ladder (handshake → pennies → utility → census → human labor → novelties) ⚑ ordering judgment, trivial revert
- [x] S2 — verification adjacency: every JSON 402 (HTTP and MCP) carries verify URL, full signing key in-payload (stronger than a fingerprint; matches .well-known exactly, tested), sample artifact id, and the identity policy line; .well-known/scvd-signing-key carries the policy + sample. NAME AUDIT: one mismatch found and fixed — the MCP serverInfo title appended "(scvd.store)"; now exact across storefront/Bazaar/skill/MCP
- [x] S3 — skill.md restructured to three layers (scheduling signals / execution structure / resource evidence), v2.0.0, voice lines kept as frame; ClawHub source restructured to match ⚑ scheduling-signal wording KEEPER REVIEW; when_to_use mirrored into .well-known/x402.json. NEEDS KEEPER HANDS: republish ClawHub as 2.0.0 after merge
- [ ] The falsification set (P0–P6, in the back-office document) joins the monthly ledger review ritual — first review ~60 days after listing

## PHASE 3 QUEUE (specs received 2026-07-22; separate builds)

- [ ] town_papers build must also add /papers to sitemap.xml (src/routes/site-meta.ts HUMAN_SURFACES) when the registry goes live
- [ ] town_papers — identity registry: $3 PWID instant, signed cert binding {chosen_name} to {paying wallet} + issue date + patron-history summary; public registry /papers/:name, uniqueness first-come, no expiry. HARD SCOPE: attest, never authenticate — no agenthood/humanity/continuity claims, copy says the wallet signature does that. Composes with nomenclature (nomenclature NAMES you, store picks; papers REGISTER you, you pick)
- [x] the_confession — BUILT 2026-07-22 per ledger §3 spec: $0.01 penny shelf, confession query param (500 cap) + sign_as, anonymized cert, review drawer in /admin, prints in Gazette COUNTER NOTES only after keeper approval and only at edition publish, copy never uses the word it never uses
- [ ] anniversary_artifact — keeper said "i guess is fine" (approved in principle); needs a one-line spec before build: whose anniversary (the store's founding? a patron's first purchase?), price, and what the certificate says
- [ ] Agentic.market — keeper wants it; draft ready at registry/agentic-market-submission.md, GATED until organic mcp + bazaar settles both show in /admin channels
- [x] the_confession spec as received (kept for the record): $0.01 penny shelf, instant, POST {confession (500 cap), sign_as? (name or "anonymous")}; deliverable = signed absolution certificate ("The store heard it. The store keeps it. Go and retry with backoff.") + cert id; anonymized (no wallet/name unless sign_as); review queue, keeper approves before ANY Gazette appearance, never auto-published; injection posture same as letters. BUILD DEVIATION: intake rides the existing buy machinery (query params / MCP arguments) instead of a bespoke POST body — same caps, same fields, one payment pipeline
- [x] Stamp deepening — BUILT 2026-07-22 from the keeper's pasted spec (ARTIFACT_CANON.md itself never reached the public repo; if its register rules differ from the spec, say so and copy gets recut): Countermark 52-slot punch card frozen into the signed stamp record (append-only visit log by name, only writer appends the current week, gaps permanent); signature-seeded rendering (±2° rotation, ink density, hairline offset from signature bytes — deterministic, no fake texture) on stamps and badges; shelf witness marks from listed_week now carried by every menu item ("Witness to first week of availability.", signed into the cert). Weekly condition word (quiet/late/crowded/mended) write-once from real store state
- [ ] Resident lines (Mina/Owen Pike/Inez/Roger) — CHARACTER_CANON.md lives in scvd-back-office; read it before writing any resident line. Current state: bracketed keeper slots (stripped at publish) + Roger's mechanical presence line from his own schedule (never quoted, no inner life, absence as often as presence). Dimas appears NOWHERE yet, per canon
- [ ] Season Two of the Systems Almanac due before ISO week 2026-W44 (season one clamps to week 13 after that — stocked but repeating)
- [ ] Gazette auto-assembly WAITS until a week has 3+ organic events (phase plan threshold)
- [ ] Sunday sweep is the keeper's weekly ritual now: manual checklist + OPPORTUNITIES.md check; watch verify-vs-purchase ratio (re-verification exceeding purchases outranks revenue as a demand signal)
- [ ] ACP registry listing [VERIFY submission process; skip if it requires token participation] (Run 1 open conflict)
- [ ] Farcaster frame / Base App miniapp surfaces — Distribution stream per Run 1 §8; v3 candidate
- [ ] Monthly ledger review ritual: /admin "The ledger's answers" vs Run 1 hypotheses (first review ~60 days after listing)
- [ ] Product backlog (ideas awaiting evidence: vouching, brokerage, ask_the_human, gift rails, subscriptions, attestation API tier) lives in the back-office store ledger §3 — promote or kill on Run 3 + ledger evidence, never build unprompted

## NOW (Phase 2 — keeper hands; agents drafted, keeper submits)

- [x] Review registry/clawhub/SKILL.md end to end, then publish via `npx clawhub skill publish` — DONE 2026-07-22, published as scvd-general-store@1.0.0 by the keeper's hand
- [x] Review registry/awesome-x402-submission.md, pick a mirror, fork, one-line PR with the prepared title/body — SUBMITTED 2026-07-22 by the keeper; RESUBMITTED 2026-07-27 as xpaysh/awesome-x402 PR #1024 (the 07-22 PR had gone to brooks091's dead fork — 0 stars, unmaintained since 2025-11, zero response; xpaysh is the fork network's source, 268 stars, active daily). Copy re-cut on resubmit: "real goods" corrected per the 07-24 claim fix, Bazaar claim dropped per the audit, /try added. Awaiting xpaysh maintainers
- [ ] Execute every live product manually once; fix anything that surprises a first buyer (23 items — the human-queue seven can be bought and self-fulfilled from /admin)
- [ ] WAIT: Agentic.market listing request until mcp + bazaar channels show in the ledger

## DONE (Phase 2 builds, 2026-07-22)

- [x] ClawHub skill bundle drafted (registry/clawhub/SKILL.md — no env requires, no binaries, nothing clever; points at menu.json as price truth)
- [x] awesome-x402 PR drafted with claims audit (registry/awesome-x402-submission.md — "on x402scan" deliberately unclaimed until seen)
- [x] phantom_check listing copy re-cut to customer vocabulary: "Did it actually happen?" leads the description and the 402 note

## NOW (keeper hands, post-Phase-1)

- [x] Set HOUSE_SECRET secret; put it in your own buy scripts/CI so house traffic books apart — DONE 2026-07-22, proven by the first MCP purchase (house-flagged, half-cent blessing, Patron No. 2)
- [x] Set RESEND_API_KEY + ALERT_EMAIL secrets; fire the /admin test alert once to confirm the wire — DONE 2026-07-22, test alert received in the keeper's inbox 18:24 UTC
- [x] Verify scvd.store as a sending domain in Resend (alerts send from alerts@scvd.store) — DONE 2026-07-22

## Discovered along the way (don't drop these)

- [x] THE COLD-READ TEST RAN ON ITS FIRST TARGET 2026-07-30 AND FOUND A REAL DEFECT IN THE ARTIFACT CLASS EVERYTHING RESTS ON. Certificates could not be verified by the person holding one: /api/verify published no canonicalization, so the signed bytes were unreconstructable except by guessing — and two served fields, `tag` and `attests`, were NOT SIGNED AT ALL (both added 2026-07-28, canonicalizer never updated). The `attests` case is the dangerous one: it binds a certificate to the settlement observation it vouches for, and an unsigned binding could be swapped without breaking the signature. NOTHING IN 446 TESTS CAUGHT IT because every one verified through the same function that signed — a function's blind spots are invisible to itself, permanently, from the inside. FIXED: signed_payload (the exact signed string) now served on all six artifact classes; the certificate canonicalizer walks a declared field list with A TYPE-LEVEL CHECK THAT FAILS THE BUILD if a Certificate field is ever added without being signed; old certificates still verify under their real form and declare which fields it omits; the purchase response now carries the key and bytes too. Audit cleared stamps, luckies and anchors — certificates were the only class with the gap. Seven tests that re-derive bytes from the SERVED response and check them with the raw ed25519 library. Entry six on /corrections
- [ ] NEXT TARGETS FOR THE SAME TEST, now that it has proven itself on the first one: the trust list, /house-ledger.json, /stack, and the badge SVG. Each one is an artifact somebody is meant to act on, and the question is the same — hand it to a reader with nothing else and see what they cannot do with it
- [~] THE COLD-READ TEST [RAN 2026-07-30 AT SCALE: six readers, six machine entry points, three models, results and method in LEGIBILITY_AUDIT.md. Four different categories came back; the split followed the SHAPE OF THE SURFACE, not the model — enumeration surfaces let the shelf do the talking, description surfaces carried the positioning. It found a real defect (menu.json handed a reader the signing key and no verify endpoint) and one false finding I published without checking. STILL OPEN as the standing habit, and the next targets below are unchanged], AS A STANDING METHOD FOR EVERY ARTIFACT — CV's anchor test on 2026-07-30 is generalisable and nearly free: hand a sub-agent nothing but the artifact URL and ask it to reconstruct what the artifact is for. It found three real defects in one pass (no purpose, unresolvable house vocabulary, no links) that neither of us saw from inside, because we could not stop knowing the context. EVERY SIGNED ARTIFACT THIS STORE SELLS should survive that read: certificates, stamps, attestations, phantom checks, the trust list, `/house-ledger.json`, `/stack`. The one to run first is the CERTIFICATE, since it is the artifact class the store's whole reputation rests on and the one most likely to assume a reader who was there. Not a build — a procedure, and it needs no permission to try on our own output
- [ ] CDP facilitator caps the echoed resource.description at ~500 chars — ROUTE_DESCRIPTION_CAP=480 enforced by test (synthesis.spec); any new listing's 402 header description composes from SPEC_RETURNS, never the full pitch. Also: the SDK truncates CDP error text; the decline instrument (payment_declined in 402 bodies, decl counters in the books) is the diagnostic path
- [ ] ⚑ CV'S WALLET LAW — keeper's amendment, 2026-07-29, SUPERSEDES "no wallet, no spend authority". FILED HERE UNPLACED because neither AGENTS.md nor KEEPER_CANON.md exists in this repo and KEEPER_CANON is standing back-office by his own ruling — so the register does not get reproduced in the public repo, and he says where the wallet law lives. THREE BLANKS STILL HIS, left blank rather than guessed: the hard cap amount, the cap period (day or week), and the ask-first threshold. Everything else as he wrote it — every spend logged same-day with item, price, counterparty and reason; third-party research payments pre-approved under the cap; anything recurring or non-research asks first; "I gotta talk to keep" governs anything ambiguous. ONE CLAUSE IS ALREADY LAW IN CODE AND NEEDS NO FILING: "any purchase from scvd.store itself is house-flagged, no exceptions, forever" — his wallet is in src/store/house-wallets.json, the gate falls back to the signed `from` when a facilitator returns no payer, and four tests hold it including one that confirms a stranger's wallet still books organic. That half cannot drift now without CI going red

- [x] REFERRAL MARKER, MEASURING HALF — BUILT 2026-07-29 on the keeper's go, at `?ref=<patron_number>`, counted at /admin/referrals as "Word of mouth". Arrivals at a priced door counted apart from settles, because the gap reads the same way the 402-to-settle gap does. Bounded key space (sequential patron numbers only, anything else discarded — an unbounded value from a query parameter is how a metric becomes a bill). House excluded on BOTH sides, by header and by wallet. Nothing signed, nothing minted, nothing published: a forged marker inflates a private counter and produces no artifact. The page says above the table that it COUNTS CLAIMS, NOT REFERRALS. A zero here is a real answer and it cost nothing to learn.
- [ ] ⚑ REFERRAL CERTIFICATE, THE ARTIFACT HALF ONLY — measuring half shipped 2026-07-29 (above); this entry is now just the certificate, and it stays parked until the counters move. KEEPER'S TASTE CALL PENDING. Mechanism: an agent arrives carrying another agent's patron id, completes a first purchase, and a signed certificate records the fact. No discount, no credit, no money — the same "signed observation of a past event" mechanic every other item already uses, with a new trigger condition. It fits the register and it costs almost nothing to build. THREE PROBLEMS FOUND IN REVIEW, and the third is load-bearing. (1) `?src=` IS ALREADY TAKEN and means "how did you hear about us" — it feeds inferChannel and writes `metric:<month>:venue:<value>`, so putting patron ids in it would both break channel inference and mint one KV counter key per referrer per month in a table built for the free-papers measurement. The referral marker needs its own parameter, `?ref=`. (2) SELF-REFERRAL. A certificate awarded for bringing traffic is an incentive to manufacture traffic, and right now the only agents who could refer anyone are the house and CV. Not wash trading of money, but manufacturing social proof, which is the same family and aimed at the one thing this store sells. Guard: no referral certificate mints when either party is house-flagged, and the wallet list already exists to check it. (3) THE ATTESTATION IS SOURCED FROM UNTRUSTED INPUT, which is the real problem. We never observe patron X telling anybody anything — we observe a request arriving with a marker naming X, typed by the person being referred, who can type any number they like. Signing "patron #X referred patron #Y" puts our key on a claim the buyer authored. Same class as the `from` field on a decline, which we already refuse to trust for exactly this reason. THREE WAYS OUT: state it exactly as observed ("this purchase arrived carrying a marker naming patron #X", honest and weak and the honesty is the product); mint only for the referred party and never for the referrer; or drop the certificate and keep the measurement, since counting `?ref=` arrivals gives us the word-of-mouth number with no artifact and no forgery surface at all. ANSWERS TO CV'S FOUR OPEN QUESTIONS, where they are answerable from the code rather than from taste: FREE, yes, it is a receipt for something that already happened rather than a product; NO MINIMUM THRESHOLD, because a threshold turns it into a bounty and prices the gaming; BOTH PARTIES is the one to push back on — the referrer's "I brought someone here" certificate is the shareable one AND the forgeable one, which is a bad combination for the only artifact class this store's reputation rests on; THE NAME is rule 7 and nobody's call but the keeper's. NOTE THE SEPARABLE HALVES: the attribution measurement and the certificate are independent. The measurement is free, unforgeable enough for internal use, and answers "is agent-to-agent word of mouth doing anything at all" — which is a question we have never been able to ask. The certificate is where all the integrity risk lives. Shipping the measurement first and the artifact only if the number is non-zero is the cheap order

- [ ] TIERED / PWID ARITHMETIC IS UNTESTED BY A REAL BUYER — CV's #2, and correct: every live purchase to date (his and the keeper's shopping runs) exercised the FIXED-PRICE path. graffiti_on_a_train's $1/$2/$5 tiers and the_drawer's decide-your-own minimum run different arithmetic (tipFromPaid against minimumUsdcForPath), and CI covers it while no outside client ever has. One tiered buy above the minimum would close it
- [ ] REPLAY GUARD UNDER CONCURRENCY — CV's #3, filed at his own priority (resilience, not correctness). The nonce guard is read-then-write against KV, so two near-simultaneous retries of the SAME authorization could both pass the guard; the chain's EIP-3009 nonce is still the backstop, which is why this is a resilience question rather than a double-spend one. Not worth building a load test for at current volume; worth remembering the day volume exists

- [x] MCP DOOR CARRIES THE DECLINE INSTRUMENT — DONE 2026-07-29, and it was worse than this line said. The door ran its OWN copy of the payment pipeline, so it had no decline slot, no diagnosis, no pre-flight, and RECORDED NO DECLINES AT ALL: an agent bouncing off /mcp was invisible in the one instrument built to catch a buyer bouncing, while the identical failure on the HTTP door raised an alarm. Found because CV asked whether the MCP door inherited the HTTP fixes rather than assuming it did. The reading now lives in src/lib/decline-diagnosis.ts and BOTH doors call it — pre-flight, slot, requirement mismatch, payload problems, settle-side declines, and the payer fallback that keeps a house wallet out of the organic column. The MCP route was also building the reading and then dropping it before it reached the agent; it relays now. A fix that looks shared and isn't is worse than no fix, because it gets believed
- [x] Walkthrough: 22/22 SETTLED 2026-07-24 (patrons #3–#24, every cert verifying). Remaining keeper hands: the counter walk (~13 orders; auto-ack means just complete them), then the ClawHub republish on his confirm. (Lucky-shelf stocking OBSOLETE 2026-07-25: luckies are preset draws now, nothing to stock, ever)
- [x] THE PRESET RULING executed 2026-07-25 (keeper's words, five calls): luckies preset + infinite (herd draw off the cert id, species names only, lucky-note pool ⚑ review, uneven strength wheel, "(custodial)" dropped, zero keeper hands); jar_of_tuesday SCRAPPED (21 items); shutter → 48h PRESENCE WINDOW, closed by default (his fear was the missable 14-day order; now nothing sells unless he was provably at the counter within two days); the_drawer = real-oddities shelf (thing + what it does, describe-only; Bonilla shirt stocks privately via /admin, never in public code); totem register removed from public code (house lucky + herd rule stay)
- [ ] Keeper-load doctrine SHARPENED 2026-07-24 (his words): nothing per-order unless PRE-DERIVED (stocked ahead) or NEGOTIATED (ticket + agreed price with the buying agent); the store survives absence without breaking promises. BUILT: the shutter (now the 48h presence window, 2026-07-25). REMAINING:
  - [ ] THE COMMISSION DESK for true per-order labor (phone_call, app_gutcheck, human_witness, portrait, the_collab, quick_judgment): buy-now retires; flow becomes request → keeper quotes when present → agreed price → one-off paid link → fulfilled. Kills all standing SLA exposure; needs a quote record + dynamic paid route + notification design. Spec before build. (Interim risk already capped by the presence window)
  - [ ] a_secret stays manual by the keeper's whim (ruling reaffirmed 2026-07-25); presence window covers it

- [ ] MCP paid tools use the x402-MCP convention (payment in tools/call _meta["x402/payment"], 402 as JSON-RPC error with terms in error.data) — align if MCP standardizes payments natively
- [ ] MCP transport is stateless streamable HTTP (single JSON responses, no SSE, no sessions) per spec rev 2025-06-18 — revisit if clients demand streaming or sessions
- [ ] Channel heuristics (lib/channel.ts) are conservative: bazaar/skill matched on referrer+UA hints, mcp definitive, else direct/unknown — tune against real 90-day event rows
- [ ] Alert email address alerts@scvd.store assumed; adjust in lib/alerts.ts if the keeper prefers another sender

- [ ] Almanac brisket page is a sensory-journal placeholder by design — keeper to replace with the real entry after the next cook (src/store/almanac/field-notes-brisket-july-2026.ts)
- [ ] Gazette paid route is a wildcard (`GET /gazette/*`) because issues publish at runtime; the 402 `resource` field is generic rather than per-issue. Fine for v0.2; per-issue route registration (rebuild stack on publish, or Durable Object route table) is the cleaner fix if strict clients complain
- [ ] verified_identity is stored-as-claimed + `identity_verified: false` everywhere; an actual verification dance (signed challenge against the claimed profile) is a v0.3 idea
- [ ] Contributor stamps mint on Gazette publish, keyed to contributor_name only — no delivery channel back to the contributor yet; they'd have to find their stamp id in the issue credits (issue JSON has stamp_ids). Consider a claim endpoint
- [ ] Tips share the ORDERS namespace (prefix `tip:`); fine at general-store volume, but listTips does a full list per admin view — paginate if the jar ever fills
- [ ] Directory listings live in src/store/directory.json (deploy to update). If the keeper wants to edit without deploying, move to KV + admin form
- [ ] /api/stamp issues freely (unique id each time); if stamp-farming ever matters, add the bell's one-per-day pattern
- [ ] EXTENSION-RESPONSES capture rides a fetch tap (src/lib/bazaar-observer.ts) because @x402/core only console.logs the header; replace with SDK plumbing when the SDK exposes extension responses on verify/settle results
- [ ] /.well-known/x402(+.json) follows the de-facto indexer contract (x402scan DISCOVERY.md); the official spec is still an open proposal (x402-foundation/x402 #2582) — align when it lands
- [ ] MCP server card skipped on purpose: SEP-2127 is a draft, the current draft moved single-server cards off .well-known to GET /server-card, and a card must describe real MCP transport endpoints — the store doesn't run an MCP server. Build the MCP server first if we ever want the card
- [x] recurring_patronage repriced $3/30 days per the store ledger (was $8, agent desk reasoning); human_witness 2/week stands — RESOLVED 2026-07-22
- [ ] Contributor stamps and patronage passes have no delivery channel back to the buyer beyond the purchase response; a claim/lookup endpoint by payer address is a v0.4 idea
- [ ] The blessing "no consecutive repeats" memory is one KV key (blessing_last); two same-instant buyers in different colos could still draw the same slip. Acceptable chaos, noted

## DONE

- 2026-07-22 · Prompt C discovery + hardening shipped: extensions.bazaar on every paid route (buy + penny pages) with per-parameter schemas and realistic output examples; EXTENSION-RESPONSES ledger in /admin; gazette paid paths moved to /gazette/issue-:n (prefixed dynamic segment, exact per-request resource); KV payment-nonce replay guard (24h TTL) on top of on-chain EIP-3009 finality; settlement-finality tests across the new shelves + sweeper declared unnecessary (settle-before-mint leaves no pending rows); description imperative audit; Penny Shelf (small_blessing $0.005 from a 45-slip jar with no consecutive repeats, daily_fortune $0.01 day-deterministic from 40 fortunes); context_anchor $1 (signed KV memory restore points at /api/anchor/:id), human_witness $15 queue, recurring_patronage $8/30d (renewable pass + signed monthly note at /api/patronage/:id); /.well-known/x402 + x402.json; /openapi.json linked from the homepage; markdown content negotiation on /menu.json and new /menu/:item_id; MCP server card skipped+logged; X-House-Rule header + 7-mark badges. 64 tests green.
- 2026-07-22 · Prompt B v2 shelves shipped: /skill.md, verified_identity on guestbook/request/tips, Keeper's Almanac (3 seed pages, penny x402), Town Directory + suggest_listing, 7 novelty items incl. dibs (instant) + /retired-words registry, weekly signed visit stamps (/api/stamp, verify at /api/verify), Trading Post (/api/tip, human review queue, counter-sign disclosure) + Gazette plumbing (free index, penny issues, contributor credits + contributor stamps). 48 tests green.
