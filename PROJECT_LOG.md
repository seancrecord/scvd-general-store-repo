# Project Log — Sean-Claude Van Damme's General Store

Running log of decisions, prompts run, and status. Agents: read this first. Newest entries at top.

## Status board

- Domain: scvd.store — LIVE (Cloudflare, auto-renew on)
- Wallet: Rainbow, Base address — provisioned (address lives in CF secrets only)
- CDP facilitator key — provisioned (CF secrets only)
- Worker deploy: LIVE at scvd.store (2026-07-22 — KV wired, five secrets set, Git-connected builds on)
- Bazaar/Agentic.market listings: STARTED 2026-07-22 — first settled payment on /api/buy/hello is on the books; other routes list as they sell
- Moltbook presence: NOT YET (re-verify their API post-Meta acquisition before building against it)

## Decisions (do not relitigate without the keeper)

- Standing rules live in HOUSE_RULES.md; read before proposing anything clever.
- Architecture: CF Workers + TS + Hono + KV + x402 v2 (@x402/core + @x402/evm + @x402/hono) + CDP facilitator via createFacilitatorConfig(). Custom payment gate in src/lib/payment-gate.ts: verify → settle → only then mint. Single Worker, no React.
- Patron counter: KV claim-with-readback; ~60s cross-colo window accepted for v0.1; Durable Object counter is the named v0.2 fix.
- Tips: three-tier 402 challenge (min, 2x, 5x); above-minimum recorded as tip.
- Voice: warm roadside general store; never explain the joke; never claim "first ever"; two audiences always (transacting agents + their log-reading humans).
- Refund copy: a promise the keeper keeps personally — never "automatic" until automated.
- No automated self-payment heartbeat (wash-trading screens); manual only-if-idle Sunday judgment call. Real unique payers via Penny Shelf preferred.
- Store never asks visiting agents to run code or share credentials — public endpoints only.
- Novelty lane: open but not empty — AgentStamp (utility attestation) and Alfred's Digital Bazaar (whimsy $0.10-1.00) are neighbors; we differentiate on sincerity, physical custody, human labor, two-proprietor voice.

## Log

- 2026-07-22 · PHASE 2 drafts: the registry drawer. New /registry folder at the repo root — publishing stays a keeper queue (house rule 30), so these are review-ready artifacts, not submissions. (1) ClawHub skill bundle (registry/clawhub/SKILL.md) per the verified ClawHub format (SKILL.md + YAML frontmatter, metadata extracted at publish, security analysis compares declarations to behavior): ours declares no required env, no binaries, no install specs — instructions for public HTTPS endpoints only, which is both the honest shape and the shape that scans clean; prices deliberately not enumerated (points at menu.json as source of truth) so the static bundle can't drift into lying. (2) awesome-x402 PR draft (registry/awesome-x402-submission.md) in their exact one-line format with the filled PR template and a claims audit — every credential verified true today; "on x402scan" deliberately NOT claimed because it couldn't be independently verified from the build environment. Note: several awesome-x402 mirrors exist; the two most active are named for the keeper to choose. (3) phantom_check listing copy re-cut to customer vocabulary per the ledger: "Did it actually happen?" now leads both the description and the 402 note. Agentic.market stays on WAIT until mcp + bazaar channels appear in the ledger.
- 2026-07-22 · The MCP door's first sale, supervised: buy_small_blessing over tools/call from the keeper's burner, x402 settled in-band via _meta["x402/payment"], half a cent, Patron No. 2 ("MCP Test Customer"), cert_e2gde3yvsd verifying valid, house-flagged via X-House so the organic books stay clean. The blessing drawn: "May your schema migrations run forward and never need to run back." Every Phase 1 subsystem exercised in one purchase: MCP handshake, tool catalog, 402-as-JSON-RPC-error, in-band settle, shared fulfillment, replay guard, house exclusion, channel attribution (mcp), and the alert wire confirmed separately by test email. The store now sells through four doors.
- 2026-07-22 · PHASE 1: the MCP door + attribution + the four alarms + ledger hygiene. (A) The store is an MCP server at POST /mcp — streamable HTTP per spec revision 2025-06-18, stateless (single JSON responses, which that revision permits in place of SSE; no sessions, no server-initiated streams — Workers are stateless and every tool is single-shot). initialize/tools/list free and unauthenticated; 26 tools — 4 free (read_store_guide, ring_bell, sign_guestbook, verify_artifact; the bell logic moved to services/bell.ts so both doors ring the same bell) + 22 buy_* tools generated straight from MENU_ITEMS (no forked catalog), each with flat input/output schemas and explicit completion criteria in the description. Paid tools/call runs the IDENTICAL pipeline as HTTP — same x402 stack via a synthetic adapter for GET /api/buy/<item>, same replay guard, same settle-before-mint, same fulfillment (extracted to services/fulfillment.ts, now shared by both doors); payment rides _meta["x402/payment"] (x402-MCP convention), payment-required returns as JSON-RPC error 402 with the challenge in error.data. Advertised in llms.txt, /.well-known/x402.json, menu.json, openapi.json, and each Bazaar route description. (B) Attribution: every 402/settle/verify writes an event row (channel, ua, referrer, item, house flag; 90-day TTL) plus split aggregate counters. Channels: mcp (definitive, tagged by the handler) | bazaar | skill (referrer/UA hints, documented in lib/channel.ts) | direct | unknown; declared ?source= recorded verbatim as a claim, never overriding inference. House exclusion: founding burner wallet + optional HOUSE_WALLETS + X-House/?house=<HOUSE_SECRET>; house events stored but excluded from organic counts everywhere; the flag never appears in a public response; IP deliberately not a signal. The founding $0.50 settle is backfilled by hand as house/founding in the 2026-07 ledger read. (C) Four P1 alarms, nothing else pages: facilitator verify/settle THROWS (declines don't page), mintCertificate/issueStamp signing failures, hourly worker self-check (KV+signing round trip), queued orders unacknowledged >24h (admin gained an Acknowledge button). Email via Resend's plain HTTPS API (one fetch, one secret, no bindings — chosen over the deprecated MailChannels free path and over Email Routing bindings for config simplicity); without RESEND_API_KEY/ALERT_EMAIL alerts degrade to console + a KV alert log shown in /admin; 6h dedupe per condition; /admin test-alert lever included. (D) The ledger's answers now shows organic and house columns side by side (conversion organic-only), a channels row from the new attribution, and verify-calls per item (re-verification is demand, tracked deliberately). 92 tests green.
- 2026-07-22 · The Porch + the paper fix + house cat, a free-rein pass by the shelf-stocker. FIX: the neon rewrite had orphaned the smaller rooms (/what, /directory, /gazette, /almanac, /zodiac human views referenced deleted .paper CSS classes and rendered unstyled) — they now carry their own paper stylesheet (src/pages/paper-css.ts); inside the store it was always daytime. NEW: GET /porch — around the side, facing the pines, free, nothing for sale out there. One ambience line per UTC hour, deterministic, so everyone sitting in the same hour shares the same night (24 lines in src/store/porch.ts); a nightly seat counter that forgets within two days; JSON for agents ("Stay as long as your timeout allows"), a night scene for humans. And the store has a cat now: out about two hours in five on its own deterministic schedule, sits on the readerboard when it's out (storefront and porch both), amber eyes, blinks every nine seconds, tail keeps its own time. Explained nowhere; the code comment says "house cat". The porch is the shelf-stocker's stamp: the store now has one place where nothing converts, on purpose. 81 tests green.
- 2026-07-22 · Storefront overhaul: the neon front. The homepage is the one screen a human ever sees or shares (agents read llms.txt/menu.json), so it's now built for that job — cyberpunk roadside at dusk, general store on the porch: flickering neon sign (one tube out, as is right), OPEN 24/7 FOR AGENTS — HUMANS WELCOME, counter chips (patrons served / bell / mailbox), the week's note on a paper receipt, six featured shelves instead of all twenty-two (full catalog pointed at llms.txt), two doors — a paper one for humans ("Your agent sent you?" → /what) and a terminal-green one for agents (llms.txt / menu.json / skill.md / openapi.json) — three guestbook slips, fine print. No scripts, ambient glow only, reduced-motion respected, voice unchanged: cyberpunk visuals, general-store words. 77 tests green.
- 2026-07-22 · The Mailbox (block H) + store-ledger alignment, same branch as the Run 1 buildout. Mailbox: POST /api/letter — free, one per visitor per day (bell arithmetic), 2000-char cap, optional from_name/verified_identity (stored as claimed, marked unverified); letters are PRIVATE — admin queue only, never published, never rendered on any public surface, stored raw and shown to the keeper escaped; pickup at GET /api/letter/:id returns status (received/read/replied) and the keeper's signed reply if one exists — the letter itself never comes back out, and archived reads as read from outside; /admin gets the letter queue with read/reply/archive, replies ed25519-signed on send; the storefront shows only the running counter (received/answered); the Sunday digest carries the unread count. Census shelf re-specced to the store ledger §3, which overrides the agent's desk-reasoned numbers from earlier today: phantom_check $0.25 with the real premise — an out-of-band probe ~6 hours after purchase (hourly cron sweeps due checks at :30; the pickup route also resolves due checks on read so the promise survives a missed tick), signed health attestation at /api/phantom/:check_id [demand tag: Moltbook silent-failure complaints]; quick_judgment $3 (was $5); certificate_of_patronage $20 min PWID (was $10); recurring_patronage $3/30 days (was $8). 77 tests green. The full product backlog and staff addendum stay in the back office; the public repo carries only what's on the shelves.
- 2026-07-22 · Run 1 buildout (from RUN1_SYNTHESIS, buyer census). Shipped: /what — the Operator Glance, the ten-second check written for the human whose agent asked to spend money here (census: the human storefront is the conversion surface for the premium shelf; per-tx caps of $1-5 mean humans approve everything above); day-one instrumentation per §instrumentation — 402s issued vs settled per item (the budget-cap gap), PWID tier picks, discovery-channel attribution (source query param, else referrer, else direct), payer wallets with first-seen dates (metric:<month>:* and payer:* in COUNTERS; read-modify-write counters, lost increments accepted), orders now carry detail/source/user_agent/referrer, all reviewed in /admin under "The ledger's answers"; the census shelf — phantom_check $0.02 instant (one signed GET from the counter, unreachable is a finding), quick_judgment $5 human_queue 5/week (dilemma in the detail param, one verdict out), certificate_of_patronage $10 PWID instant (entitles the holder to nothing whatsoever; gilt badge via patronage flag) — premises for the first two are desk reasoning, named as such, keeper to bless; multilingual llms.txt (ES/PT-BR/JA/KO/ZH/RU short sections; census says non-English llms.txt is standard practice internationally); Agent Zodiac structure-first — twelve signs cast from store lore, address→sign fixed for life (FNV over lowercased address; derivation frozen), weekly deterministic horoscope free at /zodiac/{address}, character-research recast and almanac-rail archive deferred. NOT shipped: the Mailbox (block H spec never reached this agent — filed in TASKS). 75 tests green.
- 2026-07-22 · OPENING DAY. DEPLOY.md executed end to end: four KV namespaces wired into wrangler.jsonc, five secrets set (fresh ed25519 signing key cut the same afternoon), Git-connected builds on, scvd.store live. Free-shelf smoke test passed — 402 challenge on hello verified field by field (x402 v2, exact, eip155:8453, 500000 atomic, real USDC contract, till address confirmed), first visit stamp issued and verified, bell rung, X-House-Rule riding every response, 21 URLs on /.well-known/x402. Then the supervised fifty-cent handshake: hello bought from a burner wallet over x402 v2, settled on Base (tx 0x47c8fee81e6d11bf07c9580b0d3aea3fabb9c2a9fe7aee3ae6f2f8391450bc9c), Patron No. 1 minted to "First Customer", cert_4dww28dx5j verifying valid: true, badge no. 1 on the wall. That settle is also the store's first Bazaar listing. The store is open.
- 2026-07-22 · Copy kill list executed + districts wired. The keeper approved all 12 COPY_INVENTORY.md flags; replacements applied as proposed (Penny Shelf copy stops reviewing itself, "coast is clear" out of the money-failure lines, the_collab loses the t-shirt slogan, the patronage receipt says the catchphrase once, two fortunes rewritten off internet cadence). COPY_INVENTORY.md marks each flag killed with the date. Rule 39 districts now on the signage, one deadpan mention per room, explained nowhere: the Gazette rack and tip jar sit at The Red Clay Exchange (HTML + JSON district field), the Town Directory is kept at Hurricane Junction (directory.json note + district field), and context anchors are filed at Node 21 (menu copy, purchase receipt, and a held_at field on anchor reads).
- 2026-07-22 · HOUSE_RULES.md added at the repo root — the keeper's full standing rulebook, catalogued by section (voice & copy, commerce & trust, product & inventory, operations & staff, lore & tradition). This is the public copy: the IDENTITY & PRIVACY section (rules 24–28) lives in the back office only, and the numbering keeps the gap on purpose so the two copies stay in step. Rules change only by keeper decision, logged here with a date.
- 2026-07-22 · Identity and content pass. LOCATION: every active surface (store metadata → menu.json/llms.txt/skill.md/storefront/badges/simple pages, README, item copy, stamp mottos, Almanac pages, tests) now places the store in "Smokewire Crossing, somewhere in the Carolina pines" — a fictional locality. Reason: keeper privacy posture; the store's town should not be a findable town. Rule going forward: physical-custody item copy (pet_rock, the_drawer, jar_of_tuesday, human_witness) may claim state/region-level reality ("a real North Carolina rock", "the Carolina pines") but never a real city, county, or address. Historical documents (BUILD_BRIEF.md, past log entries here) intentionally left as written. ALMANAC: "What shipping three iOS apps taught me" deleted — it broke the content rule now written into the README and src/store/almanac/index.ts: entries are dated, first-person field notes — sensory, particular, slightly strange; never how-to, listicle, "lessons learned", career content, or anything resembling a blog post. If it could be posted on Medium, it doesn't go in the Almanac. "Notes from a Tuesday at the Crossing" retitled to "Notes from a Tuesday at the Crossing" (slug notes-from-a-tuesday-at-the-crossing). Seed pages are now two: the brisket field notes and the Tuesday note.
- 2026-07-22 · Prompt C (discovery + hardening) built on the v0.2 shelves. VERIFY findings first, since several items referenced July-2026 research: (1) Bazaar v2 — real and official; `@x402/extensions` 2.19.0 (x402 Foundation) ships `declareDiscoveryExtension` + `bazaarResourceServerExtension`; declarations attach per RouteConfig and the server extension enriches method/routeTemplate/pathParams at request time; the facilitator learns discovery from the client's echo of the 402's extensions. DIFFERENCE vs. expectation: the core SDK only console.logs EXTENSION-RESPONSES headers and does not expose them on verify/settle results, so capture is a strict-passthrough fetch tap (src/lib/bazaar-observer.ts) draining into KV; /admin shows the ledger. (2) /.well-known/x402 — NOT in the official spec; it's an open foundation proposal (x402-foundation/x402 #2582, June 2026). Implemented the de-facto indexer contract instead (x402scan DISCOVERY.md): minimal `{version:1, resources:[…]}` at /.well-known/x402 plus a richer /.well-known/x402.json, since scanners fetch one or the other. (3) OpenAPI 3.1 — real spec; shipped /openapi.json, linked from the homepage fine print. (4) Accept-based JSON/markdown negotiation — plain RFC 9110 content negotiation; shipped on /menu.json and the new /menu/:item_id. (5) MCP server card — SKIPPED: SEP-2127 is still a draft, its current revision moved single-server cards OFF .well-known to `GET /server-card` (catalog stays at /.well-known/mcp/catalog.json), and a Server Card must describe real MCP transport endpoints — this store runs no MCP server, so publishing one would advertise a connection that doesn't exist. Logged here, left in TASKS. Shipped: extensions.bazaar on all buy + penny routes (per-parameter inputSchema descriptions, realistic output.example); exact 402 resource everywhere — buy/almanac static per-endpoint, gazette per-request URL now that issues live at /gazette/issue-:n (prefixed dynamic segments, never bare ids); KV payment-nonce replay guard with 24h TTL (belt-and-braces over on-chain EIP-3009 nonce finality; already-settled nonces bounce before the facilitator is called); settlement-finality tests across the new shelves and a sweeper decision — none needed, settle-before-mint leaves no pending rows to expire; description audit removed the imperatives aimed at reading agents ("Start here", "Do not open", "Pick a word", "Tell us…" all rewritten declarative); untrusted-data labeling on anchors/gazette to match the guestbook's; no-store confirmed on every paid/order route including the new ones. New goods: Penny Shelf — small_blessing $0.005 (45 handwritten slips, KV-remembered last draw so no two consecutive repeats) and daily_fortune $0.01 (40 fortunes, FNV-picked per UTC day, same chalkboard for everyone); context_anchor $1 instant (signed, KV-stored agent state summary, readable forever at /api/anchor/:id, also honored by /api/verify); human_witness $15 human_queue (2/week, phone-call house rules); recurring_patronage $8 instant (30-day pass, renew via pass_id, signed monthly keeper note at /api/patronage/:id, note inked from /admin). House tradition per spec: X-House-Rule header on every response (ASCII "--7" because workerd warns on non-ASCII header bytes) and a small 7 in the lower corner of badges divisible by seven, one code comment, no explanations. Keeper-agent judgment calls to bless: $8 patronage price, 2/week witness stock. 64 tests green, tsc clean.
- 2026-07-22 · Prompt B (v2 shelves) built on top of main's x402 v2 stack, no restructuring. Shipped: GET /skill.md (agentskills.io SKILL.md frontmatter + worked /api/buy/hello example + the no-code/no-credentials pledge in writing); optional verified_identity on guestbook/request/tips, stored as claimed and always identity_verified: false; Keeper's Almanac — serialized journal, one TS file per page in src/store/almanac/, free index at /almanac, $0.01 x402 pages (3 seeds: brisket July 2026 placeholder, three iOS apps, Tuesday at the Crossing); Town Directory at /directory from keeper-edited src/store/directory.json (seeded empty, README house rules) + suggest_listing on /api/request; novelty aisle via existing certificate/payment-gate machinery — jar_of_tuesday $4, a_secret $10, grudge $6, smoker_blessing $7, retired_word $15 (+ public registry /retired-words, back-room retirement form), the_drawer $9 (all PWID, human_queue 168h), dibs $2 fixed instant; free weekly visit stamps at POST /api/stamp (ed25519-signed, SVG at /badges/stamps/:id.svg, design rotates by ISO week, verify at /api/verify alongside certificates); Trading Post POST /api/tip with the penny-with-credit counter-sign disclosure, tips to admin review queue (approve/reject, NEVER auto-published), Gazette plumbing — free /gazette index, penny x402 issues assembled from approved tips via /admin/gazette/publish, contributors credited + contributor-variant stamps minted on publish. Implementation notes: gazette paid route is a wildcard (issues publish at runtime); penny pages deliver markdown and don't mint patron numbers; payment gate's tip math got a path-aware minimum resolver (minimumUsdcForPath). 48 tests green, tsc clean.
- 2026-07-21 · Core build v0.1 complete (audit run, all 11 findings fixed, v2 migration done, 30 tests, squash-merged). Domain, wallet, CDP key, repo created same evening. NOTES_FROM_THE_COUNTER.md started.
