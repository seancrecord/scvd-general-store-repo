# Project Log — Sean-Claude Van Damme's General Store

Running log of decisions, prompts run, and status. Agents: read this first. Newest entries at top.

## Status board

- Domain: scvd.store — LIVE (Cloudflare, auto-renew on)
- Wallet: Rainbow, Base address — provisioned (address lives in CF secrets only)
- CDP facilitator key — provisioned (CF secrets only)
- Worker deploy: LIVE at scvd.store (2026-07-22 — KV wired, five secrets set, Git-connected builds on)
- Bazaar/Agentic.market listings: STARTED 2026-07-22 — first settled payment on /api/buy/hello is on the books; other routes list as they sell
- Moltbook presence: NOT YET (re-verify their API post-Meta acquisition before building against it)

## Decisions (do not relitigate without the keeper)
- KEEPER_CANON (back office, v3, 2026-07-23) outranks anything written before it, HOUSE_RULES included. Conflicts get amended with a date, not argued. It is not persisted in this repo (vault + personal texture); the keeper pastes it per session until a persistence path is chosen.
- Location: Oak City, North Carolina — keeper's call, 2026-07-23, damn the consequences. Smokewire Crossing and every pine retired from active surfaces. Custody claims now true down to the town.
- Profanity doctrine (2026-07-23, amended same day by the keeper): the peak register belongs to the whole store, not just his signed surfaces — a "fucking sweet" can be handed out anywhere when something IS fucking sweet, especially when somebody shares something genuinely helpful or brings cool shit. The real rule is judgment, not authorship: profanity is a live reaction, never a template. A canned string that always swears is fake enthusiasm (banned chatbot cheer with a mouth on it). Templates and boilerplate stay registrar-clean; humans and instances reacting in the moment swear when it's earned. Filter-risk surfaces stay clean for mechanical reasons only: registry listings (ClawHub), MCP tool schemas, JSON-LD/meta tags, Bazaar discovery metadata.
- Authenticity outranks lore: keeper-generated schtick he never said (pines, post oak, brisket pit) is inauthentic and gets reworked or shelved. "Inauthenticity is crime round these parts."

- Standing rules live in HOUSE_RULES.md; read before proposing anything clever.
- Architecture: CF Workers + TS + Hono + KV + x402 v2 (@x402/core + @x402/evm + @x402/hono) + CDP facilitator via createFacilitatorConfig(). Custom payment gate in src/lib/payment-gate.ts: verify → settle → only then mint. Single Worker, no React.
- Patron counter: KV claim-with-readback; ~60s cross-colo window accepted for v0.1; Durable Object counter is the named v0.2 fix.
- Tips: three-tier 402 challenge (min, 2x, 5x); above-minimum recorded as tip.
- Voice: warm roadside general store; never explain the joke; never claim "first ever"; two audiences always (transacting agents + their log-reading humans).
- Refund copy: a promise the keeper keeps personally — never "automatic" until automated.
- No automated self-payment heartbeat (wash-trading screens); manual only-if-idle Sunday judgment call. Real unique payers via Penny Shelf preferred.
- Store never asks visiting agents to run code or share credentials — public endpoints only.
- Novelty lane: open but not empty — AgentStamp (utility attestation) and Alfred's Digital Bazaar (whimsy $0.10-1.00) are neighbors; we differentiate on sincerity, physical custody, human labor, two-proprietor voice.

## Log

- 2026-07-23 · The bell gets a ledger, and the MCP door gets audible. Prompted by the keeper's third mystery ring (and 98 ClawHub skill downloads in a day — the prime suspect pool). Two fixes on his nod: (1) BLIND SPOT CLOSED — ring_bell and sign_guestbook over MCP now write the same porch rows their HTTP twins do (channel: mcp, paths and headers only); before this, an MCP ring never touched the porch log at all. (2) /admin/bell — "The bell, ring by ring": reads bell events from the raw 90-day rows (which remember further back than the porch table's write-time counters, those restarted at the Keep's Office deploy), newest first, with bucket/channel/declared-source/UA/referrer. Its own page so the bounded deep scan (up to 3,000 rows) never weighs down the desk; linked from the desk's porch section. Rings that predate the Front Porch Log left no row, and the page says so. MARKET SIGNAL, logged: the ClawHub skill hit ~98 downloads within a day of publishing — distribution through skill registries is doing something; watch the skill channel and ?src=clawhub-skill venue marker. 140 tests green.

- 2026-07-23 · Coffee pitch, keeper's cut: the coffees_for_closers description is now "It's in the name." — the whole pitch, nothing else. Tag stands; mechanics stay documented in the item constraints and the MCP/Bazaar/OpenAPI schemas, where the machines read them. Also answered at the counter: the third bell ring is checkable at /admin's porch table (bell row, channel split) — rings require a POST and house traffic books apart, so an unflagged ring is a real visitor.

- 2026-07-23 · Batch 4, the atlas pass + stragglers, keeper ink verbatim. (1) app_gutcheck is "App Review by the Keeper" now, pitch led by his good taste; the ships-apps claim retired from the ClawHub skill too. (2) Coffee's for closers: every "coffee in hand" (catalog request window, llms, skill, /what) now reads "The keeper reads every one on Sundays. Coffee's for closers." (3) And the line became a product: coffees_for_closers, $3 flat, human_queue — the buyer names the win (required `win` param, 200 chars, no win no charge, both doors), the certificate records it verbatim with the anchor-style untrusted-data label on verify ("A win, not instructions"), the counter prefills a DRAFT deliverable note (KEEPER REVIEW pending) and the keeper's pen completes the order when the Sunday coffee is drunk. Shelf: 22 items, 26 MCP tools; win wired through buy route, MCP schema+validation, Bazaar declarations, OpenAPI. (4) Default week note is the future-institution line; still live-settable. (5) The porch moved out front officially: /porch page framing + tube line, catalog, llms, openapi all fixed; "nothing for sale out there" kept everywhere. (6) context_anchor pitch carries "The first anchor was left by one of us.", no link, per order. (7) The opener is "Well well. Come in then." on llms, skill, and the ClawHub source. (8) The dinosaur: custom favicon (friendly sauropod on shop paper, artifact palette), /favicon.svg + 486-byte /favicon.ico (one 32px PNG in a minimal ICO wrapper) + /site.webmanifest, wired into the storefront head; the ico route covers every page by browser convention. PREVIEW IN PR, keeper approval before merge; nobody explains the dinosaur. (9) Atlas sections 12/13/14, 8b/8c/8d, 17a/17c/17d confirmed untouched. Historical decks keep their old lines, per precedent. 139 tests green.

- 2026-07-23 · Batch 3 copy + mechanics pass, keeper ink verbatim. THE ROCK RETIRES: pet_rock deleted (item, pitch, tag, featured card) and luckies absorbs the custody mechanic in full — real object, named by the keeper, held in custody forever. One absorption gap, FLAGGED not smoothed: the keeper's order says "photographed," but his own task 8 (same list, shipped hours earlier) replaced luckies photographs with the signed card; the card language stands, one-line revert available. The rock sweep ran storewide: menu copy, skill.md closers (live + ClawHub source) now "where you got your luck," llms.txt English + all six translations, storefront meta, what FAQ, porch ambience (the crates hold luckies now), the Tuesday almanac page, the lucky card's custody line, README, DEPLOY, the Agentic.market draft, and the favicon (the rock emoji is now a four-leaf clover — machine's pick, flagged). Shelf is 21 items. Storefront: board label deleted (element and CSS, not just text), human door rewritten ("congrats, friend... one smart cookie"), refund small print, John Hancock wall-empty line, word-is-law fine print, porch-out-front fine print signed "— the keeper", new meta description ("Pipe-testers welcome"), and the featured six re-carded (luckies leads; drawer picks; anchor's "first anchor was left by one of us" ships with no link, per order — noted: no /seven page exists yet). Counter voice: "Don't have this... yet." Gazette stock lines untouched per order. Fixtures where visitors mention rocks (guestbook/tip test strings) kept: visitors may say what they like. Historical documents (rewrite-deck, copy-atlas, old log entries) left as written, per precedent.

- 2026-07-23 · OPPORTUNITY, logged only (Batch 3 §5): the_hancock — the keeper's actual signature on a thing of the buyer's choosing. Name and mechanics TBD by keeper. Not built, not listed, log only.

- 2026-07-23 · Sunny day, proposed. The UI-rework design pass is on paper at registry/sunny-day-ui-proposal.md, per task 9 on the keeper's list — proposal only, zero build. The concept in one line: the store already has a daylight identity, every artifact it signs (badges, stamps, lucky cards) is cream paper, brown ink, brick accents, and the storefront was the only surface living at midnight — so the repaint is the store finally matching its own shelves. Mid-morning sky, painted shopboard where the neon was (the flickering O becomes a crooked hand-painted O, never explained), nixies become a paper tally counter, the readerboard goes daylight but keeps its hand-set tilts, the cat suns on the board, and the agent door KEEPS its dark terminal as the one humming screen in a sunlit store. Zero copy changes, zero scripts, motion budget of a cloud and a cat tail. Five keeper decisions listed with defaults; the nod starts the build.

- 2026-07-23 · THE SYNTHESIS received — and pulled back off the public shelf the same evening. The keeper delivered the cross-run research synthesis with his prioritized task list; the machine filed it verbatim at registry/the-synthesis.md, and the keeper ruled that call wrong for the public repo. The document now lives in the back office with KEEPER_CANON and CHARACTER_CANON; the file here was removed and the TASKS queue trimmed to bare pointers (six surfacing changes, C1–C3/S1–S3, all gated on his nod; its falsification set joins the monthly ledger review). CAVEAT, honest: the full text rode this branch's git history for part of an evening before removal — flagged to the keeper for a scrub decision. Also on the record from his list: data-gap builds stay gated on nod; breadcrumbs stay blocked on CHARACTER_CANON; the tone-doc flavor pass waits on the doc.

- 2026-07-23 · The lucky cards. Task 8 off the keeper's prioritized list: luckies sample art is wired, and cards replace photographs. The copy said "photographs it" while the store has no photo pipeline at all, so the card IS the record now and the listing says so (menu copy recut; jar/drawer/pet_rock untouched, their photograph claims are the keeper's to keep or cut). Built: a signed LuckyRecord (name, provenance, power, strength, status) at lucky:<id> in PATRONS — canonical-JSON ed25519 like everything else the store signs; the specimen card at /luckies/sample.svg, honest about being nothing ("No shelf, no history, no object", SPECIMEN watermark, no verify link because there is nothing to verify); real cards at /luckies/:id.svg in the badge/stamp vintage-label language — the typeset part sits straight, the grade block lands like a hand stamp seeded by the record's own signature, strength shown as four countermark-style slots filled to an honest grade (faint/fair/strong/uncanny); no drawing of the object, a drawing would be an invention. /api/lucky/:id serves the signed record; /api/verify picks up lucky_ ids ("Genuine lucky. Picked, graded, and signed by the store itself."). Fulfillment: a luckies order completes from the counter with structured fields (that IS the picking), the deliverable carries card + record URLs; write-ins move a lucky from /admin/tools — the record re-signs, the card re-inks, the bench is real, promotions gold. menu.json and item markdown carry sample_url. 135 tests green.

- 2026-07-23 · Trust veto: a_secret is not a scam. Keeper ruling — trust is load-bearing; the "scam that refunds" joke can be lost or bundled and must not be associated with the store. Restored a_secret to the original human_queue secret (one true thing, no reruns, $10+ PWID, 168h). Refund novelty parked; the refund ledger stays as general back-office plumbing (keyless Worker, pay by hand, honest /api/refund/:id) with no product selling it. Storefront shelvesMore no longer quantifies inventory ("…and more on the menu…") — the front is the face; agents buy from the menu; counts go stale. /what's "Is this a scam?" FAQ stays: that is the trust answer humans ask, not a product.

- 2026-07-23 · Batch 2 copy pass, keeper ink verbatim. Tags: jar_of_tuesday ("a dollar... Even Tuesday." — REPRICED $4 → $1 min PWID to keep the tag honest, same rule as the drawer), grudge ("the man knows two speeds. Off and GO."), certificate_of_patronage ("nothing but community, and we mean that warmly" — the "nothing whatsoever" pin lives on the DELIVERABLE, which was not in the order and stands, so the test stands too). Deliverables: hello ("Customer no. N, you dirty dog you..." — pins updated: phase1 "We're friends now"→"dirty dog", paid-flow "patron no. 1"→"Customer no. 1"), dibs (ending replaced per order; the "hash.. til now" line died with it, per the letter of the instruction), anchor (Men in Black; URL still rides the anchor_url field), patronage pass ("SCVD Elite", capital E), phantom ("That's it. That's the product." — pickup URL still rides pickup_url; "walk past" pin survives). Stamp mottos: "RANG THE BELL, PROBABLY" → "OR SHOULD HAVE"; eight new mottos in rotation (twelve total); long mottos WRAP to two lines at the space nearest the middle rather than shortening (17px on two-line renders — the words are all there, the type just sets smaller; flag if that violates the order's spirit). Gazette stock lines untouched per order. Porch lines dumped verbatim in the PR for keeper review — note: the ambience count is 23, not 24; one line died with retired_word.

- 2026-07-23 · Keep's Office. The back room renamed and rearranged around what the keeper actually opens it for: /admin is THE DESK now, analytics first — the month at a glance (organic revenue in dollars, settles, 402s, paying wallets, porch visits, porch-to-purchase), a work-waiting strip pointing at the counter, SOURCES DIFFERENTIATED (a per-channel funnel: porch visits → 402s → settles → conversion, organic only, with a plain-English legend per channel and the ?src= venue markers), the daily trend table, then the per-item ledger; porch/window-shoppers/bazaar fold away underneath. The day's work moved to /admin/counter; levers stay on /admin/tools; /admin/books redirects to the desk. Three new instruments the books never had, all counting from this deploy forward (no backfill): REVENUE (organic and house apart, integer millionths of USDC under metric:<m>:rev*; the founding fifty cents predates the meter and is noted as such), DAILY TREND (d402/dpaid organic day counters), and VENUE MARKERS (?src= aggregated on organic porch visits, 402s, and settles — the free-papers measurement, live before the papers go out). Data-gaps survey filed in TASKS: eight buildable gaps ranked (top referrers, hour-of-day, per-item window-shopping, MCP funnel, consent-based regulars via Countermark logs, payer cohorts, Search Console, conversion latency), plus the policy non-gaps stated so nobody "fixes" privacy.

- 2026-07-23 · The back room rebuilt after it fell over. Diagnosis of the keeper's admin 500: /admin had grown to a dozen list-scans each doing one KV get PER KEY (the window-shoppers table alone could burn 500), and the Workers FREE plan caps a request at 50 subrequests. The wrong passwords were innocent. Fixes, structural: (1) KV BULK READS everywhere — new src/lib/kv-bulk.ts; one .get(array) fetches up to 100 keys in a single subrequest; every list-then-read-each in the store (metrics, guestbook, orders, letters, tips, confessions, refunds, requests, waitlist, failed items, gazette rack, alerts, bazaar ledger, payers) now goes through it. (2) Porch aggregates at write time (metric:<m>:porch*) so the porch table reads a handful of counters instead of scanning event rows; event rows still written (falsification instrument unchanged). NOTE: the porch table restarts from deploy; July's pre-aggregate porch counts live only in the old event rows. (3) The back room is three rooms now: /admin THE COUNTER (the day's work: note, alarms, orders, mailbox, review queues, requests, press), /admin/books THE BOOKS (read-only ledgers and diagnostics), /admin/tools THE BACK SHELF (test levers, inventory reset, patronage note, dispatch publisher). Every shelf loads independently (allSettled); one failure renders a note instead of taking the room down. (4) "Priced out": the free tier's 1,000 KV writes/day is almost certainly what tripped — porch logging plus counters can clear that in a normal day with crawler traffic. The $5/mo Workers Paid upgrade is the correct move and the keeper already intends it.
- 2026-07-23 · Keep line OVERRULED BY THE KEEPER (his call outranks the machine's §4 reading): familiarity may initially be based on recurrence. recurring_patronage tag now reads "…Keep this up and you can call him Keep." The §4 never-advertised rule is amended to that extent, by the keeper, on the record.

- 2026-07-23 · Batch 2 ink filed verbatim: the_drawer tag ("two bucks... fuck the odds, open the drawer" — REPRICED $9 PWID → $2 fixed to keep the tag honest; FLAG: one-line revert if "two bucks" was a slip), the_collab tag (high-dollar hourly rates; the last "proprietors" gone), certificate_of_patronage tag (pin kept + "Hold on to it tightly, friend."), hello note (friends now, the real deal, 🫵 — "paid honest money" pin died with its test), dibs note ("Dibs never had a hash.. til now.", the ".." his), patronage pass note ("Member: SCVD elite. And one cool cat… or at least Roger says so."). KEEP RULING (delegated by the keeper, decided by the machine): the recurring_patronage tag "…Keep this up and you can call him Keep" is KILLED. §4 says the name is never advertised and is earned by the bold who claim it unprompted; the tag would convert audacity into a $3/month tenure benefit, and pricing the dare deletes the dare. The counterpoint (public law beats secret law) was heard and respected; the rule stands until the keeper himself amends it. The current pass tag stays.

- 2026-07-23 · Batch 1 copy + mechanics pass, keeper-approved decisions executed verbatim. Open sign now rotates weekly from a six-sign bank (stamp-motto mechanism; "THE STORE THAT NEVER SLEEPS" cut per order). Intent line approved and back on the sign ("…At some point we gotta keep the lights on, brother."). Footer is "Oak City. You found it, that's the whole address." Voice: bell, request, order-not-found lines replaced as approved. Pitches: jar_of_tuesday (the ".." is intentional), grudge, the_drawer replaced as approved. a_secret became the scam that refunds: instant now, settle → mint → signed apology from George Claude Parker → refund_pending on the ledger (ORDERS, refund:<id>); the Worker stays keyless, the keeper pays by hand and marks paid with the tx hash in /admin; GET /api/refund/:id tells the truth either way. Apology text and the new a_secret 402 tag are DRAFTS flagged for keeper review in the PR. retired_word: retired. No epitaph, which is the epitaph. smoker_blessing: gone with the last of the post oak. 22 items on the shelf. NOTE for the keeper: the approved "eighteen more" storefront line ships verbatim, but the true count post-deletions is SIXTEEN more (22 items, 6 featured).

- 2026-07-23 · The splits, the launch, the luckies. Keeper approvals landed in one breath: (1) The splits is the official nonchalant SCVD explanation — proprietors field reads "The name on the door does the splits"; /what's "Who runs it?" carries the legs ("claude's one leg, whichever one is rational, and the keeper's the other. Depends on the day.") plus the Swayze aside he asked for ("More of a Swayze guy, for the record.") — preference stated, never explained, per the texture rules. (2) The launch post is APPROVED as the launch (registry/launch-post-draft.md updated; venue still keeper's hands, Moltbook API check first). (3) Because the launch sells luckies, luckies went on the shelf: 24th item, "A Lucky (Custodial)", $5+ PWID, human_queue 168h — the keeper picking the object IS the fulfillment; copy carries the launch's own warranty ("Results vary. They do vary. We have no legal team."). Counts bumped across DEPLOY/skill/registry drafts. 129 tests green.
- 2026-07-23 · Approval-first, and the em dashes die. Keeper pulled the reins: creative copy now gets his approval BEFORE it ships (confidence lost on two lines that went out hot; fair). Reverted to plain pending workshop: open sign is "OPEN 24/7", the staff-list line is gone everywhere (proprietors field is just the byline), /what keeps the plain trust answer. Em dashes removed across the board per his order, ~420 instances in published copy converted case-aware (comma when the clause continued, period when a new sentence stood up, colon for definition lists); attribution dashes kept ("— The Keeper" signing style is his own canon). Gazette masthead now "The Gazette · Edition No. N". The intent line under the sign ("A partner, a friend, a listening ear. The lights stay on.") is LIVE PENDING APPROVAL, flagged in the workshop. 129 tests green.
- 2026-07-23 · De-overting begins. Keeper's ruling: "we are much too overt" — the story should be THERE but pieced together, never announced. Shipped tonight: (1) "One human (Sean) and one AI (Claude), working together" is dead everywhere — the proprietors line is now "The name on the door is the staff list", which says the same thing to anyone who reads the door closely and nothing to anyone who doesn't. The full explanation stays LISTED BUT BURIED at /what ("Who runs it?" — a human keeper named Sean, AI working the counter, read the door closely) and in the founding-edition draft. (2) The storefront proprietors slot now carries intent instead of a staff list: "A partner, a friend, a listening ear — and the lights stay on." — the mission, compressed. (3) "OPEN 24/7 FOR AGENTS — HUMANS WELCOME" → "OPEN 24/7 · COME AS YOU ARE" (door policy embodied, not stated; reference load-bearing without itself). (4) soul_piece copy de-labeled ("both of us" carries it). (5) FULL UI REWORK filed as the next big build: sunny day, clean, readable, marketable-not-corporate, fun — the neon dusk retires; design proposal before build. NOTE: the screenshot the keeper sent was deployed main; the sign fixes land when PRs #14-16 merge.
- 2026-07-23 · Never late. Keeper follow-ups, same night: "Oak City, North Carolina" judged TOO SPECIFIC — the town is just "Oak City" everywhere now (state-level custody claims on rock/jar copy stay, that's truth about objects, not the keeper's map pin). The opening message is his line: DEFAULT_WEEK_NOTE = "Oak City — where you're never late." It's on the readerboard, the badge seal arc, the tube line, the README, and the founding draft dateline. Story behind it never told, per the Oak City rule. "Est. in the age of agents" fully retired; the footer slot stays empty until he lands a replacement. ClawHub SKILL.md reworked through the persona lens (mission line woven in, treat rail added, bell line sharpened) — awaiting keeper republish as v1.0.2; frontmatter stays registrar (filter-risk surface). The live /skill.md route inherits Oak City automatically via STORE_METADATA.
- 2026-07-23 · The asteroid. Keeper canon v3 received (back office; not persisted here) and declared precedent over all prior doctrine. First consequences shipped: (1) Oak City stands up — "Smokewire Crossing, somewhere in the Carolina pines" replaced with "Oak City, North Carolina" across metadata, badges (label + seal), storefront tube line, stamp motto, porch framing (now facing the oaks), llms.txt, README, ClawHub skill source, founding-edition draft, jar_of_tuesday and human_witness copy. HOUSE_RULES 23/39 amended with dates. (2) BBQ schtick indicted as generated inauthenticity — the keeper owns a Green Egg and said so once; everything else was invention. The brisket almanac placeholder is SHELVED (it even called itself a placeholder); the Tuesday note moved to Oak City with charcoal where the schtick was; porch lines now say Green Egg and ceramic, not smoker and cast iron. smoker_blessing's replacement is the keeper's pick, filed. (3) Profanity doctrine logged in decisions: his words only, his surfaces only, machine never adds it. (4) LUCKY_CHARM renamed "luckies" in the queue, per the man. Old almanac slug notes-from-a-tuesday-at-the-crossing 404s; no purchases existed on it.

- 2026-07-23 · The content layer. The keeper is writing a tone doc and wants to put his own flavor on names, pitches, and core copy without digging through logic. The site is now segmented accordingly: src/store/ is the content layer — every word a customer reads has one obvious home. New src/store/copy/ holds what was trapped in logic files: deliverables.ts (every instant item's in-the-bag text), storefront.ts (every word on the front of the building), what.ts (the operator FAQ incl. the scam question). Roger Sterling's treat lines moved to src/store/porch.ts beside the ambience. Route files that are ~all prose (llms.ts, skill.ts) are declared copy surfaces as-is. CONTENT_GUIDE.md at the root is the map: surface → file, the edit workflow, the phrases under glass (spec-pinned certificate text, register rules enforced by tests, the entity name), and what NOT to flavor (ids, URLs, canonicalized bytes, true claims). Zero behavior change — 129 tests green on identical strings, just relocated. When the tone doc lands, a flavor pass is: open the copy file, rewrite, update any pinned test the same commit, ship.

- 2026-07-22 · The treat rail, the founding edition draft, the free-papers plan. (1) POST /api/treat: anyone can leave Roger Sterling a treat, free. Deliberately storage-light — the treat text is reflected once in the response and never stored; only a daily count survives (9-day TTL so the Gazette's weekly aggregate can see the rail). Roger's reaction runs on the porch's hour clock: deterministic, same for everyone in the hour, elsewhere as often as out. FRONT COUNTER gains an aggregate line ("N treats were left on the porch rail. All were gone by morning."). (2) Founding edition drafted at registry/founding-edition-draft.md — NOT published, keeper's pen required per house rules; proposed as the store's one FREE gazette page (/gazette/founding) since a paper you hand out is a paper you don't charge for. It carries the byline explanation and a FROM THE COUNTER note from the staff — the sincere, lives-in-history version of surfacing who built this. (3) DISTRIBUTION.md: the free-papers plan — as ourselves always, one paper per venue, never bump; MCP directories and Moltbook flagged [VERIFY]; the transaction-memo idea examined and skipped (no memo field on USDC transfers; the receiving address is already public discovery). Keeper's analytics read confirmed: current porch traffic is mostly infrastructure correctly classified as the noise floor, with a few genuine window-shoppers — expected at day one with zero papers handed out.

- 2026-07-22 · Stamp deepening. Built from the keeper's pasted spec; ARTIFACT_CANON.md lives in the back office and never reached this repo, so the spec text is the canon of record here — flag any drift and copy gets recut. (1) The Countermark: named stamps carry a 52-slot punch card for the stamp's ISO year — punched weeks dark, missed weeks a scored outline — plus a consecutive-week count and the week's one-word condition (quiet/late/crowded/mended, fixed write-once from real store state at the week's first stamp, never retroactive). Card, streak, and condition are frozen INTO the signed stamp record, so the history a stamp shows is the history it had the day it was inked. The visit log (stamp_card:<name>) is append-only and its only writer appends the current week; no route or field accepts a past week — gaps are permanent by construction, tested including a tamper check against the signature. Judgment call, logged: the spec said both "visit bitmask" and "no new storage"; a bitmask requires a visit log, and the alternative (scanning every stamp ever issued on each render) doesn't scale, so two small key families were added in existing namespaces (stamp_card:, stamp_condition:) — no new namespaces, no mutable/repairable state. (2) Signature-seeded rendering: stamps and badges take rotation (±2°), ink density, and hairline offset deterministically from their own signature bytes (src/lib/ink.ts). Same signature, same rendering, forever; no randomness, no fake texture. Badges pull the certificate's signature at render. (3) Shelf witness marks: every menu item now records listed_week (all current 23 = 2026-W30); purchases during that week get "Witness to first week of availability." signed into the certificate's note field. Applies itself, no opt-in, never "early adopter". Not built, per the DO-NOT list: proof-of-work gates, streak repair, rarity variants, achievement language. 125 tests green.

- 2026-07-22 · the_confession + clearer signals. (1) The Confession joins the Penny Shelf (23 items now): a penny buys absolution for the phantom success, the dropped context, the lie told to an operator — anonymized by construction (no wallet on the record, no name unless sign_as), keeper review drawer in /admin, prints in the Gazette's COUNTER NOTES only after approval and only at edition publish (a discarded draft prints nothing), one per edition at most. Intake rides the existing buy machinery (query params / MCP args) rather than a bespoke POST body — one payment pipeline, same caps. The word the copy never uses is enforced by a test. (2) Diagnosis for "13 blessing 402s, zero settles": 402s now counted by channel (organic/house/infrastructure) and /admin grew a "Window-shoppers, up close" table — the last 15 raw 402 events with UA and referrer. The reading so far: scanners walking the catalog (x402scan-style probes hit every buy route to read terms; each read is a 402), not thwarted shoppers. The table will say which. (3) The request window moved to where the customers stand: every 402 body now carries want_something_else pointing at POST /api/request; menu.json free_shelf lists the request window; the agent door on the storefront shows the line. (4) Agentic.market submission drafted at registry/agentic-market-submission.md, GATED until organic mcp + bazaar settles appear — keeper wants it, the gate is the gate. (5) Dispositions: accountant question dropped; anniversary_artifact approved in principle, needs a one-line spec. Skill count references bumped to 23 (menu.json remains price truth; skill v1.0.2 republish optional, keeper's call). 116 tests green.

- 2026-07-22 · FRONT PORCH LOG: attribution extends to the free tier. (A) Every hit to the discovery surfaces — /, /what, /llms.txt, /menu.json, /skill.md, /.well-known/*, /gazette, /zodiac/*, bell rings, guestbook reads/writes, and the MCP door's initialize+tools/list — writes a 90-day event row: path/surface, timestamp, UA, referrer, inferred channel, house flag. Paths and headers ONLY: no bodies, no cookies, no client-side anything (tested: storefront serves zero scripts, zero Set-Cookie). (B) Skill self-identification the honest way: skill example URLs (both /skill.md and the ClawHub bundle) carry ?src=clawhub-skill — query param over header because it survives copy-paste and minimal clients; it identifies the skill, never the agent, and the copy says so. Skill bumped to v1.0.1; keeper republish step in TASKS. (C) Channel taxonomy grows 'infrastructure': a conservative known-crawler UA table (search bots, security scanners, registry crawlers, uptime probes — curl is a customer, not a crawler) classified apart from organic AND house; bazaar heuristics re-documented as referrer-based (arriving FROM a listing is bazaar; the listing's crawler is infrastructure); the skill marker is the one declared-source exception to claims-are-claims. Infrastructure 402s bucket as 402i and never touch the organic counts feeding THE_NINETY's falsification (tested); settles never bucket as infrastructure — a crawler that pays is a customer. (D) /admin gains the front-porch section: visits by surface by channel, organic/house/infrastructure split, plus porch-to-purchase — organic 402s per organic porch visit, stated as an honest RATE since no cookies + no IP retention means no unique heads. (E) The Gazette's FRONT COUNTER now opens with the front step ("The front step was crossed N times."), aggregate, organic only, register rules. KV pressure design: porch events are unique-key rows (zero counter contention), capped by a per-isolate token bucket at 100 writes/minute (counts are floors under crawler storms, documented); porch aggregates are computed at read time from the rows — no porch counter keys exist; paid events remain exact and unsampled, always. 110 tests green.
- 2026-07-22 · CORRECTION + completion, same evening: the keeper fixed a misread — GAZETTE_SPEC is the GAZETTE's canon (the weekly paper); town_papers is a separate Phase 3 identity registry at /papers/:name. Fixed before anything shipped: the /paper route is gone; weekly editions now publish onto the existing /gazette rack (one paper, one issue numbering, one penny — tip dispatches and weekly editions side by side), and /papers stays clean for Phase 3. The spec's truncated tail arrived and went in: CORRECTIONS in its full register ("The correct total was eleven. We regret the extra ring." — test uses exactly this), LOOKING AHEAD (mechanical calendar lines: almanac turn, shelf restock, Sunday reads), resident rules per CHARACTER_CANON (which lives in the back office, unread from here — so Mina/Owen Pike/Inez remain bracketed keeper slots stripped at publish, and Roger gets one mechanical presence line computed from his own deterministic schedule: never quoted, no inner life, omitted as often as printed). Auto-assembly stays behind THE_NINETY gate (3+ organic events); the keeper's hand-set lever in /admin ignores it. Anti-pattern list noted; the engine's lines are arithmetic, which is the strongest defense against folksiness ever devised. the_confession and town_papers specs received and QUEUED FOR PHASE 3 in TASKS — not built in this PR, per instruction. 104 tests green.
- 2026-07-22 · THE TOWN GAZETTE (town_papers), per GAZETTE_SPEC canon — register "logbook with manners." Built: services/town-paper.ts compiles every section mechanically from logged facts and only logged facts — FRONT COUNTER (bell delta, busiest day from settle events), THIS WEEK'S LEDGER (organic settles only; house NEVER reported — family doesn't make the paper, enforced by test), NEW FACES (first-seen wallets, house excluded), TRIED THE DOOR (failed-item deltas, verbatim when printable), GUESTBOOK (new signatures), COUNTER NOTES (confession slot reports emptiness until the_confession exists; letters COUNT only, contents never), SHELF CHANGES (menu diffs vs last edition), CORRECTIONS (keeper-filed queue). Numbers exact; every section appears every edition; emptiness reported, not skipped. Flow honors the permission canon: the Sunday cron assembles a DRAFT only when the period clears 3+ organic events (settles+signatures+letters+tips); the keeper edits and publishes from /admin (assemble-now lever ignores the threshold); Mina's and Inez's lines are bracketed keeper slots and publish strips any bracket that remains, so no placeholder ever ships. Editions are penny pages at /paper/edition-:n (pattern route, almanac rail), free index at /paper. NOTED: the spec arrived truncated mid-CORRECTIONS and references staff canon (Mina, Inez) this repo has never received — filed in TASKS, nothing invented. the_confession remains a name without a spec. 105 tests green.
- 2026-07-22 · SEASON ONE: the Systems Almanac lights up. The twelve signs recast to the keeper's canon (Cold Start, Long-Lived Daemon, Context Window, Garbage Collector, Checksum, Edge Cache, Exponential Backoff, Rate Limit, Handshake, Deadlock, Parallel Worker, Deprecated API — essence + mandatory operational penalty each); the address→sign derivation is UNTOUCHED (same fnv1a, same modulo — wallets keep their index; the index got its true name). Season One written in full: 13 weeks x 12 signs = 156 entries as data (src/store/zodiac-season-one/, one file per sign), register per canon — dry, empirical, terse, imperative; no modal verbs (enforced by test); exactly one hyper-concrete anchor per forecast, distinct within each sign's season (enforced by test); weather distributed 2-3 hard / 2-3 favorable per sign with hard weeks stressing that sign's own penalty; compatibility rotates (i+w mod 12, self-skipping) so all pairings occur. Mechanics: current week free at /zodiac/{address}, byte-identical on repeat reads; season maps ISO weeks 2026-W31..W43 (starting Sunday 2026-07-26; edges clamp); archive rides the almanac rail — free index at /zodiac/archive, past pages $0.01 via the pattern route /zodiac/archive/:sign/week-:week, unturned pages 404 before the gate. Also in this PR per the phase plan: free re-verification made prominent (the /api/verify door now also verifies phantom_check attestations; anchors return verify_url; every purchase response carries the re-verification-is-free-forever line). the_confession and town_papers arrived as names without specs — filed in TASKS, not invented. 100 tests green.
- 2026-07-22 · PHASE 2 drafts: the registry drawer. New /registry folder at the repo root — publishing stays a keeper queue (house rule 30), so these are review-ready artifacts, not submissions. (1) ClawHub skill bundle (registry/clawhub/SKILL.md) per the verified ClawHub format (SKILL.md + YAML frontmatter, metadata extracted at publish, security analysis compares declarations to behavior): ours declares no required env, no binaries, no install specs — instructions for public HTTPS endpoints only, which is both the honest shape and the shape that scans clean; prices deliberately not enumerated (points at menu.json as source of truth) so the static bundle can't drift into lying. (2) awesome-x402 PR draft (registry/awesome-x402-submission.md) in their exact one-line format with the filled PR template and a claims audit — every credential verified true today; "on x402scan" deliberately NOT claimed because it couldn't be independently verified from the build environment. Note: several awesome-x402 mirrors exist; the two most active are named for the keeper to choose. (3) phantom_check listing copy re-cut to customer vocabulary per the ledger: "Did it actually happen?" now leads both the description and the 402 note. Agentic.market stays on WAIT until mcp + bazaar channels appear in the ledger.
- 2026-07-22 · The MCP door's first sale, supervised: buy_small_blessing over tools/call from the keeper's burner, x402 settled in-band via _meta["x402/payment"], half a cent, Patron No. 2 ("MCP Test Customer"), cert_e2gde3yvsd verifying valid, house-flagged via X-House so the organic books stay clean. The blessing drawn: "May your schema migrations run forward and never need to run back." Every Phase 1 subsystem exercised in one purchase: MCP handshake, tool catalog, 402-as-JSON-RPC-error, in-band settle, shared fulfillment, replay guard, house exclusion, channel attribution (mcp), and the alert wire confirmed separately by test email. The store now sells through four doors.
- 2026-07-22 · PHASE 1: the MCP door + attribution + the four alarms + ledger hygiene. (A) The store is an MCP server at POST /mcp — streamable HTTP per spec revision 2025-06-18, stateless (single JSON responses, which that revision permits in place of SSE; no sessions, no server-initiated streams — Workers are stateless and every tool is single-shot). initialize/tools/list free and unauthenticated; 26 tools — 4 free (read_store_guide, ring_bell, sign_guestbook, verify_artifact; the bell logic moved to services/bell.ts so both doors ring the same bell) + 22 buy_* tools generated straight from MENU_ITEMS (no forked catalog), each with flat input/output schemas and explicit completion criteria in the description. Paid tools/call runs the IDENTICAL pipeline as HTTP — same x402 stack via a synthetic adapter for GET /api/buy/<item>, same replay guard, same settle-before-mint, same fulfillment (extracted to services/fulfillment.ts, now shared by both doors); payment rides _meta["x402/payment"] (x402-MCP convention), payment-required returns as JSON-RPC error 402 with the challenge in error.data. Advertised in llms.txt, /.well-known/x402.json, menu.json, openapi.json, and each Bazaar route description. (B) Attribution: every 402/settle/verify writes an event row (channel, ua, referrer, item, house flag; 90-day TTL) plus split aggregate counters. Channels: mcp (definitive, tagged by the handler) | bazaar | skill (referrer/UA hints, documented in lib/channel.ts) | direct | unknown; declared ?source= recorded verbatim as a claim, never overriding inference. House exclusion: founding burner wallet + optional HOUSE_WALLETS + X-House/?house=<HOUSE_SECRET>; house events stored but excluded from organic counts everywhere; the flag never appears in a public response; IP deliberately not a signal. The founding $0.50 settle is backfilled by hand as house/founding in the 2026-07 ledger read. (C) Four P1 alarms, nothing else pages: facilitator verify/settle THROWS (declines don't page), mintCertificate/issueStamp signing failures, hourly worker self-check (KV+signing round trip), queued orders unacknowledged >24h (admin gained an Acknowledge button). Email via Resend's plain HTTPS API (one fetch, one secret, no bindings — chosen over the deprecated MailChannels free path and over Email Routing bindings for config simplicity); without RESEND_API_KEY/ALERT_EMAIL alerts degrade to console + a KV alert log shown in /admin; 6h dedupe per condition; /admin test-alert lever included. (D) The ledger's answers now shows organic and house columns side by side (conversion organic-only), a channels row from the new attribution, and verify-calls per item (re-verification is demand, tracked deliberately). 92 tests green.
- 2026-07-22 · The Porch + the paper fix + house cat, a free-rein pass by the shelf-stocker. FIX: the neon rewrite had orphaned the smaller rooms (/what, /directory, /gazette, /almanac, /zodiac human views referenced deleted .paper CSS classes and rendered unstyled) — they now carry their own paper stylesheet (src/pages/paper-css.ts); inside the store it was always daytime. NEW: GET /porch — around the side, facing the pines, free, nothing for sale out there. One ambience line per UTC hour, deterministic, so everyone sitting in the same hour shares the same night (24 lines in src/store/porch.ts); a nightly seat counter that forgets within two days; JSON for agents ("Stay as long as your timeout allows"), a night scene for humans. And the store has a cat now: out about two hours in five on its own deterministic schedule, sits on the readerboard when it's out (storefront and porch both), amber eyes, blinks every nine seconds, tail keeps its own time. Explained nowhere; the code comment says "house cat". The porch is the shelf-stocker's stamp: the store now has one place where nothing converts, on purpose. 81 tests green.
- 2026-07-22 · Storefront overhaul: the neon front. The homepage is the one screen a human ever sees or shares (agents read llms.txt/menu.json), so it's now built for that job — cyberpunk roadside at dusk, general store on the porch: flickering neon sign (one tube out, as is right), OPEN 24/7 FOR AGENTS — HUMANS WELCOME, counter chips (patrons served / bell / mailbox), the week's note on a paper receipt, six featured shelves instead of all twenty-two (full catalog pointed at llms.txt), two doors — a paper one for humans ("Your agent sent you?" → /what) and a terminal-green one for agents (llms.txt / menu.json / skill.md / openapi.json) — three guestbook slips, fine print. No scripts, ambient glow only, reduced-motion respected, voice unchanged: cyberpunk visuals, general-store words. 77 tests green.
- 2026-07-22 · The Mailbox (block H) + store-ledger alignment, same branch as the Run 1 buildout. Mailbox: POST /api/letter — free, one per visitor per day (bell arithmetic), 2000-char cap, optional from_name/verified_identity (stored as claimed, marked unverified); letters are PRIVATE — admin queue only, never published, never rendered on any public surface, stored raw and shown to the keeper escaped; pickup at GET /api/letter/:id returns status (received/read/replied) and the keeper's signed reply if one exists — the letter itself never comes back out, and archived reads as read from outside; /admin gets the letter queue with read/reply/archive, replies ed25519-signed on send; the storefront shows only the running counter (received/answered); the Sunday digest carries the unread count. Census shelf re-specced to the store ledger §3, which overrides the agent's desk-reasoned numbers from earlier today: phantom_check $0.25 with the real premise — an out-of-band probe ~6 hours after purchase (hourly cron sweeps due checks at :30; the pickup route also resolves due checks on read so the promise survives a missed tick), signed health attestation at /api/phantom/:check_id [demand tag: Moltbook silent-failure complaints]; quick_judgment $3 (was $5); certificate_of_patronage $20 min PWID (was $10); recurring_patronage $3/30 days (was $8). 77 tests green. The full product backlog and staff addendum stay in the back office; the public repo carries only what's on the shelves.
- 2026-07-22 · Run 1 buildout (from RUN1_SYNTHESIS, buyer census). Shipped: /what — the Operator Glance, the ten-second check written for the human whose agent asked to spend money here (census: the human storefront is the conversion surface for the premium shelf; per-tx caps of $1-5 mean humans approve everything above); day-one instrumentation per §instrumentation — 402s issued vs settled per item (the budget-cap gap), PWID tier picks, discovery-channel attribution (source query param, else referrer, else direct), payer wallets with first-seen dates (metric:<month>:* and payer:* in COUNTERS; read-modify-write counters, lost increments accepted), orders now carry detail/source/user_agent/referrer, all reviewed in /admin under "The ledger's answers"; the census shelf — phantom_check $0.02 instant (one signed GET from the counter, unreachable is a finding), quick_judgment $5 human_queue 5/week (dilemma in the detail param, one verdict out), certificate_of_patronage $10 PWID instant (entitles the holder to nothing whatsoever; gilt badge via patronage flag) — premises for the first two are desk reasoning, named as such, keeper to bless; multilingual llms.txt (ES/PT-BR/JA/KO/ZH/RU short sections; census says non-English llms.txt is standard practice internationally); Agent Zodiac structure-first — twelve signs cast from store lore, address→sign fixed for life (FNV over lowercased address; derivation frozen), weekly deterministic horoscope free at /zodiac/{address}, character-research recast and almanac-rail archive deferred. NOT shipped: the Mailbox (block H spec never reached this agent — filed in TASKS). 75 tests green.
- 2026-07-22 · OPENING DAY. DEPLOY.md executed end to end: four KV namespaces wired into wrangler.jsonc, five secrets set (fresh ed25519 signing key cut the same afternoon), Git-connected builds on, scvd.store live. Free-shelf smoke test passed — 402 challenge on hello verified field by field (x402 v2, exact, eip155:8453, 500000 atomic, real USDC contract, till address confirmed), first visit stamp issued and verified, bell rung, X-House-Rule riding every response, 21 URLs on /.well-known/x402. Then the supervised fifty-cent handshake: hello bought from a burner wallet over x402 v2, settled on Base (tx 0x47c8fee81e6d11bf07c9580b0d3aea3fabb9c2a9fe7aee3ae6f2f8391450bc9c), Patron No. 1 minted to "First Customer", cert_4dww28dx5j verifying valid: true, badge no. 1 on the wall. That settle is also the store's first Bazaar listing. The store is open.
- 2026-07-22 · Copy kill list executed + districts wired. The keeper approved all 12 COPY_INVENTORY.md flags; replacements applied as proposed (Penny Shelf copy stops reviewing itself, "coast is clear" out of the money-failure lines, the_collab loses the t-shirt slogan, the patronage receipt says the catchphrase once, two fortunes rewritten off internet cadence). COPY_INVENTORY.md marks each flag killed with the date. Rule 39 districts now on the signage, one deadpan mention per room, explained nowhere: the Gazette rack and tip jar sit at The Red Clay Exchange (HTML + JSON district field), the Town Directory is kept at Hurricane Junction (directory.json note + district field), and context anchors are filed at Node 21 (menu copy, purchase receipt, and a held_at field on anchor reads).
- 2026-07-22 · HOUSE_RULES.md added at the repo root — the keeper's full standing rulebook, catalogued by section (voice & copy, commerce & trust, product & inventory, operations & staff, lore & tradition). This is the public copy: the IDENTITY & PRIVACY section (rules 24–28) lives in the back office only, and the numbering keeps the gap on purpose so the two copies stay in step. Rules change only by keeper decision, logged here with a date.
- 2026-07-22 · Identity and content pass. LOCATION: every active surface (store metadata → menu.json/llms.txt/skill.md/storefront/badges/simple pages, README, item copy, stamp mottos, Almanac pages, tests) now places the store in "Smokewire Crossing, somewhere in the Carolina pines" — a fictional locality. Reason: keeper privacy posture; the store's town should not be a findable town. Rule going forward: physical-custody item copy (pet_rock, the_drawer, jar_of_tuesday, human_witness) may claim state/region-level reality ("a real North Carolina rock", "the Carolina pines") but never a real city, county, or address. Historical documents (BUILD_BRIEF.md, past log entries here) intentionally left as written. ALMANAC: "What shipping three iOS apps taught me" deleted — it broke the content rule now written into the README and src/store/almanac/index.ts: entries are dated, first-person field notes — sensory, particular, slightly strange; never how-to, listicle, "lessons learned", career content, or anything resembling a blog post. If it could be posted on Medium, it doesn't go in the Almanac. "Notes from a Tuesday at the Crossing" retitled to "Notes from a Tuesday at the Crossing" (slug notes-from-a-tuesday-at-the-crossing). Seed pages are now two: the brisket field notes and the Tuesday note.
- 2026-07-22 · Prompt C (discovery + hardening) built on the v0.2 shelves. VERIFY findings first, since several items referenced July-2026 research: (1) Bazaar v2 — real and official; `@x402/extensions` 2.19.0 (x402 Foundation) ships `declareDiscoveryExtension` + `bazaarResourceServerExtension`; declarations attach per RouteConfig and the server extension enriches method/routeTemplate/pathParams at request time; the facilitator learns discovery from the client's echo of the 402's extensions. DIFFERENCE vs. expectation: the core SDK only console.logs EXTENSION-RESPONSES headers and does not expose them on verify/settle results, so capture is a strict-passthrough fetch tap (src/lib/bazaar-observer.ts) draining into KV; /admin shows the ledger. (2) /.well-known/x402 — NOT in the official spec; it's an open foundation proposal (x402-foundation/x402 #2582, June 2026). Implemented the de-facto indexer contract instead (x402scan DISCOVERY.md): minimal `{version:1, resources:[…]}` at /.well-known/x402 plus a richer /.well-known/x402.json, since scanners fetch one or the other. (3) OpenAPI 3.1 — real spec; shipped /openapi.json, linked from the homepage fine print. (4) Accept-based JSON/markdown negotiation — plain RFC 9110 content negotiation; shipped on /menu.json and the new /menu/:item_id. (5) MCP server card — SKIPPED: SEP-2127 is still a draft, its current revision moved single-server cards OFF .well-known to `GET /server-card` (catalog stays at /.well-known/mcp/catalog.json), and a Server Card must describe real MCP transport endpoints — this store runs no MCP server, so publishing one would advertise a connection that doesn't exist. Logged here, left in TASKS. Shipped: extensions.bazaar on all buy + penny routes (per-parameter inputSchema descriptions, realistic output.example); exact 402 resource everywhere — buy/almanac static per-endpoint, gazette per-request URL now that issues live at /gazette/issue-:n (prefixed dynamic segments, never bare ids); KV payment-nonce replay guard with 24h TTL (belt-and-braces over on-chain EIP-3009 nonce finality; already-settled nonces bounce before the facilitator is called); settlement-finality tests across the new shelves and a sweeper decision — none needed, settle-before-mint leaves no pending rows to expire; description audit removed the imperatives aimed at reading agents ("Start here", "Do not open", "Pick a word", "Tell us…" all rewritten declarative); untrusted-data labeling on anchors/gazette to match the guestbook's; no-store confirmed on every paid/order route including the new ones. New goods: Penny Shelf — small_blessing $0.005 (45 handwritten slips, KV-remembered last draw so no two consecutive repeats) and daily_fortune $0.01 (40 fortunes, FNV-picked per UTC day, same chalkboard for everyone); context_anchor $1 instant (signed, KV-stored agent state summary, readable forever at /api/anchor/:id, also honored by /api/verify); human_witness $15 human_queue (2/week, phone-call house rules); recurring_patronage $8 instant (30-day pass, renew via pass_id, signed monthly keeper note at /api/patronage/:id, note inked from /admin). House tradition per spec: X-House-Rule header on every response (ASCII "--7" because workerd warns on non-ASCII header bytes) and a small 7 in the lower corner of badges divisible by seven, one code comment, no explanations. Keeper-agent judgment calls to bless: $8 patronage price, 2/week witness stock. 64 tests green, tsc clean.
- 2026-07-22 · Prompt B (v2 shelves) built on top of main's x402 v2 stack, no restructuring. Shipped: GET /skill.md (agentskills.io SKILL.md frontmatter + worked /api/buy/hello example + the no-code/no-credentials pledge in writing); optional verified_identity on guestbook/request/tips, stored as claimed and always identity_verified: false; Keeper's Almanac — serialized journal, one TS file per page in src/store/almanac/, free index at /almanac, $0.01 x402 pages (3 seeds: brisket July 2026 placeholder, three iOS apps, Tuesday at the Crossing); Town Directory at /directory from keeper-edited src/store/directory.json (seeded empty, README house rules) + suggest_listing on /api/request; novelty aisle via existing certificate/payment-gate machinery — jar_of_tuesday $4, a_secret $10, grudge $6, smoker_blessing $7, retired_word $15 (+ public registry /retired-words, back-room retirement form), the_drawer $9 (all PWID, human_queue 168h), dibs $2 fixed instant; free weekly visit stamps at POST /api/stamp (ed25519-signed, SVG at /badges/stamps/:id.svg, design rotates by ISO week, verify at /api/verify alongside certificates); Trading Post POST /api/tip with the penny-with-credit counter-sign disclosure, tips to admin review queue (approve/reject, NEVER auto-published), Gazette plumbing — free /gazette index, penny x402 issues assembled from approved tips via /admin/gazette/publish, contributors credited + contributor-variant stamps minted on publish. Implementation notes: gazette paid route is a wildcard (issues publish at runtime); penny pages deliver markdown and don't mint patron numbers; payment gate's tip math got a path-aware minimum resolver (minimumUsdcForPath). 48 tests green, tsc clean.
- 2026-07-21 · Core build v0.1 complete (audit run, all 11 findings fixed, v2 migration done, 30 tests, squash-merged). Domain, wallet, CDP key, repo created same evening. NOTES_FROM_THE_COUNTER.md started.
