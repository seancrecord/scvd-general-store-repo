import type { Correction } from "./types";

export const correction: Correction = {
  date: "2026-09-20",
  what_was_wrong: "Host-history pages called their unsigned JSON summary the signed rows. The summary links signed corpus originals, but its tier, gap counts and combined timeline are derived context. A valid signature on one retained snapshot does not authenticate that whole view or the other weeks it cites.",
  how_long: "The wording is confirmed in the September 20 source read of the host page and its Markdown twin. The separate September 19 buyer cohort retained two handoffs that attributed unsigned history to signed evidence; this establishes the interpretation failure, not a causal link to one sentence.",
  found_by: "Review of the retained buyer handoffs followed by a read of the host-history route and the focused verification guide.",
  what_changed: "Host JSON, HTML and Markdown now explicitly label the summary unsigned and point to the originals for verification; OpenAPI describes that scope. The buyer guide supplies the existing exact-subject command and explains dates, omitted rows and unsigned context. Tests execute the guide's command against signed and tampered fixtures and check every history view while preserving the original snapshot bytes. Native usability requires a separate qualified cohort.",
};
