import type { Correction } from "./types";
export const correction: Correction = {
  date: "2026-09-12",
  what_was_wrong: "The shelf quoted the same receiving wallet two ways. The doors Worker, which answers every unpaid knock on /api/buy/*, had its Polygon, Arbitrum and World pay-to secrets typed in lowercase; the store Worker had the same wallet checksummed. Nothing showed until 2026-09-11, when launch_check and opening_day began answering from the store: from then on thirty doors quoted 0xdd35… on Polygon and World and two doors quoted 0xDD35…, and the two store-answered doors offered no Arbitrum rail at all, because ARBITRUM_PAY_TO was never set on the store. x402-list diffs the whole accepts[] on every probe: at 02:38 UTC on 2026-09-12 its change feed recorded a payTo rotation and an Arbitrum removal for this store, it re-captured the envelope at 04:30 UTC, and its EIP-712 signability check — which had read pass on the 2026-09-08 capture — has read unknown since. Unknown is excluded from its ?signable=true results. Every EVM entry on the wire carried extra.name and extra.version the whole time; the credit was lost to a shelf that answered one question two ways.",
  how_long: "The two spellings existed from the day the secrets were entered (Polygon 2026-08-20, Arbitrum and World 2026-09-06) and reached the wire from the 2026-09-11 handover until this correction deployed. The Arbitrum gap on the store Worker persists until the keeper sets ARBITRUM_PAY_TO there; the store's own /rails page and its two 402s name what it accepts, so the gap is visible rather than hidden.",
  found_by: "The keeper, reading x402-list's assessment on 2026-09-12: the EIP-712 row had gone from pass to unknown with no change to the domain fields. The store's own npm run doors:live read 31 of 33 doors as differing from discovery the same afternoon; x402-list's change feed supplied the timestamps.",
  what_changed: "Every EVM pay-to now passes through one function that checksums it (EIP-55) before it reaches a 402, the discovery accepts, or a bank walk, so two Workers with the same wallet typed in two cases quote one string. A spec hands the helpers every spelling a keeper could type and requires the wire to carry exactly one, on every EVM rail, in the terms and in the manifest. What code cannot fix is named for the keeper: ARBITRUM_PAY_TO must be entered on the store Worker as it is on the doors Worker, and the live parity instrument (npm run doors:live) is the check that both were.",
};
