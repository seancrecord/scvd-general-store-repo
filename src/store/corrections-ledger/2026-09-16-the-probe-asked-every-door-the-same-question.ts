import type { Correction } from "./types";

export const correction: Correction = {
  date: "2026-09-16",
  what_was_wrong:
    "Every probe this store points at a stranger's x402 door sent GET and only GET. A door that takes POST answers 405, and the battery scored that 405 as a FAILED status-402 check — the published defect no-402, \"listed, but serves no payment challenge\" — against doors serving a flawless challenge one verb over. On 2026-09-08, round 2026-W37, the weekly census walked https://agents.suedeai.ai/api/gateway/topup, discovered through a public x402 directory listing, and published not_ready on its passport page and into the corpus. On 2026-09-14 the automated outreach step read that row, drafted a note naming the failed check, and sent it to the operator's security contact. The endpoint declares the operation as POST in its own published OpenAPI, and re-probed from here on 2026-09-16 it answers GET with 405 (no Allow header) and POST with 402 carrying a valid x402Version 2 challenge: scheme exact, eip155:8453, 1000000 atomic USDC, a canonical Base USDC asset, and a bazaar extension whose info.input.method reads POST. The verdict was a fact about our instrument wearing a finding about his door. The defect vocabulary did not catch it because the vocabulary said the same thing: no-402 asserted a 402 \"to an unpaid GET\" and was falsified only by a 402 \"to an unauthenticated GET\", so the class as published AUTHORISED the wrong finding.",
  how_long:
    "The GET-only probe is as old as the probe. The count that matters is not that: the same failure mode — a GET-only probe false-negativing a POST-only but otherwise compliant door — was flagged internally on 2026-09-11 against a different host and not acted on. Three days later a note went out to an operator on the strength of it. One published round carries the named host (2026-W37, walked 2026-09-08); how many earlier rounds and how many other hosts carry it is NOT known yet and is not estimated here — every not_ready whose only failed check is status-402 is a candidate, and auditSentNotes is being run over the hosts a note already went to so the next entry can state a denominator instead of an impression. Rows sealed before today were scored under the battery as it stood and stand as history; nothing is re-signed. W37 stays in the chain as walked and the corrected instrument publishes fresh rows beside it on the next walk.",
  found_by:
    "The operator, Jason, who runs the endpoint. He rechecked it himself after our note, reported GET 405 and POST 402 with the challenge, pointed at his own OpenAPI declaring the operation as POST, and asked for the exact method, URL, headers, body, timestamp and discovery source our probe used — the question that made the answer unavoidable. Our own corpus row confirmed a bare unauthenticated GET with no retry. Outside eyes, on a defect our own house rule 52 has forbidden since 2026-08-25 and which our own 2026-09-14 note on scheme drift had already named in the exact words that fit here: \"a verdict on our reader wearing a finding about their door.\"",
  what_changed:
    "The verb is resolved instead of assumed, and the law lives in ONE file (lib/probe-method.ts) that all four door probes call. FOUR, because the premise that one shared function backed every probing surface was wrong and checking it was the load-bearing step: preflight.probeOnce backs the free preflight, service_audit, conformance_watch and good_buyer, but ward-round.probeHost — the census, outreach, passport refresh, the long walk and the bounty board — carried its own hard-coded GET, and standing-watch carried a third. probeHost is the one that published Jason's verdict, so a fix proven only through probeOnce would have changed nothing that reached him. The fourth was found while fixing the other three and was in none of the scoping that opened this work: the PAID launch check omitted the method field entirely, which defaults to GET, so a POST-only door was not merely reported as serving no challenge — it was reported as unpayable by an instrument that never asked it to sell anything. Its verb is now resolved on the unpaid approach stage and reused verbatim by the paid presentation and the replay, because a payment presented by a different method than the one the challenge answered is not the same request and the whole artifact is the claim that it is. Grepping for the hard-coded string would not have found it; the structural guard now walks all four files. The resolution order: a method the feed or challenge DECLARED (declaredMethod, read off the discovery row the census already holds, costing no extra request); else GET; and on a 405 or 501 ONLY, exactly one more request — the method named in Allow when the door sends one, POST otherwise. Allow is read and is never required, because the door that caught us sends none. The published ceiling of \"exactly one outbound request per call, ever\" is now two, stated in preflight.ts, in single_probe_note, in the OpenAPI and in a new probe_method block on every report naming the method used, every method attempted, and where the choice came from. When every method is refused the battery emits NO checks at all and a new verdict, method_unresolved, says so: not ready, not not_ready, not unreachable — nothing was observed, and the gap is ours. Structurally, runChecks returns method_unresolved beside an EMPTY check list precisely so the `failed.length === 0 ? \"ready\" : \"not_ready\"` idiom every caller writes cannot quietly render it as a clean bill, which is rule 52's flattering answer in one ternary; every call site branches on it first. ward-round gained carriesVerdict(), asked once instead of `verdict !== \"not_probed\"` spelled out in eight readers, because a second non-verdict would otherwise have joined every public ready DENOMINATOR while never joining a numerator. The outreach desk will not draft a note from a method_unresolved row at all — not a softer note, no note. The standing watch freezes the resolved verb on the record so a seven-day series is 168 looks at one question, since canonicalizeProbe's field order is frozen and a signed row has nowhere to say the method changed; its unresolved ticks are counted under probes_method_unresolved and stay out of the ready/not_ready tally and the conformance drift set. The paid audit throws before its surface, rail and blacklist reads rather than binding an empty battery into a certificate. discovery/host-probe.ts explicitly passes fallback:false — a 405 on a fixed /.well-known path is an answer about the document, and the default is ON so the next x402 door somebody adds inherits the fix rather than the defect. The defect vocabulary goes to v19 (v18 was taken the same day by the separately versioned MPP core draft-01 work, which landed on main while this was in flight): no-402 now asserts a 402 to a method the resource ACCEPTS, and a 405 or 501 to the probing instrument's method now falsifies a finding of the class outright, with a note to every consumer that an instrument probing with a single hard-coded method is making a claim v19 does not support. Enforcement extends test/bounded-read-honesty.spec.ts rather than starting a new file, exactly as the 2026-09-04 chain-reading correction did, because rule 52 already forbade this and only its enforcement was too narrow: the guard holds a POST-only door being found, a method refusal producing no check rather than a failing one, the naive ready idiom being caught, a document read NOT growing a fallback, and — structurally, per rule 46 — that none of the four door probes fixes its own verb again.",
};
