import type { A2AReading } from "@/lib/a2a-instrument";
export const A2A_PRICE_USDC = 49;
export const A2A_PROPOSITION = "Check an A2A agent, reproduce its failures, and hand a developer the repairs and regression tests.";
export const A2A_MONEY = `The $${A2A_PRICE_USDC} pilot buys one signed repair kit, one recheck within 30 days, and seven days of daily card checks, with no automatic renewal.`;
export const A2A_FREE = "The card check, test runner and repair guidance are free; the paid kit adds hosted runtime tests and signed records.";
export const A2A_ACTIONS = "Bounded outbound GET requests read the public card and operator authorization file. With that explicit permission, JSON-RPC POST requests exercise its safe-to-repeat fixture and malformed inputs at purchase and recheck. Daily watch passes read only the card; no credentials or payment go to the target.";
export const A2A_REPAIR_RECIPES: Record<string, { change: string; acceptance: string; implementation: string }> = {
  "card-http": { change: "Serve the card as JSON at the supplied URL, without a redirect or login page.", acceptance: "A GET returns successful HTTP with application/json and the complete card body.", implementation: "Hono: return c.json(card). Check proxy routing and static-file rules at the deployed URL." },
  "card-schema": { change: "Validate the complete card against the official schema for the version it declares. Derive skills and capabilities from the implemented methods.", acceptance: "The versioned AgentCard validator accepts the deployed document.", implementation: "Validate the built card in CI, including every skill entry, rather than asserting only the top-level names." },
  "endpoint-url": { change: "Advertise an HTTPS endpoint reachable on the public internet. This instrument requires a URL without credentials, query or fragment.", acceptance: "The exact URL passes the published probe policy.", implementation: "Use a stable route. Keep authentication out of the URL; authenticated runtime tests are outside this pilot." },
  "send-schema": { change: "Validate the returned Task or Message against the official schema. A Task needs its kind, id, contextId and status; a Message needs its own required fields.", acceptance: "The authorized fixture returns a schema-valid result and a JSON-RPC id matching the request.", implementation: "Hono/Workers: create a contextId for a new conversation, retain it with the task and return it on every task response. These are suggested changes, not a patch inspected against your source." },
  "task-get": { change: "Persist each task before returning it, and read tasks/get from that same storage. Preserve task and context IDs.", acceptance: "Create a task, immediately retrieve its ID, and validate the retrieved response. Test from a second instance too.", implementation: "Hono/Workers: a Durable Object per task can provide immediate consistency. Set an explicit retention policy and return -32001 only for an unknown or expired task." },
  "terminal-cancel": { change: "Load the task before deciding cancellation. Distinguish a terminal task from a missing ID.", acceptance: "Cancel the returned terminal task: -32002. Get an unknown ID: -32001.", implementation: "Return a JSON-RPC TaskNotCancelableError for a completed, failed, rejected or canceled task; do not turn every cancellation into task-not-found." },
  "parse-error": { change: "Catch JSON parsing failures at the protocol boundary and return the standard parse error.", acceptance: "An incomplete JSON body returns -32700 with id null and no HTTP 5xx.", implementation: "Keep protocol error handling inside the A2A route so malformed JSON does not reach a generic HTML error page." },
  "null-part": { change: "Validate the complete request before reading a part kind or dispatching application work.", acceptance: "A message with parts:[null] returns -32602 and does not dispatch the task.", implementation: "Use an official-schema validator compiled at build time; do not read part.kind before checking the value." },
  "trailing-part": { change: "Validate every message part before using the first supported part.", acceptance: "A valid first part followed by null returns -32602 and does not dispatch the task.", implementation: "Schema validation must cover the whole array. A find() over part.kind does not validate the entries it skips." },
  "unknown-task": { change: "Return the standard task-not-found error for IDs absent from task storage.", acceptance: "A random unknown ID returns -32001 in a valid JSON-RPC error envelope.", implementation: "Keep unknown-ID handling separate from storage failures; a failed storage read does not prove absence." },
};
export function repairRows(reading: A2AReading) {
  return reading.checks.filter(c => c.state === "fail").map(c => ({ check: c.id, evidence: c.evidence, spec: c.spec, ...(A2A_REPAIR_RECIPES[c.id] ?? { change: "No repair recipe is available for this check.", acceptance: c.detail, implementation: "Review the bounded exchange against the linked protocol requirement." }), status: "suggested_not_applied" }));
}
