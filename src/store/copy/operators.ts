/**
 * THE OPERATORS' ROOM, ITS THREE SENTENCES (house rule 60, 2026-09-04).
 * The room opened 2026-09-03 under the pre-rule list; it earns a row
 * today because it gained a door of its own (POST /api/declare-door)
 * and a door needs a feature. Typed once here; the feature register,
 * the page, its JSON twin and the guide import them, and
 * test/feature-surfaces.spec.ts holds them identical across all four.
 * No quotes inside a sentence: the page escapes them and the match
 * dies.
 */
export const OPERATORS_OPENED = "2026-09-03";

/** The proposition: the shelf from the seller's side, free first. */
export const OPERATORS_PROPOSITION =
  "You run an x402 door. This room is the shelf from your side, in the order a launch happens: free first at every step, and a signed, dated artifact only when you need one a stranger can check without asking us.";

/** The money sentence. */
export const OPERATORS_FOR_MONEY =
  "The free instruments cost nothing and never will: the preflight, your own well-known file as the way into the census, the passport page with its chip, and your host history. Every paid item is priced on the shelf where it is sold and read off the shelf when this page is served; the 402 at the door is the only price that binds.";

/** Free first, as every room says it. */
export const OPERATORS_FREE_FIRST =
  "Free first at every step: your own file to be found by, the preflight before you launch, the census the week you launch, your history while you stand, and the look when something goes wrong.";
