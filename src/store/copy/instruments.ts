/**
 * THE THREE INSTRUMENT ROOMS, THEIR THREE SENTENCES EACH (house rule
 * 60, 2026-09-04).
 *
 * Typed once here; the feature register, each page, each JSON twin and
 * the guide import them, and test/feature-surfaces.spec.ts holds them
 * identical across all four. The rule exists because the trade counter
 * shipped on every surface a guard reads and on none of the pages a
 * human integrator opens first — a door can be published everywhere
 * and still be unfindable, and a value proposition that reads
 * differently in four places is four propositions.
 *
 * NO QUOTES AND NO APOSTROPHES IN THESE SENTENCES. The page escapes
 * them and the cross-surface match dies, so the register refuses them
 * outright. It makes the prose a little more formal than the house
 * voice usually is; that is the price of the sentence being provably
 * the same sentence everywhere it appears.
 *
 * THE HARDER PART, and worth saying once here rather than three times
 * below: all three of these rooms sell nothing. Rule 60 still asks
 * each of them what money buys, and the honest answer for an
 * instrument-transparency room is that money buys nothing at all and
 * is not meant to. Saying so plainly is better than leaving the field
 * empty, because a reader who cannot tell free-forever from
 * not-priced-yet will assume the second.
 */

export const INSTRUMENTS_OPENED = "2026-09-04";

/* ── /sources ─────────────────────────────────────────────────────── */

export const SOURCES_PROPOSITION =
  "Every ecosystem figure this store publishes rests on a handful of public directories, and this page names all of them beside the last time each one actually answered us, derived from the stored weekly rounds rather than maintained by hand.";

export const SOURCES_FOR_MONEY =
  "Nothing here costs money and nothing here is for sale: this is the store reporting on its own reach, and a measurement project that charged for its list of limitations would have the incentive pointing the wrong way.";

export const SOURCES_FREE_FIRST =
  "The register and the wards heartbeat are free to read as a page or as JSON, with no account and no key, and the rows carry the fields to recount them yourself.";

/* ── /ledger ──────────────────────────────────────────────────────── */

export const LEDGER_PROPOSITION =
  "One signed week of the observed x402 neighbourhood read as a research note: what we reached, what answered, what moved, which defects by name, and on the same page what this instrument could not see.";

export const LEDGER_FOR_MONEY =
  "Nothing here costs money: every figure is a count over a signed snapshot that is already free to fetch, and the paid instruments are priced on the shelf where they are sold rather than gated behind this reading.";

export const LEDGER_FREE_FIRST =
  "Every week the chain holds is free to read as a page or as JSON, and each one names the signed snapshot it counted so you can rebuild the whole reading yourself.";

/* ── /mcp-ward ────────────────────────────────────────────────────── */

export const MCP_WARD_PROPOSITION =
  "A second ward on the same instrument design pointed at the MCP registry instead of at x402 doors: it counts registrations and records when a host stops being listed, and it shares no total with the x402 side.";

export const MCP_WARD_FOR_MONEY =
  "Nothing here costs money and no MCP verdict is for sale, because none exists: this ward enumerates and does not probe, so there is no reading to price.";

export const MCP_WARD_FREE_FIRST =
  "The whole register is free as a page or as JSON, including the passes that were truncated and therefore recorded no delisting at all.";
