/** Shared by pre-payment admission and the discovery schema. */
export const BUNDLE_MIN_HASHES = 2;
export const BUNDLE_MAX_HASHES = 20;
/** An EVM transaction hash is 0x followed by 64 hexadecimal characters. */
export const BUNDLE_HASH_CHARACTERS = 66;
