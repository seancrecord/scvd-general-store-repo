# Gap-finding series — running list

**Opened 2026-08-25.** Desk index only. Each prompt is a reading of
installed running code. Nothing here is a ruling or a fix. Papers
are dated; if the till or the install moves, re-run the prompt,
do not trust the paper.

**Rule of every prompt.** Answer from the code that is installed
and running. Every claim is a file:line or a live fetch. Library
vs store, and which is in force. Every instance, never one
sample. Find the writer of a field before trusting its name.

---

## Done

| # | Prompt | Heir to | Paper | The finding that is the point |
|---|---|---|---|---|
| 1 | Silent defaults in the dependency chain | `$1` spend cap | `docs/SILENT_DEFAULTS_2026-08.md` | Unmodified `@x402/fetch` applies `DEFAULT_MAX_AMOUNT_PER_PAYMENT = "$1"`. We never see the throw. Thirteen menu ids and every commission rung die on all three rails. |
| 2 | Client can refuse us with no second request | same cap, the abort inventory | `docs/CLIENT_ABORTS_AND_PUBLISHED_COUNTS_2026-08.md` (Part I) | After our 402, selection / cap / sign all run on the buyer’s machine. Challenge increments; no decline is booked. We cannot tell A6 from walk-away. |
| 3 | What our published numbers actually count | `organic_verifies` | same file (Part II) | `organic_verifies` is free `/api/verify` of already-issued artifacts, not the facilitator verify step. That ratio is the five-report false funnel. |
| 4 | Where the library's behaviour is not our behaviour | cold-isolate miss | `docs/LIBRARY_VS_STORE_2026-08.md` | We do not mount `paymentMiddleware`. First 402 on a cold isolate awaits facilitator `initialize()`. Analysts who read the Hono README described a server we do not run. |
| 5 | The catalogue against every constraint that gates a purchase | "one endpoint, one price" | `docs/CATALOGUE_CONSTRAINTS_2026-08.md` | The `$1` cap kills thirteen ids. `graffiti_on_a_train` survives the cap and dies in Node's 16 KiB header parser (17,007 bytes, nine signed accepts). Challenge booked; no payment. |
| 6 | Discovery and identity surfaces, checked for agreement | the area nobody has swept | `docs/DISCOVERY_SURFACES_2026-08.md` | Self-row agrees on 24 item ids. `menu.json` `store.chains` is still `["base","solana"]`. Glama/x402scan still sell retired SKUs. Third-party listings have no store-side mechanism. |
| 7 | The failure modes we have no instrument for | the meta-prompt | `docs/UNINSTRUMENTED_FAILURES_2026-08.md` | The holes that flatter: `corrected_conversion_rate` only strips crawlers; shutter 503 never enters the funnel; self-row `agree` is ids only. Cap and header make conversion look worse — they are second. |

## Not yet run

Leave a row when a prompt is handed over. Do not fill a finding
from memory.

| # | Prompt | Heir to | Paper |
|---|---|---|---|
| | *(none queued)* | | |

---

## Cross-links inside the papers

Prompt 1 provenance points at 2–3 and 4. Prompts 2–3 point at 1
and 4. Prompt 4 points at 1–3. Prompt 5 points at 1–2 (the cap
and the invisible abort) and measures the header the cap papers
did not. Prompt 6 points at 1–4 and names the self-row as the
join that already exists and what it does not cover. Prompt 7
is the sort of 1–6 by what we cannot see, flatterers first.
