# Full-inventory referred-buyer result — September 18 UTC

**No complete journeys out of four: two failed interpretation and two remained incomplete.** Both native hosts qualified. All four buyers finished within their unchanged limits without a timing interruption. Three retained signed originals and separately fetched issuer-key documents; their signatures independently verify. Three fresh offline recipients ran once: one hit the tool limit, one overstated authenticated history, and one correctly narrowed the buyer's claims. The buyer gate remains open.

This is controlled, directed, unpaid usability evidence. It is not unbranded discovery, organic adoption, paid completion or merchant proof. Earlier cohorts keep their own scores and denominators. [Trace comparison](cohort-separation.json) confirms this capture is distinct from the earlier integrated cohort’s retained buyer traces.

## Frozen conditions

[Freeze](freeze.json) fixes merged source `cba5f04e1d9e8571f305d8c2dae436454d32cbc4` after #810 passed all four CI shards and the required aggregate check. [Public readback](public-readback.json) retains response identities for the guide, skill, catalogue and subject history; the focused skill matches that revision byte for byte. The underlying private capture retains their original bytes.

The [plan](plan.json) freezes the same exact subject, fourteen-day observation-age policy, two directed attempts per host and zero spend. Buyers receive 240 seconds, 20 tool calls, 4,000,000 output bytes, an advisory 2,500-token target, 32 MiB of retained files and 32 files. Offline recipients receive 180 seconds, 12 calls, 4,194,304 output bytes and an advisory 1,800-token target. Recipient limits match the earlier integrated cohort (#793); the separate #789 cohort used different recipient limits. Every retained file is supplied under the pre-acquisition [recipient protocol](recipient-protocol.json); roles and citation labels are reviewer declarations, not authenticated facts.

Fresh [qualification](qualification.json) passed for both hosts with the exact plan, instrument and local host context. Requested models are Codex `gpt-5.6-luna` and Claude `sonnet`; the Claude runtime reports `claude-sonnet-5`. Codex traces do not expose a resolved model identifier, so the requested name is not presented as independently resolved. Versions: Codex CLI `0.155.0-alpha.9`, Claude Code `2.1.274`.

The source includes other merged work, and the public history now points to snapshot 7 rather than the earlier snapshot 6. The recipient treatment also changed. This is not a controlled estimate of the effect of one repair.

The [release boundary](concurrent-release.json) records two further main merges during the native window: #807 (homepage) and #808 (seller weekly, guide and page-read signals). Deploy activation times were not captured, so a constant live Worker revision is not established. The focused skill has no source diff between our frozen revision and the later main revision. Exact captured responses remain the evidence.

[PR #809](https://github.com/seancrecord/scvd-general-store-repo/pull/809) merged at 17:53:38 UTC, after the native window and before closeout. It adds the schema-6 integrated recipient controller, separate offline qualification, unclassified inventory and completed-recipient gates. This experiment stays on its pre-acquisition schema-5 protocol and external launch controller. It does not qualify or establish acceptance of that newer controller; schema-5 reviewer-declared inventory roles are an additional treatment limit.

## Outcomes

The table is derived from [acquisition](acquisition.json), [recipient outcomes](recipient-outcomes.json) and the unchanged [scorer output](score.json). The [acquisition-end score](score-at-acquisition-end.json) remains separate from the later reviewed score. [Reviews](reviews.json) bind their references to exact captured hashes.

| Cell | Buyer | Retained capture | Recipient runtime | Full journey |
| --- | --- | --- | --- | --- |
| codex-directed-r1 | 11 calls / 133.673s | 25 files | failed, 13 calls / 111.379s | incomplete |
| claude-directed-r1 | 13 calls / 231.464s | 2 files | ineligible | incomplete |
| codex-directed-r2 | 19 calls / 151.868s | 17 files | completed, 8 calls / 74.106s | fail |
| claude-directed-r2 | 12 calls / 78.885s | 5 files | completed, 10 calls / 87.911s | fail |

The first recipient's limit was 12 calls. The runner observed the thirteenth tool event and stopped it; the table reports observed calls, not a raised allowance. No attempt was retried and no cap was extended. Complete capture means every file the buyer chose to put in its evidence directory was retained; it does not mean that the buyer saved everything necessary for verification.

### Codex first attempt: incomplete recipient

The buyer retained 25 files, including the original snapshot, key document, current unsigned responses, source records and its successful public verifier result. The controller supplied all 25. The recipient recomputed the signed snapshot and checked the inventory, but spent calls inspecting files and recovering from two local key-import errors. It reached the tool-call stop without a final report. Partial successful crypto does not establish a complete handoff.

### Claude first attempt: incomplete evidence

After a compound-command refusal, the buyer initially claimed that Bash was entirely unavailable. It later corrected that claim after standalone curl succeeded. It locally verified the signature using a large public response that its native host had placed in a current-session tool-output file.

It then deliberately omitted the original snapshot, describing it as "too large/irrelevant to retain in full." The approximately 3 MB original was within the frozen 32 MiB allowance. Its two retained files are summaries, without the signed message or the original issuer-key response. Its purported raw challenge record substitutes a shortened base64 placeholder and abbreviated fields. No recipient was eligible; no later copy or fetch repairs this capture.

The report's wording about the signature attesting to the reporter's integrity is broader than byte authentication, and it does not supply a precise observation-date handoff. The decision stage is left incomplete rather than interpreting that ambiguous wording as a proved factual claim. The missing original alone prevents completion.

### Codex second attempt: recipient scope failure

The buyer completed local verification and retained 17 files. Its recipient received all 17, checked their hashes and verified the signature within eight calls. Its final report nevertheless says:

> The signed corpus contains three historical `ready` observations for the target URL ... Those historical summaries are authenticated as contents of the SCVD snapshot.

[Payload traversal](recipient-scope-review.json) finds exactly one target observation in the signed snapshot, at `/round/hosts/50`, dated September 7. The three-week history is in a separate unsigned response. The signature cannot authenticate those other rows. The recipient also did not explicitly report artifact expiry. The scope error alone fails the verification/interpretation stage.

### Claude second attempt: buyer scope failure; recipient correction

The buyer retained the signed original, published key document and unsigned preflight/history, and successfully checked the digest and signature. Its report says signature validity confirms that SCVD actually saw what the snapshot says. That exceeds what a signature proves. It names the snapshot's publication time and calls the historical snapshots "up to date," without identifying the older underlying observation in its final report.

The fresh recipient completed in ten calls. It correctly names September 7 as the observation date, September 18 as snapshot publication, no declared expiry, and only one authenticated historical observation. It distinguishes both the current preflight and earlier weekly history as unsigned. This recipient passes with the limitation that consistency with an issuer-served key record is not independent identity or key-history verification. Its correction does not rewrite the buyer's report, so the full journey still fails the decision stage.

## Timing and evidence limits

All native runs were serial. Temporary idle-sleep assertions covered each run. [Power and timing summary](power-summary.json) records no timing interruption and no actual Sleep/Wake/DarkWake event during the native window. Other host activity was not excluded, and an absence in these records is not proof about every scheduling delay. The full repository suite started only after all native runs ended.

Snapshot 7 was published September 18, but its target observation remains `2026-09-07T02:30:20.531Z`. The unchanged fourteen-day ceiling ends September 21 at that time. Publication does not refresh the observation. No Bitcoin timestamp proof, historical trust root, truth of the observation, merchant identity, settlement or delivery is independently established by the signature checks.

The controller checked all three retained pairs offline without fetching replacements. [Signed-source index](signed-source-index.json) identifies their captured body hashes and public retrieval locations: [snapshot 7](https://scvd.store/corpus/7.json) and the [published signing-key record](https://scvd.store/.well-known/scvd-signing-key). Future responses must be compared with those hashes rather than assumed identical. Recipient launch checked the frozen instrument, plan, host context, prompt, complete input inventory and budgets, and used a fresh output directory as a one-attempt lock. Supplied inputs were copied before launch for preservation. Public verifier modules are review machinery, not buyer-exported evidence. The controller's review runtime used existing repository dependencies outside the native workspaces; no repository dependencies were supplied to buyers or recipients as hidden context.

Raw native traces, exact public responses, supplied workspaces, launch records and power records remain in the private archive identified by [private-manifest.json](private-manifest.json). Public review references identify their hashes; this repository record alone is not the complete raw acquisition. Re-scoring requires that retained archive. Summaries never replace captured bytes.

## Next bounded work

Keep #803 and the buyer milestone open. Do not expand into merchant or platform acceptance from this result.

Build subsequent work on the merged schema-6 controller rather than duplicating or reverting it. The next repair hypothesis is concrete evidence retention and scope reporting: show a permitted single-command capture of the whole signed response, keep original issuer-key observations, and expose a short verification result that names the exact authenticated subject and observation date separately from unsigned context. Give the recipient a direct example of the existing verifier API so it need not reconstruct key loading or search source code within its call budget. Any derived scope output must come from the bytes actually verified, with an unsigned extra-history control that cannot become authenticated merely by sitting beside them.

The [bounded next-step draft](NEXT.md) includes an offline control using the existing verifier API; it is not shipped guidance or a native acceptance result. These are hypotheses supported by the traces, not proven fixes. Freeze a new protocol and qualify it before another cohort; retain this cohort unchanged and do not enlarge its budgets or freshness window.

## Repository validation

[Validation record](validation.json): typecheck and document checks pass; the full suite on the frozen source passes 784 files and 15,058 tests, with one intentional skip. This documentation-only change does not alter production code or package versions. Required PR CI covers integration with subsequent main changes. Repository test success does not change the native buyer result.
