# Next repair hypothesis: retain first, report only verified scope

This is a proposed guidance repair, not a shipped capability or another native cohort. The [offline demonstration](scope-example-result.json) uses the current public verifier against one retained original; it does not establish that a buyer or recipient will follow a new example.

1. Give the buyer a literal single-command response capture before inspection. Use `curl --fail --output ./evidence/original.json ACTUAL_CITED_SNAPSHOT_URL` and a separate command for the key record. Replace the URL with the public URL actually cited by the lookup. Keep source URLs separately. Save the entire original; a multi-megabyte response within the declared allowance is evidence, not a reason to keep only a summary. Inspect bounded fields from the saved file afterward.
2. Give the recipient a short existing-API example. Load the retained original and separately obtained key, call `createEvidenceBundle` and `verifyEvidenceBundle` with an explicit size allowance, and refuse to report authenticated observations unless `valid` and `evidence_complete` both hold. Use only the returned `signed_claims` as the source of authenticated rows.
3. For a recognized corpus snapshot, select the exact target from `signed_claims.round.hosts`. Print its signed pointer, URL and `observed_at`; print snapshot `taken_at` separately. Identify absence of declared expiry without converting `maxTimeoutSeconds` into artifact expiry. Other artifact formats need their own supported extraction, not a silent corpus assumption.
4. Label the issuer-key acquisition basis, unsigned current response and unsigned multi-week history separately. A signature authenticates bytes against the supplied key; it does not establish truth, legitimate ownership, observation-time authorization, delivery or Bitcoin anchoring.

The prototype's controls use the real retained bytes: the baseline yields one target observation; adding the unsigned three-row history to response decoration leaves that result unchanged; editing signed content fails the digest check; editing and recomputing its digest still fails signature verification. Neither tampered case yields an authenticated row. These are local controls for the example, not native acceptance.

Implement the smallest guide/example change on a fresh branch from current main, retaining the schema-6 controller merged in #809, with a regression that fails without it. This schema-5 cohort does not provide that controller's separate offline qualification or native acceptance. Preserve the four-cell record. Before another native experiment, freeze a new instrument/plan and qualify it; keep budgets unchanged and keep the original observation-age policy. The existing September 7 observation leaves that window September 21 at 02:30:20.531 UTC. Do not manufacture a fresh reading by using snapshot publication time.

Source check against merged #809: `scripts/lib/buyer-cold.mjs` has the schema-6 unclassified full-inventory prompt and names the verifier modules and size allowance, but supplies no direct invocation example. The focused skill already tells buyers to retain originals; the proposed change makes that existing requirement executable in a single capture command, rather than claiming retention guidance was absent. Treat these as navigation/interpretation hypotheses and test the actual public or recipient surface changed.
