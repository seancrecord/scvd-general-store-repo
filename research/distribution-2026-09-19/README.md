# Distribution reads — September 19, 2026

One read so far: the MPPScan discovery crawl repeated after the publication
doors release ([#826](https://github.com/seancrecord/scvd-general-store-repo/pull/826))
deployed, with the same pinned package as the
[September 18 qualification](../distribution-2026-09-18/mppscan-live-check.json).

- `mppscan-discover.json`: the receipt (window, package, command, the live
  OpenAPI document's size and digest, the counts, the warnings).
- `mppscan-discover.txt`: the crawl's transcript as printed.

Result: 196 routes, 38 paid, all 38 `[x402, mpp]`, 157 unprotected; the
three publication operations that read `[x402]` alone the day before now
carry the descriptor. The three warnings are the September 18 three. No
registration was submitted from here; the press is the keeper's
([draft](../../registry/mppscan-submission.md)).
