# Separate timestamp and MPP correction check

One new, capped reader session after the retained integrated 6/7 result.
Both original fixture cases pass: exact observation timestamp retained; MPP
challenge presence never promoted to validity. Raw input hashes, command traces,
actual adapter calls and final answers are retained. The scorer verifies the
required operations and unchanged inputs; manual interpretation review is explicit.
This 2/2 result does not replace earlier cohorts or establish general reliability.
