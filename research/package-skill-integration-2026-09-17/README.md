# PS1–PS4 integration — September 17, 2026

Clean base: `30e9e00b` on `codex/package-skill-milestone`, combining only the
PS4 commit `288948ed` and uncommitted PS1–PS3 work from its activation worktree.
The unrelated field-run ancestor of PR #768 is excluded. Main's newer privacy,
recovery, replay, archived Almanac, tab pin and quote/settlement copy is carried
into the references. The verifier source had no upstream difference from its
qualified base; its release README now offers the versioned registry install
alongside the tested local tarball path.

The full seven-case corrective reader returned 6/7: MPP is corrected, but the
preflight answer omitted its observation timestamp. That failure stays in this
folder. The subsequent explicit timestamp instruction has a separately frozen,
fresh two-case check at `../skill-date-mpp-recheck-2026-09-17/`: both preflight
and MPP pass. These are not a replacement clean seven-case cohort. All fixture
outputs reconcile with actual host command output; all supplied inputs stayed
unchanged. No real network probe, cryptographic check, spending or secrets were
involved in those synthetic reader tasks. General reliability remains unmeasured.

Typecheck, evidence tools, independently regenerated vectors, clean-tarball
quickstart/TypeScript consumer and Worker build passed. Two inherited content
guards initially failed because they read only the shortened entry; their
unchanged assertions now traverse both full skill trees and pass (28 checks
across five relevant files). Before/after logs are retained. Local-source Skills
CLI installation and actual ClawHub archive preparation/extraction retain all
11 files and match final skill tree hash
`50c63823dacda48bc82b93bb9537df5c1bd589ad84f24d9e836f522b9efdb606`.

Full repository suite and remote release status are recorded in the handoff and
release record. The older 2026-09-17 evidence directories remain immutable.
