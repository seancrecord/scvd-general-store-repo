# Integrated referred-buyer qualification — September 18 UTC

This acquisition follows the published verifier and capped Claude probe in
[the preceding record](../verify-source-qualification-2026-09-17/REPORT.md).
It keeps that attempt unchanged. The separate completion diagnostics passed,
but did not establish the previous timeout's cause.

The [freeze](freeze.json) names the integrated runner changes: #783 provides a
session scratch/cache directory, #786 links the existing large-corpus
instructions, and #788 scopes permission refusals to the actual invocation.
The public package remains x402-verify 1.5.0. This is an integrated acceptance
reading, not a controlled attribution of improvement to any single repair.

The four referred cells, models, budgets, permissions and historical freshness
policy remain in [plan.json](plan.json). One new generic probe per host must
pass before any buyer runs. Every buyer outcome remains in the denominator;
no retry, retrospective cap change or replacement artifact is permitted.
Only captured original/key pairs can qualify for fresh offline recipients.

Raw native events and host metadata remain private. The final report and
manifest distinguish diagnostic success, host qualification, buyer behavior,
cryptographic verification, retention and recipient interpretation.

[Final reviewed result](REPORT.md): one complete, one failed and two incomplete
journeys. Two buyer signature checks succeeded; one fresh recipient completed.
[Score](score.json), [reviews](reviews.json), [capture details](acquisition.json),
[private evidence manifest](private-manifest.json), and [validation](validation.json).

A [concurrent cohort](concurrent-cohort.json) in #789 has separate traces and
outcomes; neither acquisition replaces the other.
