# Adoption and latency reading — September 10, 2026

`preview-inventory.json` is derived from the local prepared menu, not a
deployment claim. `latency.json` retains all requests from the explicit
three-URL sample. `anchor-lifecycle.json` contains service-reported times
from two selected public records; it does not independently verify proofs.
Scope, method and remaining work: `../../docs/ADOPTION_AND_LATENCY_2026-09.md`.
