# ROADMAP — September 2026

Feature order. The desk is `KEEPER_LIST.md` (his hands).
Two queues, one job each.

August phases 0–5 (what shipped, with acceptance) live at
`docs/archive/OBSERVATORY_ROADMAP_2026-08.md`. Do not update
that file. The 2026-09-01 dump attic is
`docs/archive/DESK_DUMP_2026-09-01.md`.

**Still open.** Ranking a queue is not closing a row. A
stranger paying for a verification-tier item reorders this
file behind that item (Assumption 0).

The bottleneck named 2026-09-01 is packaging, not another
`llms.txt`. Agents already have doors. Humans and routing
models need a first screen that makes the value obvious,
and a public proof they can cite. Grow from inventory we
already produce. Do not grow by becoming a score.

---

## NOW — next builds (serial, #65), in order

What is left, top first. Done rows fall to the bottom of
this file the day they close (his ask, 2026-09-02), so
this section is always the queue and nothing else.

| # | Task | Why now | Acceptance |
| --- | --- | --- | --- |
| V3 | **The MPP read-only battery.** PR 1 BUILT 2026-09-04 on his "get bolder on actual implementation" and his rulings on the design (decision 3 firm: the x402 verdict keeps its meaning permanently, `protocols_spoken` is the union; decision 2 wants the passport copy mocked up before he rules; the rest approved as-is): `src/lib/mpp-challenge.ts` (RFC 9110 parser, the quoted-comma trap included), `src/services/mpp-battery.ts` (the Tier 0 checks and advisories, `protocols_spoken` derived, `countMppMisreads` with its denominators), the `mpp` block and `protocols_spoken` on the free report and so on the dry run and the look, `www-authenticate` on the capture list from this round, `/api/practice/mpp-shape`, the `mpp` family row, vocabulary v11 (one class per check that can fail, sourced to the draft), the CLI's protocol lines, the recorded doors in `test/fixtures/mpp/`. NEXT: PR 2 (the census column, the corpus fields, the brief's count, the passport ruling once he has seen the copy in `docs/MPP_READ_ONLY_2026-09.md`), PR 3 (the paid audit's discovery read). | The second wire. A door speaking MPP read as a broken x402 door was a verdict on our reader wearing a finding about their door. | `test/mpp-battery.spec.ts`: the parser, every recorded door failing exactly the checks it is bad in, the report's block and the verdict untouched, the practice door, the family row, a class for every failing check. |

Ordered 2026-09-03 under his lens: value or potential value
if the market takes off, not ROI now ("think of it as tech
in 0% interest rate times"). A1–A3, the next three moves of
his delegated-agent memo of the same evening
(`docs/DELEGATED_AGENT_PLAN_2026-09.md`), are built and sit
under DONE; what they wait on is his press and his four
rulings in that document. The rest of the memo and his
channel list sit under LATER as C1–C7. One PR
at a time; draft copy goes in chat, not on a flag.

---

## SOON — dated waits

| # | Task | Why | Acceptance |
| --- | --- | --- | --- |
| S8-v3 | **The v3 fold (S8 PR 2).** `discovery-info-fails-schema` and `offer-contradicts-challenge` fold into a preflight v3 verdict; `AUDIT_CRITERIA_VERSION` and `CENSUS_BATTERY` move together; dated `/criteria` note; nothing resigned. His yes / no / later, on the advisory rows the census has read so far; no calendar gate (a dated wait an agent had written here was struck 2026-09-03: "i dont wait i decide yes/no/later"). | The fold is a battery decision made on evidence, the way L3b went into v2. | Version constants moved together; `test/battery-inside-the-bytes` holds all three producers equal; a v2 `ready` issued before the fold keeps its meaning. |

---

## LATER — option value, demand-tagged

Hands in many pots, cheap, reversible. Build when a
trigger fires, not from enthusiasm. Detail: dump attic
+ August parked table.

**If discovery takeoff (Bazaar / Agentic.Market / Circle
lists us and strangers arrive):** pull S1–S2 forward;
do not invent `/for-merchants` pages.

**If someone links a scvd.store reading unprompted:**
cheap `/check/{host}` (census-only). Live-probe stays
parked (unfurl / SSRF).

**If a named counterparty wants a rail:** lane A, flag-
dark, after he inks the rail run-through. Not before.

**If webmcp channel has arrival data:** `check_this_store`,
porch `ring_bell`, WebMCP conformance instrument.

| # | Task | Trigger / why later |
| --- | --- | --- |
| L1 | Phase 4 `/agent/v1`, `find_endpoints`, `@scvd/agent` | Nothing is asking. Free desks already have MCP tools. |
| L2 | Phase 5 chips, `@scvd/launch-check`, first `cross_ref` consumer | 5.2 pricing is ⚑. 5.1 partly live as `/corpus/diff.json`. |
| L3 | First lane-B reader (MPP / AP2 / ACP) | After rail ink + intake collision. Parser, not a till. ACP/UCP merchant checkout stays read-only. |
| L4 | Directory / facilitator QA as a *report we already run* | Sell the weekly brief (S1) to a named directory. Do not build a second census. |
| L5 | Corpus slices as paid history / API | Free public summaries stay free. Paid = convenience and history, not a score. Demand tag. |
| L6 | The look — DONE 2026-09-02, moved to DONE below. | Named by the keeper the same day after the SOON/LATER review. No threshold SKU: none was asked for, and the row said maybe. |
| L7 | The preflight Action — DONE 2026-09-02, moved to DONE below. | Named second by the keeper the same day, after L6. |
| L8 | Parked tickets: none. | Checked 2026-09-02: the seven numbers this row carried (#83, #70, #71, #68, #80, #57, #58) are August pull requests, all merged, and the tracker holds no open issue. The row stays so the next parked ticket has a place to land. |
| L9 | Solana parity gaps 2 to 5, Tab leftovers | Per-build. The `/observatory` page and parity gap 1 are DONE 2026-09-02 (rows below); gap 4 is structurally blocked and gap 5 has no oracle, per SOLANA_PARITY.md. |
| L10 | Card family, cheap `/check/{host}`, D5 patronage sell-up | Demand / ink. |
| L11 | Cross-protocol evidence desk | Same as L3, grown up. "First attestation authority" is not the goal; cheap readers are. |
| L12 | Sponsored bounties: an operator pays to post their door on the board | Demand tag: an operator asks. Honest only if loud — public bounty, disclosed finder wallets, corpus flags the settles bounty-driven never organic. The quiet version is the wash trading we called out. Do it that way or not at all. |

### The channels — how an agent or a developer meets the store (his list and his memo, 2026-09-03)

Seven ways a stranger arrives, each with what already stands and
what is missing, ordered under the takeoff lens. The memo's parts
are mapped onto them in `docs/DELEGATED_AGENT_PLAN_2026-09.md`.
After A1–A3 the recommended order was C2, C1, C4; all three
shipped 2026-09-03 on his "take them in your order". C5 shipped whole the same
evening, C7, C6 and C3 the next morning. Every channel on this table
is built; what remains on each is his press or a named ask.

| # | Channel | How they arrive | What stands today | What is missing | Order |
| --- | --- | --- | --- | --- | --- |
| C2 | Framework templates | Developers copy examples from OpenAI Agents, Vercel AI SDK, LangChain/LangGraph, CrewAI, PydanticAI, AutoGen, Claude Code / Cursor, Copilot | DONE 2026-09-03 (row below): `examples/`, eight frameworks over one shared module in two languages, tested in CI against recorded readings. | One `examples/` directory, one operational workflow per framework (an agent is about to pay; it calls preflight; it reads network, asset, recipient and the defect state; it decides), each runnable against the live free doors and tested in CI against fixtures. | 1 |
| C1 | Runtime workflow | An agent hits an x402 failure, a payment ambiguity, or a receipt-verification task | DONE 2026-09-03 (row below): `buyer_hint` on every class, `remediation` on the report and the audit, `next_action` and `documentation_url` on refusals, the CLI and the Action rendering the server's rows. | A `remediation` per defect class (what the operator does, what the buyer does) on the vocabulary, the preflight report and the audit; error bodies carrying `documentation_url`, `verification_url` and `next_action` on the doors, the CLI and the Action — only where SCVD is a legitimate remedy. Small. | 2 |
| C4 | OpenAPI / API tool calling | Developers wrap the API in their own agents or frameworks | DONE 2026-09-03 (row below): `/openapi-tools.json`, the free instruments as function-calling tools with one worked call each, derived. | `/openapi-tools.json`: only the free deterministic doors with function-calling-shaped schemas and one worked call each, derived from the contract, never a second typed list. Small. | 3 |
| C5 | Code dependencies | Developers install packages, examples, CLIs, GitHub Actions | DONE 2026-09-03 (C5a and C5b below): `x402-verify` 1.1, `x402-sign`, `scvd-cli` 0.2.0, `scvd-tab`, the preflight Action, the ClawHub skill, `examples/`, and the four new packages. Publish is his button. | Nothing on this row until a package is asked for by name. | 4 |
| C6 | Retrieval / data, and the corpus as a feed | Agents discover the research during a factual task, or subscribe | DONE 2026-09-04 (row below): `/corpus/latest.json`, `/corpus/changes/{week}.json`, Last-Modified beside the ETag, the subscribe example, the notebook. | Nothing on this row until a subscriber asks for a shape that is not served. | 5 |
| C7 | Other agents | Agents link to, invoke, reproduce and discuss useful tools | DONE 2026-09-04 (row below): `/fixtures.json` and `/fixtures/{set}/{name}.json` with sha256, a `cite` line on the signed documents and pages. | Nothing on this row until another instrument asks for a set that is not served. | 6 |
| C3 | Agent-resource catalogs | Agent platforms discover MCP, A2A, API, skill and nested catalog records | DONE 2026-09-04 (row below): `/.well-known/ard.json` and `/.well-known/ai-catalog.json` name every record — both MCP doors, the evidence card, the contract, the tools document, every dataset, every feed, the skills — and the RFC 9727 api-catalog knows each URL first. | An AWS Agent Registry entry (his press, KEEPER_LIST). | 7 |

What not to chase, per the memo, recorded so the next intake does
not reopen them: more generic directories; a marketplace identity;
"trust layer" positioning; an A2A chat agent that duplicates the
site; paid mentions; awesome-list follow-ups; a giant overlapping
tool catalog.

---

## DONE — newest first

Closed rows, kept for the record. Nothing here is a queue.

| # | Task | Why it was built | Acceptance, as met |
| --- | --- | --- | --- |
| W3 | **The directory walks: 402index and x402scan, read.** DONE 2026-09-04, the night the keeper priced both by hand (PR #482, $0.03): `src/services/directory-walk.ts` walks each on the hourly press on a stored cursor — 402index free at fifty pages a tick over its 104,106 rows, x402scan at a cent a page from the field wallet through `src/lib/pay-fetch.ts`, the launch check's refusals as a function (own wallet, unscreened payTo, over cap, redirect, retry) — and the Sunday round reads each one's LAST COMPLETED PASS, never the directory; a truncated or stale pass is null to the census. A dollar a pass caps the paid one, his to raise. Both fixture sets served at `/fixtures/{402index,x402scan}/…`; crank on `/admin/ward`. | The roster had waited on "a price the keeper has seen" since 2026-08-18; he went and saw them, and the roster had 402index wrong (free, not rate-limited into 402s). | `test/directory-walk.spec.ts`: both parsers against the captured bodies, the walk machine (a tick, a completed pass, the page and spend ceilings, an unreadable page holding its cursor), the census's freshness rule, and every refusal `payOnce` makes; `test/fixtures-served.spec.ts` holds the two sets to their directories. |
| W2 | **The MCP ward.** DONE 2026-09-04 ("carve out an mcp ward and do another list similarly"): `src/services/mcp-ward.ts` walks the official MCP registry in hourly batches on a stored cursor (90,845 rows across 909 pages on the day it was walked to the end), completes a pass when the registry's own cursor runs out, and folds mortality — appeared, stopped being listed, listed again — into its own register at `KV_KEYS.mcpRegister`. It counts and does not knock: no session is opened and no MCP verdict is issued, because no MCP battery exists to cite. It shares NO total with the x402 ward, by design and on every page. A truncated pass records its hosts and refuses every disappearance. Room at `/mcp-ward` (Dataset node, five answers, rule 60 row); crank at `/admin/mcp-ward` advancing one batch, with a reset that touches the register not at all. | Folding MCP servers into `population_known` would have retroactively changed what every `coverage_pct` this store has sealed was a percentage of. | `test/mcp-ward.spec.ts`: rows and hosts counted apart, mortality on a completed pass, a truncated pass records no delisting, the remote-URL count belongs to the pass not the tick; `test/admin-mcp-ward.spec.ts`: the room on an empty store, a pass in flight, reset keeps the register whole. |
| W1 | **The source register, the ward heartbeat, and the week's ledger.** DONE 2026-09-04 (his "fix our hard coded... yes please do 1 / 2 / 3 / 4", and "summarize with findings... almost as publishing research"): `/sources` derives each directory's liveness — last successful pull, consecutive failures, `never_answered` for a reader that has never got an answer — from the stored rounds' own `per_source` blocks instead of a hand-written constant; `UNREAD_DIRECTORIES` keeps its exact wire shape on the anchored corpus. The heartbeat rides the HOURLY press and checks that a round WROTE something, not merely finished; missing weeks are named on `/corpus.json` under `continuity`. `/ledger/{week}` reads one signed week as a research note with machine-derived findings, each naming the fields it came from, gaps on the same page. Three new sources: x402-list (free, shape captured, per-row provenance), agentic.market (the keeper's word; shape borrowed, refuses a paginated first page), and the roster grew from two named-unread directories to six. `test/denominator-discipline.spec.ts` holds that no rate is taken over the enumerated set. All three rooms under rule 60, on the front, in the guide (digest, the sixty-fifth). | A neighbouring measurement project's coverage page: liveness "derived from actual run history... not hand-maintained", a watchdog that checks a cycle wrote, and a front page found doing the division its own limits forbade. | `test/source-liveness.spec.ts`, `test/ward-heartbeat.spec.ts`, `test/week-ledger.spec.ts`, `test/ledger-and-sources-rooms.spec.ts`, `test/denominator-discipline.spec.ts`; `test/feature-surfaces.spec.ts` and `test/discoverable.spec.ts` walk the three rooms. Red-teamed the same day: the ledger's reach denominator, the per-tick remote count, the ascending list under a cap, and the hourly full-round read were all found and fixed before merge. |
| R7 | **Reproduce as one call, the cite box, the seats declared, and three grips.** DONE 2026-09-04 ("Let's do it all. Let's also think about making it frictionless for agents and humans."): the look takes `since` (a signed week) and carries a `reproduce` block — the live probe against one signed row, classed `same | moved | instrument_moved | not_comparable | no_such_round` by the rule typed once in `src/services/reproduce.ts` and rendered at `/criteria#result-class`, both sides printed, the failed checks added and cleared named, the row cited; the MCP `look_at_door` tool takes the same `since`. Every row surface prints the citation (`src/services/cite.ts`): `cite` on `/corpus/host/{host}.json` and its page, on `/corpus/{n}.json`, on the round JSON, and on the reproduce block; the citation watch reads the shape. Rows carry the battery that read them. The seats are declared as data on `/.well-known/trust.json` and the corpus Dataset. `/scorers` opens with "Start here": the same five steps as three shell lines, three CLI verbs and three MCP calls. The CLI (0.2.0) gained `corpus --since`, `host`, `cite` and `reproduce`, with exit codes a script can branch on. | The scorers' room described reproduce and cite as instructions to follow by hand; a scorer's engineer needed a working pipeline, not a reading assignment, and a marketplace challenged on a listing needed one call it could quote with its derivation beside it. | `test/reproduce.spec.ts`: the five classes as pure functions, the look's default and named-week comparisons, the refusal of a malformed week, the cite shape on four surfaces with the row's own digest, the seats on both data surfaces, the rule on `/criteria`, the three grips on `/scorers`; `cli/cli.test.mjs`: the new verbs, their bodies, their exit codes; `test/look.spec.ts` unchanged and green. |
| R10 | Lane C: where a name-only host's own file gives no door, the sweep reads the name directory's page for that host and walks one listed path, source `directory` — a feed's word, out of the delta and the door bank, never mistaken for the host's own. Counts under `walk.sweep.directory`. | DONE 2026-09-05 |
| R9 | The feed read resumes across hourly firings until the feed's declared total (no page cap binds); the census writes why beside every null; the register's word for that feed is `partial` (#487). The walk's roster caps at 2,000 feed doors where a week's evidence in one KV value would fail near 3,900, and the round says when it binds. NEXT, when it binds: results stored per batch under their own keys, the round's hosts in R2 alone. | DONE 2026-09-04 (cap announced; the storage move not built) |
| R8 | The sweep and the declared door: the census reads each name-only host's own `/.well-known/x402` (agent-card pointer, one hop) in the walk's idle firings and walks what a host declares for itself; `POST /api/declare-door` is the same read by hand. One consent line, rule 52 on every count. | DONE 2026-09-04 |
| R6 | **For scorers and marketplaces: two seats, named.** DONE 2026-09-03 ("build it"): `/scorers` (`src/routes/scorers.ts`) states the two seats the store occupies — the record, and the reproducible dispute artifact — and how a scorer or a marketplace consumes the evidence without inheriting an opinion: pull (the corpus index, snapshots, per-host, per-round, the diff, the tiers), verify (the verify URL, the DID document with retired keys, the anchor log, x402-verify), cite (by verify URL, never restated), reproduce (the preflight battery, the look), re-observe (differences are time; our own errors at `/corrections`). The doctrine gained `TWO_SEATS_SENTENCE` and `MISUSE_CLAUSE`, rendered on `/criteria`. Named integrations render `src/store/citing-systems.json` — empty today, and the page says so — and `scripts/citations-check.mjs` fetches every listed citing URL and fails when the citation is gone. Registered as a room; the atlas carries the door and a goal in the scorer's voice; the guide names it. | A reader drew a three-layer stack and put the store in the record seat only, handing the dispute artifact to a competitor. The store occupies two seats and had never said so on a served surface; and a diagram naming vendors would have been a claim about other people's products. | `test/scorers.spec.ts`: the seats and the clause on both faces, every named surface answers as written, the register rendered and nothing else, room and atlas registered, `/criteria` carries the seats; `scripts/citations-check.test.mjs`: the citation matcher and the verdicts; `test/doctrine.spec.ts` walks the new room. |
| C6 | **The corpus as a feed.** DONE 2026-09-04: `/corpus/latest.json` (the latest signed snapshot at an address that never changes, ETag and Last-Modified for a conditional GET) and `/corpus/changes/{week}.json` (one signed week against the one before it — additions, removals, recoveries, regressions, changed payment routes, changed prices, changed defect state — as plain fields and a plain-English changelog, derived at read from the two snapshots by the same rule `/corpus/diff.json` uses); `Last-Modified` on the index, the snapshots and the rounds; a subscribe example on `/corpus` and its JSON; `examples/corpus-recompute.ipynb`, a notebook that fetches the chain, recomputes every digest and the signatures, and counts with denominators, linked from `/corpus`; the guide names the two doors (digest, the fifty-seventh). | His "keep rolling" (2026-09-04); the channel where an agent subscribes rather than reads. | `test/corpus-changes.spec.ts`: over a seeded chain, the first week has nothing to compare, the second names each change by field and in the changelog, a week the chain lacks is a 404 naming the weeks held, latest.json carries the cite and Last-Modified, and a conditional GET on it answers 304. |
| C7 | **The fixtures at stable URLs, and a cite line on every signed document.** DONE 2026-09-04: `/fixtures.json` (a registered dataset with the sha256 of every file's served bytes) and `/fixtures/{set}/{name}.json` for the recorded doors, the MPP challenges and the verifier's vectors, derived from the three directories in the tree and held to them by `test/fixtures-served.spec.ts`; unsigned recorded material, said so on the index and in a response header. `src/lib/cite.ts`: one line derived from an artifact's own fields — issuer, what and which, when observed, how signed and where the key is, the URL whose bytes are cited — as `cite` on the verify door's receipts and reports, corpus snapshots and rounds, passports and the month, and as a line on those pages; the guide names it (digest, the fifty-sixth). | His "keep rolling" (2026-09-04); the channel where other agents link, reproduce and discuss: the exact bytes and a line to quote are what a citation needs. | `test/fixtures-served.spec.ts`: the served index equals the directories, every URL's bytes hash to the sha256 beside it, the dataset envelope passes the machine-readability guard, and the cite line rides the documents and pages named. |
| C3 | **Every record in the agent-resource catalogs.** DONE 2026-09-04: the ARD manifest (`/.well-known/ard.json`, byte-identical at `/.well-known/ai-catalog.json`) now carries one entry per record the store publishes — the store's MCP door and the verifier door with their tool names as capabilities, the evidence agent's card with its tasks, the HTTP contract, `/openapi-tools.json`, every dataset in `PUBLISHED_DATASETS` with its caution in the description, every feed in `FEEDS`, the skills — each derived from the roster a guard already walks; the RFC 9727 api-catalog gained the verifier door, one row per dataset, and the feeds as `alternate` links in Atom on the feeds row, so the ARD cross-check (no URL the api-catalog has never heard of) still holds. | His "keep rolling" (2026-09-04); the last channel on the table: a platform that reads one document learns all of it. | `test/ard-catalog.spec.ts`: both doors named and the verifier's capabilities are its tools with no buy among them, the card at the URL the api-catalog anchors with its tasks, the tools document, one entry per dataset and per feed from the rosters, identifiers unique; `test/api-catalog-and-lifecycle.spec.ts`: the verifier door, every dataset and every feed in the catalog and its index. |
| C5b | **The packages.** DONE 2026-09-03: four zero-dependency packages, each with a README, one example, a licence, a versioning line, a dated CHANGELOG and a test suite on Node's own runner against a server that is not the store — `x402-preflight` (the Action's file as a library and a command: `preflightOne`, `preflightMany`, `remediation`, `exitCodeFor`, the exit law), `scvd-corpus-client` (the CLI's library half: corpus, fresh set, host history, month, feeds, diff, defects, `withDenominator`), `scvd-defects` (vocabulary v10 as data with both halves of the remediation, the nine recorded doors as fixtures, `isStale()` against the live document; the minor version is the vocabulary version), `scvd-mcp-starter` (a stdio MCP server, one file, forwarding to the read-only verifier door). `npm run packages:test` is a named CI step; `npm run defects:cut` re-cuts the snapshot from the tree and `test/packages.spec.ts` fails when it is behind. The four publishes are his press (KEEPER_LIST). | The rest of C5: the most durable way to appear in codebases is to be installed, and each of these is the smallest honest thing a developer would install. | Each package's own tests; `test/packages.spec.ts` holds the snapshot to the vocabulary, every `files` entry to a file that exists, and every manifest to the repository's directory. |
| C5a | **The CLI at 0.2.0.** DONE 2026-09-03: `scvd look <url>`, `scvd before-you-pay <url> [--cap <usd>]`, `scvd month [YYYY-MM]`, `scvd feeds`; every rendered preflight prints the store's `remediation` rows as FIX lines and a refusal's `next_action`; README rows, a dated CHANGELOG with the versioning line, tests against a server that is not the store. Exit codes follow the answer the script asked for: the look's live verdict, the buyer's own `would_throw`. Publish is his press (KEEPER_LIST). The packages half of C5 — `x402-preflight`, `scvd-corpus-client`, `scvd-defects`, an MCP starter — stays on the row. | His "take them in your order" (2026-09-03); the CLI is the cheapest way a developer meets the four newest free doors. | `cli/cli.test.mjs`: each new command's request shape, rendering and exit code, `--cap` as the client profile, a bad month refused; the help names every command by derivation. |
| C4 | **The free instruments as function-calling tools.** DONE 2026-09-03: `/openapi-tools.json` (`src/routes/openapi-tools.ts`) lists the free, read-only instruments in the common function-calling shape — `{ type: "function", function: { name, description, parameters } }` — one worked call each, with the HTTP door, the contract's operation id and what the tool reads under `x-scvd`. Derived, never a second typed list: the tools are the MCP catalog's free read-only set (the browser surface's own derivation), the schemas its own objects, the door the WebMCP endpoint map, the operation id the contract's rule, the worked call the example every tool carries. No paid door can appear. In the API catalog's `service-desc`, on the atlas, on the developers page, in the guide (digest re-taken, the fifty-second). | His "take them in your order" (2026-09-03); the third channel: a developer wrapping the API in their own agent wants the five doors as tools, not the whole contract. | `test/openapi-tools.spec.ts`: every tool is a catalog tool with the catalog's schema, a worked call that satisfies its required fields, a door under /api whose operation id is in `/openapi.json`, read-only, never a buy; the document is in the catalog, on the atlas and in the guide; the guide is not a function. |
| C1 | **The runtime workflow: remediation, both halves.** DONE 2026-09-03: vocabulary v10 gives every defect class a `buyer_hint` beside the operator's `repair_hint` (additive; no definition moved); the free preflight report carries `remediation` — one row per failed check or raised advisory a class explains, derived through `our_signal` (a signal is every class it explains: `accepts` is two), with the class, its definition URL, the operator's half and the buyer's half, never part of the verdict, empty on a clean door — and rides into the dry run and the look through `the_door`; the paid audit's delivery carries the same rows beside the signed report, outside the signature; the CLI prints them as FIX lines and the Action's summary as "What to do" with the definition linked; every door refusal names `next_action` (the contract's own what_to_do) and `documentation_url`; the class page and the markdown print the buyer's half. | His "take them in your order" (2026-09-03); the second channel: an agent that has just read a named defect needs the next step on its own side without a second fetch. | `test/remediation.spec.ts`: both halves on every class and different; a signal resolves to every class in either spelling; rows derived, none on a clean door; the report carries them with both halves and the verdict untouched; refusals carry the next action; the served vocabulary carries the buyer's half. `cli.test.mjs` and `preflight.test.mjs` hold the renderings to the server's rows and to silence without them. |
| C2 | **The framework examples.** DONE 2026-09-03: `examples/` — one operational workflow (read the 402, one POST to the free dry run which carries the preflight whole, read the terms and the named defects, decide with every reason named) written for OpenAI Agents (with the read-only MCP door as a hosted tool), Vercel AI SDK, LangChain / LangGraph, CrewAI, PydanticAI, AutoGen, a Claude Code skill with the same rule for Cursor, and Copilot instructions, over one shared zero-dependency module in JavaScript (`shared/decide.mjs`) and Python (`shared/decide.py`); five readings recorded from the store's own battery and one expectations file both runners assert; nothing signs or pays; `cannot_tell` for unknown, never a refusal dressed as one. `npm run examples:test` is a named CI step. | His "c2 is fine take them in your order" (2026-09-03); the one channel with nothing standing. | `examples/shared/decide.test.mjs` and `test_decide.py` hold both languages to `fixtures/expected.json`; `test/examples.spec.ts` holds the recorded readings to the live battery's shape and every framework file to the shared module and the free doors. The framework files are syntax-checked, not executed, and the README says so. |
| A3 | **The verifier door: "SCVD x402 Verifier".** DONE 2026-09-03: a second MCP door at `/mcp/verifier` (`src/routes/mcp-verifier.ts`) serving exactly five read-only tools under task-shaped names — `preflight_x402_endpoint`, `verify_x402_receipt`, `lookup_endpoint_readiness`, `get_defect_definition`, `verify_scvd_artifact` — and no shelf: three are the free tools renamed and run by the same handlers `/mcp` runs, two are the evidence agent's readiness read and the defect vocabulary, which had no MCP door of their own; a paid tool's name is refused as unknown; the handshake names the verifier, not the store. On the atlas; the guide names it (digest re-taken, the forty-seventh). In the same PR the self-join stopped reading the A2A card as a catalog, since the card is the evidence agent's now and carries no shelf. The submission itself is his press (rule 30) and awaits his decision 1 on the app's shape. | The memo's third move: a plugin named for the whole store was the wrong shape for a directory reviewer or a model choosing among eighteen overlapping names. | `test/mcp-verifier.spec.ts`: five tools, none a buy, each read-only by annotation; the renamed tools carry the base tool's input schema; a receipt verifies here as on `/mcp`; readiness and a defect definition answer from the chain and the vocabulary; a paid name is refused; the door on the atlas. |
| A2 | **The evidence agent (A2A).** DONE 2026-09-03: `/.well-known/agent-card.json` (and the two older card paths) now describe a specialist in task language — x402 endpoint preflight, x402 receipt verification, x402 endpoint-readiness dataset — with the endpoint at `/a2a`, streaming and push off, the position carried; `POST /a2a` is JSON-RPC `message/send` with one data part `{ task, …input }`, answered with one completed Task whose single artifact is the bounded shape (task, observed_at, result, scope, does_not_establish, verification_url, artifact_url, key_url, evidence); the three tasks run on the preflight, the conformance desk and the look's held half (`src/services/a2a-evidence.ts`); no task state, `tasks/get` says so; refused input is a failed task or a JSON-RPC error, never a throw. On the atlas; the guide names it (digest re-taken, the forty-sixth). | The memo's second move: a specialist a planner delegates to when it needs evidence, never a storefront another agent must interpret. | `test/a2a-evidence.spec.ts`: the card's three skills by plain noun and no house name, a receipt valid against the supplied key and invalid against the wrong one with the scope saying which, never_met as a coverage fact, refusals and unknown methods answered plainly, the door on the atlas. |
| A1 | **x402-verify 1.1.0: one call, bounded evidence back.** DONE 2026-09-03: `verifyReceipt({ receipt, issuerKeyUrl })` and `verifyOffer({ offer, issuerKeyUrl })` on the 1.0 primitives — valid, a scope naming the key the bytes were checked against, what the result does not establish (always stated), the checks, the issuer, the free hosted desk; the key from the caller's URL or key, never from the artifact; seven fixtures cut from the published conformance vectors ship in the package; a dated CHANGELOG with the versioning line; the README leads with the one call; the retired keyword gone from the manifest. Publish is his button. | The memo's first move: the most durable way to appear in codebases and coding-agent contexts is to be installed. | `test/verifier-package.spec.ts` holds the shape on every shipped fixture, the key resolved from a URL and never from the artifact, and the six entries in the tarball. |
| V5 | **The state of x402, by month.** DONE 2026-09-03: `/corpus/month` (`src/services/monthly-state.ts`, `src/routes/monthly-state.ts`), derived at request from the same signed weeks the brief reads, grouped by the calendar month a snapshot was taken in — the closing week's reading (doors named, probed, payable and not, unreachable, offers seen) beside every round's counts summed as door-weeks and labelled apart; defects by registered name in door-weeks; the month before as a reading beside this one, never a share; a stable address per month at `/corpus/month/{YYYY-MM}`; JSON by Accept; a room, a dataset, an atlas door; the brief links it and the guide names it. | The citable artifact press and researchers link asks the question monthly. | `test/monthly-state.spec.ts`: grouping, closing and door-week totals, defects and the month before over synthetic points; no key reads as a share or rank; the page over seeded weeks with the stable address; room, dataset and atlas registered. |
| V4 | **Registry listings by machine.** DONE 2026-09-03: `npm run listings:check` gained its second half (`scripts/lib/listing-versions.mjs`) — the MCP registry's latest version and description against `server.json` and `tab/server.json`, npm's dist-tags against the four package manifests, ClawHub against `registry/clawhub/published.json`, x402-list's offer count and its description's last sentence against the paid shelf and the doctrine, agentic.market's endpoint count against the shelf — one row per fact, agrees / differs (ours and theirs named) / unknown / unreachable, never a score, nothing written to any index; and `.github/workflows/listings-check.yml` runs both halves every Saturday 08:30 UTC with no dependencies, red on drift. The first live read found five differing facts (KEEPER_LIST). | Drift there is invisible revenue loss, and it is invisible from every surface the store serves because the file on disk looks right. | `scripts/listings-check.test.mjs`: every reader offline against fixtures, unknown never becomes differs, the walk never throws on a dead index; the workflow keeps the reading 90 days. |
| V2 | **Feeds.** DONE 2026-09-03: four Atom feeds (`src/routes/feeds.ts`), each derived when fetched from the record the page it mirrors reads — `/feeds/brief.xml` one entry per signed week, `/feeds/corpus.xml` one per signed snapshot linking the exact bytes its signature covers, `/feeds/corrections.xml`, `/feeds/disagreements.xml` — newest first, every entry linking the page the derivation and the denominator are on; the four pages advertise their feed in the head; `/feeds` lists them as a room, a dataset and an atlas door; the guide names it. | Aggregators, newsletters and agents that poll feeds pick the store up without being told. Value if the market takes off, not ROI now. | `test/feeds.spec.ts`: one entry per source row, well-formed Atom with stable ids and dated entries, the head links on all four pages, the index registered with the pointer. |
| V1 | **Storefront slots for the five rooms off the front.** DONE 2026-09-03 by his ruling ("go ahead and do those"): `/operators`, `/observatory`, `/disagreements`, `/trust`, `/profiles` in the storefront's derived footer and structured data; the pulse stays off under its own instruction. | Visibility of what already exists is the cheapest visibility there is. | `test/discoverable.spec.ts` walks every storefront room on the front; `test/pulse.spec.ts` holds the pulse off it. |
| T1 | **The Trade Counter.** DONE 2026-09-03. Marketplaces buy the shelf on account by signed webhook (HMAC over timestamp, nonce, exact body; five-minute window; nonces on a Durable Object, the repo's first non-KV binding). Generic: dialect rows, account rows, a published pricing rule (`retail × 1.2 ÷ (1 − share)`, cents up), a derived receivable at `/api/trade/ledger`, the statement desk on `/admin`. The certificate says `settled_via: trade_account` and carries no chain fields. First account `hal` in test. `TRADE_COUNTER.md`. | His greenlight: "we sell as a product, nothing to do with hal." A marketplace's customers never touch x402; the register still tells the truth about a payment we never see. | `test/trade-counter.spec.ts`: tamper a trade field and the signature is `invalid`; replay is 409; the margin rule holds on every shelf row at every share; the absences on the certificate are asserted by name. |
| R2 | **The reader chains: Ethereum, Arbitrum One, OP Mainnet, Avalanche C-Chain.** DONE 2026-09-03, the third of the ROI list's top three: four more `EvmChain` constants in `src/lib/base-rpc.ts`, each with its CAIP-2, native USDC contract, block cadence, env stem and its own keyless fallbacks; `evmChainOf`, `rpcUrl` and `rpcEndpoints` generalized over the list; every consumer that typed Base and Polygon now derives from `EVM_CHAINS` — the statement rails and their refusal sentence, the coverage claim for `the_statement`, the canonical-USDC registry, the receivability read, the challenge price, the case file's block clock. `WALKED_EVM_CHAINS` holds the bank walk and the census to Base and Polygon: a reader chain is read when named, a walked chain every hour. Nothing on the till changed; PAYMENT_RAILS.md Part F says what was not measured (every RPC host refused from here, so the span is Polygon's 500 and a refused window reads `window_unreadable`). | Circle issues native USDC on six EVM chains and x402 doors advertise them; a statement or a receivability read on four of them answered "not a chain this store reads", which is a fact about our reader, not the wallet. | `test/evm-rails.spec.ts`: six distinct chains, fallbacks unshared, the vocabulary by name and CAIP-2, configured slots before fallbacks on every chain, readers on the conservative span, the walked list unchanged, every consumer derived; the two door-refusal tests now use a chain the store does not read. |
| R5 | **For operators: the shelf from the seller's side.** DONE 2026-09-03, the second of the ROI list's top three ("okay lets do it."): `/operators` (`src/routes/operators.ts`), the eleven seller-side items in the order a launch happens — before you launch, the week you launch, standing, when something goes wrong — with the free instrument named first at each moment (the preflight and its Action, the passport and its chip, the per-host history, the look) and every price read off the shelf through `ladderRung` when the page is served. The stages are editorial; everything a stage asserts about an item is derived, and an id that leaves the shelf drops out of the stage. Registered as a room; the atlas carries the door and a goal in the seller's voice; the guide names it. Nothing there ranks, scores or certifies. | Every paid item on the shelf is bought by somebody who runs a door, and none of them had a front door of its own: an operator met the whole shelf, novelties and all, in the order it was built. | `test/operators.spec.ts`: every stage id is on the shelf and every seller-side id staged once, free first in every stage, the page at one URL with prices from the shelf, the doctrine sentence present, room and atlas registered. |
| R3 | **The badge loop: the chip as something to paste.** DONE 2026-09-03, the first of the ROI list's top three: the passport page opens its colophon with "To paste beside your door" — the chip image, a Markdown snippet and an HTML snippet, each linking back to the passport — and the passport JSON carries the same under `embed` (`passportEmbed` in `src/pages/passport-card.ts`). The chip had existed since S2 and no surface offered it to the operator whose door it describes. | The one distribution surface this store owns that a stranger carries for it, and it went dark rather than stale by construction. | `test/passport-front.spec.ts`: the page and the JSON offer the same chip URL, Markdown and HTML, linking to the passport. |
| L9b | **The observatory: the porch's counts, read.** DONE 2026-09-02, the third of the three the keeper agreed to: `/observatory` (`src/services/observatory.ts`, `src/routes/observatory.ts`) reads the porch ledger the admin desk reads — per month for the pulse's window, per surface, organic visits beside the house and infrastructure buckets kept out of them, the channel split inside the organic count — in name order and never by count, every count a floor with the two bounds quoted from the counter's own constants (a test holds them equal). The counted roster is derived from `PORCH_EXACT`, which now counts the page itself. Registered as a room and a published dataset; the guide names it. It answers the atlas question of 2026-08-29 with a number. | The porch had counted every agent-read surface since 2026-08-21 and nothing public read the counters back: a measurement with no reading. | `test/observatory.spec.ts`: a recorded visit lands under its surface and channel with house beside it, name order, the floors equal the counter's constants, no key reads as a rate or score, room and dataset registered, the page counts itself. |
| C51 | **The disagreement record (house rule 51).** DONE 2026-09-02, the second of the three the keeper agreed to: `/disagreements` (`src/store/disagreements.ts`, `src/routes/disagreements.ts`), where this store's reading and another instrument's diverge — both readings with their derivations and sources, neither marked right, four checkable states (open, withdrawn by us with the correction, withdrawn by them, both stand), the trigger that produced the look, the date it went to the other side first. Seeded with the X-PAYMENT header divergence Cairn settled with half a cent, withdrawn by us 2026-08-26. Registered as a room; linked from the trust panel, the vocabulary page and the guide; the arrangement doc's gap paragraph closed. Nothing polls the counterpart. | Rule 51's second bullet was a commitment with no surface under it, and the arrangement doc said so in its own text. | `test/disagreements.spec.ts`: both readings with derivation and URL, sent before published, a withdrawn-by-us entry names an existing correction, no settled state, the page at one URL with the pointers. |
| L9a | **The statement on Solana (SOLANA_PARITY gap 1).** DONE 2026-09-02, the first of three the keeper agreed to ("Agreed do em"): `src/lib/solana-usdc.ts` walks every USDC token account a wallet owns at read time by `getSignaturesForAddress` paged to the window's first slot, then each transaction's settled pre/post balances (the reconciliation walker's parsing, not a third one); `src/lib/statement-rails.ts` is one artifact shape with three readers, Base and Polygon wrapping the EVM chains untouched. `the_statement` and `operator_statement` take `network=solana` with a base58 pubkey, keep every field, and say `unit: "slot"` beside from/to; the eleven-hour ceiling is in slots on that rail. Past the page or signature cap the read is `window_unreadable` with the reason, never a partial read. The doors refuse a mismatched address before money moves. Gaps 2 to 5 stay in L9 per the parity doc's order. | The only row with demand evidence marked strong, and the Operator's Statement had shipped that morning reading two of the three rails every door advertises. | `test/solana-statement.spec.ts`: rail vocabulary, the walk, both statements, the doors, a purchase end to end; the parity doc's box is ticked with the date. |
| L7 | **The preflight Action: the door check in somebody else's deploy.** DONE 2026-09-02 as `action/preflight/` (`action.yml`, one dependency-free `preflight.mjs`, its own offline tests in CI): `uses: seancrecord/scvd-general-store-repo/action/preflight@main` with `urls`, one POST to `/api/preflight/v2` per door, every check printed by name, the store's JSON kept as a report file, a job summary table, outputs `verdicts` / `worst` / `report_path`. The CLI's exit law kept: `not_ready` fails the job, `unreachable` does not unless `fail_on` says so in writing, a refused URL fails loudly because nothing was probed, a 429 names the wait. Scoped to the store's own battery, not a second verifier: x402-verify stays the library for artifacts. | The row asked for a GitHub Action that fails their deploy; the instrument existed at three doors and none of them lived where a deploy runs. | Offline tests pin every exit code; the manifest installs nothing; DISTRIBUTION.md carries the how and the press that is his. |
| L6 | **The look: what this store holds about one door, free.** DONE 2026-09-02 as `POST /api/look/v1` and the `look_at_door` MCP tool (`src/services/look.ts`, `src/routes/look.ts`): the free preflight's one live probe (v2, same limiter, every refusal inherited) folded with everything the signed chain holds about the host — rounds probed out of rounds since first sighting, the passport tier with its fraction and rows, the last probed round with its failed checks and the catalog's agreement, the passport decision, the shared-wallet fact — and one comparison, `now_against_held`, stated as same / changed / no_prior / not_comparable with both sides named. The held half is derived once per host and held in KV for the depth's hold. Never a score; no threshold SKU. On the atlas, the routing table, how-it-works, the lifecycle table, the guide, WebMCP, and under the surface contract; `test/look.spec.ts`. | An agent with a URL in one hand and a wallet in the other asked two tools and a per-host fetch for one question. The inventory existed; one door now serves it. | No key on the artifact reads as a score; counts travel with denominators; the guide digest re-taken; every free-door surface carries it. |
| S6 | **menu.json / atlas answer shopping agents.** DONE 2026-09-02: `src/lib/shopping-fields.ts`, one derivation for both surfaces — `when` per item is the routing table (`scvd://when`, `ROUTES`) reversed, free instrument first where the counter names one; `sample_url` from the specimen roster (`SAMPLES`); `verify` the one pattern `/api/verify/{cert_id}`, now also the source of the item page's verify line. The atlas's paid doors gain `buy_url`, `listing_url` and the three fields. No new typed category list: delete `ROUTES` or `SAMPLES` and the fields go empty. `test/shopping-fields.spec.ts` holds both surfaces to the same derivation and the item's typed `sample_url` to the roster. | Shopping agents need transactional data. We had it in three places. One derivation. | No new typed category list; both surfaces agree with each other and with their sources. |
| S8 | **Cross-surface consistency.** Designed 2026-09-02: `docs/S8_CROSS_SURFACE_2026-09.md`; the four decisions ruled the same day. PR 1 DONE 2026-09-02: Tier A as three advisories (`discovery-info-fails-schema`, `resource-description-absent`, `offer-contradicts-challenge`), `placement-mismatch` naming the field that moved, vocabulary v8, the `two-surfaces` practice door and fixture, `test/cross-surface-tier-a.spec.ts` walking every own door's served 402 first. PR 3 DONE 2026-09-02: the catalog column (`src/services/catalog-agreement.ts`) on every probed row and `catalog_agreement` on the round, frozen onto the long walk's roster, surfaced on `/corpus/host/{host}.json` and the brief, `our_doors.catalog_differs` for ourselves, the keeper alerted once per change of the differing set; `test/catalog-agreement.spec.ts`. PR 4 DONE 2026-09-02: Tier B as the `surfaces` section on the paid `service_audit` always, same price (`src/services/surface-reads.ts`): llms.txt by the ruled code-span convention, the OpenAPI document's payment fields with the well-known fallback, the challenge's resource URL compared rail by rail, and a bookend 402 that turns a contradiction into `moving`; four states, counts with their denominator, never the verdict; MCP tools/list named as not read; vocabulary v9 `surface-contradicts-challenge`, paid-detectable; the guide's own "Prices, by the convention" section derived from the shelf and held to it by `test/surface-reads.spec.ts`. Three tiers by what the truth costs; eight legitimate differences named so none prints as a contradiction. What remains is PR 2, the v3 fold, on the SOON row. | The thread handed us the design and we were bitten by it ourselves (the ClawHub prices). Nobody on x402-list computes it. | Advisories and rows land without moving a verdict; a fixture with two honest surfaces that disagree is reported with its denominator; the fold waits on dated evidence. |
| S10 | **The Operator's Statement.** DONE 2026-09-02 as `operator_statement`, $21, a 30-day term (his name, price and cadence): the statement's engine on a receiving address, four signed chain reads a day stitched into one block range on the hourly sweep (`src/services/operator-statement.ts`), distinct payers and the largest payer's transfers and USDC as counts beside the totals, the passes we miss counted against us, `the_next_month` on the history and never a renewal; `test/operator-statement.spec.ts`. | Revenue attestation from a party that isn't them. Built, mis-sold at ninety-nine cents to agents; now sold to operators at a cadence. | Cadence term item; payer count and concentration re-derive from the passes; pointer, not renewal. |
| S11 | **The AURa walk as a report.** DONE 2026-09-02 as `aura_walk`, The Aura Walk, $150 (his number), human queue, a week's promise, one a week ⚑ drafted with a waitlist: the cold-agent pass this store runs on itself (`AGENT_UX.md` is the spec; `src/store/aura-walk.ts` mirrors its entry points and measures, and `test/aura-walk.spec.ts` holds the two to each other), sold on a door the buyer names in `url`, run by his hand with Claude Sonnet 5 or Opus 5 by default and weaker models on request in `detail` (his ruling). The completed order carries the report with every transcript attached. Registered on every surface keeper-time is sold on; the copy on the row is ⚑ drafted for his ink. Models of varying capability shop one door; where they stall, refuse, misread the accepts, or pay on the wrong rail; signed, transcripts on the artifact. Commission class; ⚑ price ($50–150 on the desk) and the model runs are his hands. | Human-legible, high-margin, nobody else on the list offers it. The method is already published. | Commission item; transcripts attached; the method page is the spec. |
| S9 | **Named exclusions on the demand numbers.** DONE 2026-09-02: `exclusions` on `/corpus/wallet-facts.json` (house wallets with who and since, house agents, the crawler table) and `src/store/exclusions.ts`, the dated register whose newest row pins both table sizes — `test/named-exclusions.spec.ts` fails a change without its row. Publish, by name and wallet where known, which indexers, scanners and house wallets are excluded from the organic counts (`house-wallets.json` already holds the why), and write a dated row when an exclusion changes a published number. | Their mechanism is more rigorous than our published bot-vs-organic split. One derivation from a file we keep. | `/corpus/wallet-facts.json` carries the list; a change writes a dated row. |
| S7 | **Depth before you buy.** DONE 2026-09-02 (and held ten minutes from the same evening: the speed desk's morning report put the uncached derivation at +429ms on `trust_profile` and +196ms on `spot_check`, with `menu.json` doubled; each derivation now sits in KV under its subject with `derived_at` printed, the trust panel's pattern): `archive_depth` on the 402 of `spot_check`, `trust_profile` and `provenance_check` for the subject named (never a preview of the answer), the archive's own depth on their menu.json rows and item pages, zero printed as zero, never-met stated as never met; `test/depth-before-you-buy.spec.ts`. Free callers see the archive depth for a host — rounds held, weeks spanned, first and last seen — on the 402 body and the item page of `spot_check`, `provenance_check`, `trust_profile`, and any corpus slice, before paying. Zero when zero. | Their ADR-011, and our own gap: we sell history and show nowhere how much of it exists for this host. | Depth derived, printed before the money; a host we never saw says so. |
| S5 | **`/try` as the 60-second demo.** DONE 2026-09-02, his hands: the demo is recorded. Paste a door, preflight; paste a receipt, conformance; buy the cheapest item; verify. | The practice counter is already the demo. Record it; do not rebuild it. | First screen of `/try` is the five minutes. |
| N9 | **The collector cannot pay, structurally.** DONE 2026-09-02: `test/collector-cannot-pay.spec.ts` walks static and dynamic imports from every probe root and fails the build if a signing-capable module is reachable; the one path that existed (the menu's copy importing `FIELD_SPEND_CAP_USD` from the signer) is cut by `src/services/launch-check-terms.ts`. An import-graph test: nothing reachable from the census, preflight, or watch probe path can reach a signing-capable module; paying is the walkabout runner and the launch check, named, reason-logged. Small; may ride the N7(b) branch. | Their ADR. We hold it as policy today; a test makes it a property of the code. | The build fails if the probe graph reaches a signer. |
| N8 | **The Case File, $0.25.** DONE 2026-09-02 as `the_case_file` (`src/services/case-file.ts`; served at `/case/{case_id}`): seven sections present or absent by name, the gaps counted against us, the conflict line when we are a party, idempotent by tx + mandate inside a day, `watchRowsForHost` the one new lookup; `test/case-file.spec.ts` is the red-first list. Price $0.25, keeper-confirmed 2026-09-02. Prompt verbatim: `docs/PROMPTS_2026-09-02.md` §2. After N7 (section 4 cites the tier). The composability list the prompt asks for was filed in chat before code: settlement, reconciliation, mandate, corpus host rows, watch rows by host + window (expected missing), fulfillment log by tx (expected missing), launch check by id. No verdict, ever; "this store is a party" on its face when we are. | The dispute instrument without a court (23b holds): what was observed, what was not, assembled for the human deciding. Composed from primitives already on the shelf. | Every section present/absent stated; gaps counted against us; conflict line iff we are a party; no verdict word in any field; declared never touches observed; price equal in every map. |
| N7 | **Tiers on the passport — doctrine first, then the feature.** Prompt verbatim: `docs/PROMPTS_2026-09-02.md` §1. Two branches in order: (a) DONE 2026-09-02 — the doctrine swap — "never a ranking, and never a verdict without its derivation and denominator beside it" on every surface the old sentence names, dated note on `/criteria`, nothing resigned; ⚑ the sentence is his ink and the x402-list row is his press. (b) DONE 2026-09-02 — the derived tier (observed / established / standing / broken / indeterminate) from the signed per-host history on the passport's own newest-wins fold, the rule typed once on `/criteria` (`src/services/passport-tier.ts` is the only source), the fraction and the rows on the passport, its JSON, the chip, the hosted profile and `/corpus/host/{host}.json`, and `/corpus/tiers.json` alphabetical. `test/passport-tier.spec.ts` is the red-first list; derived-not-typed fails on any tier printed without its fraction. | A stranger said it unprompted (l0: reliability weighted by settled USDC, not directory rank). The tier with its denominator is the answer that stays on this side of "score." | Rule typed once; no tier without its fraction; tiers.json has no position field; old sentence absent from every public surface; the contradicting copy listed and judged for `/corrections`. |
| N5 | **Bazaar / discovery freshness for our own paid doors.** Done 2026-09-01. Every round reads the CDP search index once and records `our_doors` — claimed, found, missing, or could-not-check — on the signed round (public, dated, in the corpus); `/admin/ward` prints it; an alert fires when the missing set changes. Re-register is his press. Extended 2026-09-02 with the other direction: `stale` (retired doors the index still returns — the 11 rows Agent Economy Report scored as down) and `unknown` (paths under `/api/buy/` that are neither on the menu nor retired), alert when the stale set changes. Delisting is a letter to Coinbase, his hand. | Recency decay is how a listed store goes invisible. This is the marketplace half of "do not miss a takeoff." | A miss fails a TEST or writes a dated row. No silent re-buy. |
| N6 | **Walkabout runner to spec.** DONE 2026-09-01: `scripts/walkabout.mjs` + `scripts/lib/walkabout.mjs`, `npm run walkabout:test`, WALKABOUT.md "The runner". First real run reconciles itself; the August gap is a separate `reconcile` against run zero's block range when the keeper wants it. WALKABOUT.md approved 2026-09-01 with a standing weekly approval at the defaults. The August script has the calling-card UA and caps but caps at $0.10 not $0.05, has no sanctions screen, no one-per-domain rule, no Web Bot Auth signing, no corpus-vocabulary output. Reuse the on-chain sanctions oracle in `services/launch-check.ts`. Targets: the 2026-08-18 domain set plus the ward round, not the 100-row feed. First job of the runner: reconcile the August ledger's 180 unrecorded on-chain transfers. | The only observation class nobody else produces is what a door did with real money. CV is the hands with the wallet. | Runner obeys all eight rules; ledger + report committed per run; every number in the report re-derives from the ledger; the August gap stated in dollars. |
| N4 | **Provenance shelf (M5).** Done 2026-09-01 as `provenance_check`, The Company an Address Keeps, $5, copy as approved: the named join from the signed chain, delivered and never published; free self-audit at `/api/provenance/self` (EIP-191 challenge), consent offer at the end, self-audits counted weekly and never logged. | Only new SKU already named. Copy is the gate. | On the shelf, derived, free-own path tested, refusal in the body. |
| N3 | **Sample output + does-not-prove on the top paid SKUs.** Done 2026-09-01. Five specimens (`/samples/{once-over,conformance-watch,night-watch,launch-check,settlement-attestation}.json`), each built by the paid artifact's own arithmetic over a door that cannot exist, unsigned; printed in full on the item page, equal to the JSON byte for byte. Every item that mints an artifact class prints the class's `signs` / `does_not_prove` verbatim on the page, in menu.json and in the markdown twin. `test/item-limits.spec.ts` is the family guard. | Abstract trust is why humans bounce. The honesty is already computed. | Five flagship items. Test pins the guard. |
| N2 | **First-screen packaging.** Done 2026-09-01. The 60 words are one constant (`VALUE_PROPOSITION`); `POSITION_OPENING` opens with them, so the homepage, `/llms.txt`, `/llms-full.txt`, `agents.md`, the OpenAPI description, the MCP handshake and the skill all inherit. OG card carries them verbatim; README opens with them. Three paths into rooms that exist (`/api/preflight/v1`, `/conformance`, `/corpus`) print on the homepage, the guide and the README in the words' own order — no `/start`. `test/first-screen.spec.ts` is the rule 44 sweep. | The dump is right about the bottleneck and wrong about the noun. We already have `POSITION_LINE`. We do not have the extraction-first first screen. | One derived paragraph, six surfaces. Rule 44 sweep. No "verification layer," no "trust layer." |
| N1 | **#82 — paid audit to battery v2.** Done 2026-09-01. Headline cites the census battery. Dated note on `/criteria`. | Two instruments disagreed in public. One was what a buyer paid for. | `AUDIT_CRITERIA_VERSION` matches the census battery. A v1-only fixture fails the paid door. |
| S4 | **Watch as the standing product, said plainly.** Done 2026-09-01: subtitles inked on the four operator instruments. *2026-09-01, derived half done:* both watch histories now carry `the_next_week` — rule 23a on the artifact, the same item priced off the shelf, the door pre-filled — so the week no longer ends in silence. Never a renewal. | Recurring revenue without a new primitive. | Copy only, then rule 44. |
| S3 | **Merchant kit as a bundle, not a new brand.** Done 2026-09-01 as `opening_day`, The Opening Day, $9 (his ink): one launch check, seven days of conformance watch on the same door, the passport page, one certificate, one URL (`/api/opening-day/{cert_id}`). Same engines as the parts; no second battery. | The dump's "launch kit" is packaging of SKUs we sell. | Menu row derived. No second battery. |
| S2 | **Passport on the front.** Done 2026-09-01 (his slot ruling): `/passport` on the storefront, share colophon on every host page. `/passport` and `/passport/{host}` exist and are held off the storefront pending his slot ruling. The dump's "public endpoint passports" is this room, not a new product. Embed is a *colophon* linking the dated page, never "preflight passed." | Merchants share a page that makes them look observed, not approved. Rule 43 / 54. | Slot ruling on the desk. Then `on_storefront` and a share card that carries date, gaps, stale-after. |
| S1 | **Weekly corpus brief a stranger can quote.** Done 2026-09-01 as The Week's Doors, `/corpus/brief` (his name). Short. Hosts observed, payable vs not, named defect classes, gaps counted against us. Not a "State of x402 Checkout" brand until he inks the name. Not a ranking. `/corpus` + trajectory + `/doors` are the feed; this is the human/agent digest. | Original dated data is how we get cited. Gazette self-drafting is retired; the data is not. | One signed page or row per week, derived from the snapshot, screenshot-able without becoming a leaderboard. |

---

## NOT THIS ROADMAP

Struck or already live. Do not reopen. Do not rebuild.

**Already live — the dump often asks for these.**
`llms.txt` / `llms-full.txt`, `agents.md`, sitemap +
`.md`, `menu.json`, `atlas.json`, OpenAPI, MCP, x402,
ARD, A2A, DID / key history, corpus JSON, `/conformance`,
`/try`, `/samples`, `/doors`, `/passport`, `/coverage`,
`/when`, visit stamps, JSON-LD (Organization, WebSite,
Product, Offer, Service, ItemList, FAQPage, Dataset),
`/pricing.md`, `index.md`, Web Bot Auth desk, watches,
launch check, settlement attestation, bazaar self-row
in the ward round.

**Already the wrong noun.** "Independent receipt and
endpoint verification layer." "Trust-but-verify desk."
"Commercial evidence authority." Category is evidence
observatory. `POSITION_LINE` already says it. `/becoming`
still carrying "trust layer" is a desk ruling, not a
rebrand.

**Scores and their cousins.** Leaderboards. "Preflight
passed" badges. Agent-readiness scorecard. Rating
dashboards. KYA / wallet trust-score. Reputation
portable scores. Health index. TraceRank feed.
AggregateRating. Wikipedia `sameAs`. A numeric 0–100
anything. *A tier with its fraction and its rows (N7) is
not on this list; a tier without them is.*

**New page trees.** `/for-builders`, `/for-merchants`,
`/x402/conformance`. Rooms exist. Retrieval is titles
and first-screen answers, not a parallel sitemap.

**New primitives that reopen 23b or 17.** Escrow.
Dispute court. Insurance underwriting (named buyer
first, we remain the evidence, they hold the risk).
Broad payment-approval widgets. Managed key-succession
as a product (we have not done it for ourselves).

**More AI-visibility files for their own sake.** The
dump's own warning. Markdown twins where no markdown
document exists are a 404 on purpose.

---

## How this file is used

- One item per branch.
- ⚑ stops for the desk.
- Copy is drafted here only as a suggestion; he inks.
- When the market moves, *pull a LATER row forward* —
  do not start a third list.
- A row moves to DONE the day it closes, newest first.
  NOW is re-ordered whenever a ruling or the market
  changes the order; the top row is always the next
  branch.
